Definition 2.11 [22] A monad on a category C is a triple $(T,\eta,\_^{\dagger})$ consisting of a map $T$ from objects $X$ of C to objects $TX$ of C, a collection $\eta=(\eta_{X})_{X}$ of morphisms $\eta_{X}\colon X\to TX$, one for each object $X$ of C, and a so-called extension operation $\_^{\dagger}$ that maps every morphism $f\colon X\to TY$ to $f^{\dagger}\colon TX\to TY$ such that: 1. $\eta_{X}^{\dagger}=\mathrm{id}_{TX}$; 2. for every morphism $f\colon X\to TY$, $f^{\dagger}\circ\eta_{X}=f$; 3. for all morphisms $f\colon X\to TY$ and $g\colon Y\to TZ$, $g^{\dagger}\circ f^{\dagger}=(g^{\dagger}\circ f)^{\dagger}$.
Definition 4.1 Let $C$ be a semitopological cone, and $\nu$ be a continuous valuation on $C$. A barycentre of $\nu$ is any point $b_{\nu}\in C$ such that, for every linear lower semi-continuous map $\Lambda\colon C\to\overline{\mathbb{R}}_{+}$, $\Lambda(b_{\nu})=\int\Lambda~{}d\nu$.
Definition 5. A $C$-monomial $G$-poset is a pair $(X,\mathfrak{l})$ consisting of a $G$-poset $X$ and a functor $\mathfrak{l}:\widehat{X}\rightarrow\bullet_{C}$.
Definition 1. A $C$-monomial $G$-set is a pair $(X,\mathfrak{l})$ consisting of a finite $G$-set $X$ and a functor $\mathfrak{l}:\widehat{X}\rightarrow\bullet_{C}$.
Definition 21. The above construction $T_{U,\lambda}:(X,\mathfrak{l})\mapsto\big{(}t_{U,\lambda}(X,\mathfrak{l}),% \mathfrak{L}_{U,\lambda}\big{)}$ is called the generalized tensor induction for $C$-monomial $G$-posets, associated to $(U,\lambda)$.
Definition 3.15. (CassidyClassification, , page 222) A connected differential algebraic group is called simple if it is non-commutative and has no nontrivial connected normal differential algebraic subgroups.
Definition 3.1. A neutral differential Tannakian category over a differential field $(\operatorname{\bf k},\Delta=\{\partial_{1},\ldots,\partial_{m}\})$ is a rigid tensor abelian category $\operatorname{\mathcal{C}}$ with $\operatorname{\mathrm{E}nd}_{\operatorname{\mathcal{C}}}({\mathbbm{1}})=% \operatorname{\bf k}$, where ${\mathbbm{1}}$ is the unit object in $\operatorname{\mathcal{C}}$, supplied with the following data: 1. functors $\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}:\operatorname{% \mathcal{C}}\to\operatorname{\mathcal{C}}$, $1\leqslant i\leqslant m$, with exact sequences of functors $\begin{CD}0@>{}>{}>\operatorname{\mathrm{i}d}_{\operatorname{\mathcal{C}}}@>{% \iota_{i}}>{}>\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}@>{\pi_{% i}}>{}>\operatorname{\mathrm{i}d}_{\operatorname{\mathcal{C}}}@>{}>{}>0,\end{CD}$ that is, a functorial exact sequence $\begin{CD}0@>{}>{}>X@>{\iota_{X,i}}>{}>\operatorname{\mathrm{F}}_{% \operatorname{\mathcal{C}},i}(X)@>{\pi_{X,i}}>{}>X@>{}>{}>0\end{CD}$ for any object $X$ in $\operatorname{\mathcal{C}}$, such that the differential structure induced by all the $\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}$’s on $\operatorname{\bf k}$ coincides with the given $\Delta$ (see (difftann, , Lemma 7) and (Moshe, , Section 5.2.2)) 2. functorial isomorphisms $S_{i,j}:\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(\operatorname% {\mathrm{F}}_{\operatorname{\mathcal{C}},j}(X))\stackrel{{\scriptstyle\sim}}{{% \longrightarrow}}\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},j}(% \operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(X))$ for all $1\leqslant i,j\leqslant m$ and $X\in{\mathrm{O}b}(\operatorname{\mathcal{C}})$; 3. functorial morphisms $T_{X,Y,i}:\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(X\otimes Y)% \to\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(X)\otimes% \operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(Y)$ for all objects $X$ and $Y$ in $\operatorname{\mathcal{C}}$ and $1\leqslant i\leqslant m$; 4. an exact tensor $k$-linear functor $\omega:\operatorname{\mathcal{C}}\to\operatorname{\bf Vect}_{\operatorname{\bf k}}$ with functorial isomorphisms of $\operatorname{\bf k}$-vector spaces $\Gamma_{X,i}:\omega(\operatorname{\mathrm{F}}_{\operatorname{\mathcal{C}},i}(X% ))\stackrel{{\scriptstyle\sim}}{{\longrightarrow}}F_{i}(\omega(X))$ for any object $X$ in $\operatorname{\mathcal{C}}$, such that $\Gamma$ sends exact sequences of type (3) to exact sequences of type (1), isomorphisms of type (4) to isomorphisms of type (2), and morphisms of type (5) to the morphisms of the following type: $T_{U,V,i}:F_{i}(U\otimes_{\operatorname{\bf k}}V)\to F_{i}(U)\otimes_{% \operatorname{\bf k}}F_{i}(V)$ given by the formula $\displaystyle 1\otimes(u\otimes v)$ $\displaystyle\mapsto(1\otimes u)\otimes(1\otimes v),$ $\displaystyle\partial_{i}\otimes(u\otimes v)$ $\displaystyle\mapsto(\partial_{i}\otimes u)\otimes(1\otimes v)+(1\otimes u)% \otimes(\partial_{i}\otimes v),$ for all $u\in U$, $v\in V$, and $1\leqslant i\leqslant m$. Such a functor $\omega$ is called a differential fibre functor.
Definition 3.6. Such $G$ is called a unipotent LDAG.
Definition 4.1. A group $\Gamma$ is a product of its subgroups $M$ and $N$ if the product morphism $M\times N\to\Gamma$ is surjective and $mn=nm$ for all $m\in M$, $n\in N$. In this case, we write $\Gamma=MN.$ If $|M\cap N|<\infty$, we say that $\Gamma$ is an almost direct product of $M$ and $N$, and we write $\Gamma=M\cdot N.$
Definition 3.12. A linear differential algebraic group $G$ is called reductive if its unipotent radical is trivial, that is, $\operatorname{R_{u}}(G)=\{e\}$.
Definition 2.1. For a differential field extension $K\supset\operatorname{\bf k}$, a Kolchin closed subset $W(K)$ of $K^{n}$ over $\operatorname{\bf k}$ is the set of common zeroes of a system of differential algebraic equations with coefficients in $\operatorname{\bf k},$ that is, for $f_{1},\ldots,f_{k}\in\operatorname{\bf k}\{Y\}$ we define $W(K)=\left\{a\in K^{n}\>|\>f_{1}(a)=\ldots=f_{k}(a)=0\right\}.$
Definition 3.16. (CassidyClassification, , page 222) A connected differential algebraic group is called semisimple if it has no nontrivial connected normal commutative differential algebraic subgroups.
Definition 3.11. The subgroup $\operatorname{R_{u}}(G)$ is called the unipotent radical of $G$.
Definition 4.4. A LDAG $G\subset{\bf GL}_{n}(\operatorname{\mathcal{U}})$ is called constant-dense, if there is a LAG $G_{0}\subset{\bf GL}_{n}({\rm C})$ such that $G_{0}\subset G\subset\overline{G_{0}}.$
Definition 2.3. CassidyRep Let $G$ be a LDAG. A differential polynomial group homomorphism $\rho:G\to{\bf GL}(V)$ is called a differential representation of $G$, where $V$ is a finite dimensional vector space over $\operatorname{\bf k}$. Such a space is called a $G$-module. As usual, morphisms between $G$-modules are $\operatorname{\bf k}$-linear maps that are $G$-equivariant. The category of differential representations of $G$ is denoted by $\operatorname{\bf Rep}_{G}$.
Definition 2.2. (Cassidy, , Chapter II, Section 1, page 905) A linear differential algebraic group is a Kolchin closed subgroup $G$ of ${\bf GL}_{n}(\operatorname{\mathcal{U}}),$ that is, an intersection of a Kolchin closed subset of $\operatorname{\mathcal{U}}^{n^{2}}$ with ${\bf GL}_{n}(\operatorname{\mathcal{U}})$, which is closed under the group operations.
Definition 2.2. Let $\alpha$ be a regular cardinal. A morphism $X\to Y$ in an arbitrary category is called (1) $\alpha$-pure monomorphism, if it is an $\alpha$-filtered colimit of split monomorphisms, (2) $\alpha$-pure epimorphism, if it is an $\alpha$-filtered colimit of split epimorphisms, and (3) $\alpha$-terminal if for every factorisation $X\to X^{\prime}\to Y$ the morphism $X\to X^{\prime}$ is invertible if it is an $\alpha$-pure epimorphism.
Definition 10 Let $\mathbf{M}=(M,m,u)$ be a monad on $Set$. A pair of $Set$-endofunctors $(T,H)$ such that $HM\cong MT$ is called an $\mathbf{M}$-commuting pair.
Definition 1.4. A class $\mathcal{F}$ of objects in $\mathcal{G}$ is called deconstructible if there is a set $\mathcal{S}$ such that $\mathcal{F}=\hbox{\rm Filt-}{\mathcal{S}}$.
Definition 1.3. Let $\mathcal{S}$ be a class of objects of $\mathcal{G}$. An object $X\in\mathcal{G}$ is called $\mathcal{S}$-filtered if there exists a well-ordered direct system $(X_{\alpha},i_{\alpha\beta}\mid\alpha<\beta\leq\sigma)$ indexed by an ordinal number $\sigma$ such that (1) $X_{0}=0$ and $X_{\sigma}=X$, (2) for each limit ordinal $\mu\leq\sigma$, the colimit of the subsystem $(X_{\alpha},i_{\alpha\beta}\mid\alpha<\beta<\mu)$ is precisely $X_{\mu}$, the colimit morphisms being $i_{\alpha\mu}:X_{\alpha}\to X_{\mu}$, (3) $i_{\alpha\beta}:X_{\alpha}\to X_{\beta}$ is a monomorphism in $\mathcal{G}$ for each $\alpha<\beta\leq\sigma$, (4) $\operatorname{Coker}i_{\alpha,\alpha+1}\in\mathcal{S}$ for each $\alpha<\sigma$. The direct system $(X_{\alpha},i_{\alpha\beta})$ is then called an $\mathcal{S}$-filtration of $X$. The class of all $\mathcal{S}$-filtered objects in $\mathcal{G}$ is denoted by $\hbox{\rm Filt-}{\mathcal{S}}$.
Definition 2.6. Let $\mathcal{F}\subseteq\mathcal{G}$ be a class of objects and $\kappa$ a regular cardinal. Then $\mathcal{F}$ is said to be a $\kappa$-Kaplansky class if for any $F\in\mathcal{F}$ and a $<\kappa$-generated subobject $X\subseteq F$, there exists a $<\kappa$-presentable subobject $Y$ of $F$ such that $X\subseteq Y\subseteq F$ and $Y,F/Y\in\mathcal{F}$. We say that $\mathcal{F}$ is a Kaplansky class if it is $\kappa$-Kaplansky for some regular cardinal $\kappa$.
Definition 2.8. A subcategory $\mathcal{K}$ of a category $\mathcal{L}$ will be called nearly full if for each commutative triangle
Definition 2.4. A functor $F:\mathcal{K}\to\mathcal{L}$ will be called well $\lambda$-accessible if $\mathcal{K}$ and $\mathcal{L}$ are well $\lambda$-accessible categories and $F$ preserves $\lambda$-directed colimits and $\mu$-presentable objects for each $\lambda\leq\mu$. $F$ is called well accessible if it is well $\lambda$-accessible for some regular cardinal $\lambda$.
Definition 2.1. We say that a category $\mathcal{K}$ is well $\lambda$-accessible if it is $\mu$-accessible for each regular cardinal $\lambda\leq\mu$. $\mathcal{K}$ is well accessible if it is well $\lambda$-accessible for some regular cardinal $\lambda$.
Definition 2.6. We say that a functor $F:\mathcal{K}\to\mathcal{L}$ reflects split epimorphisms if $f$ is a split epimorphism whenever $F(f)$ is a split epimorphism.
Definition 1.1. Let $\mathcal{V}$ be a variety of algebras and $\gamma:\mathcal{V}\to{\mathcal{S}et}$ the forgetful functor. A functor $F:\mathcal{C}\to\mathcal{V}$ is called representable if $\gamma\circ F:\mathcal{C}\to{\mathcal{S}et}$ is representable in the classical sense.
Definition 3.10. Let $R$, $S$ be two rings, $C$ an $R$-coring, $U\in{}_{S}\mathcal{M}_{R}$ and $V\in{}_{R}^{C}{\mathcal{M}}_{S}$. A pair of maps $(p,E)$, where $p\in{}_{R}^{C}{\rm Hom}_{R}(C,V\otimes_{S}U)$, $E\in{}_{S}{\rm Hom}_{S}(U\otimes_{R}V,S)$ such that the following diagrams
Definition 3.5. Let $R$, $S$ be two rings, $C$ an $R$-coring, $U\in{}_{S}\mathcal{M}_{R}$ and $V\in{}_{R}^{C}{\mathcal{M}}_{S}$. A pair $(e,h)$, where $e=\sum e^{1}\otimes e^{2}\in\bigl{(}U\otimes_{R}V\bigl{)}^{S}$, $h\in{}_{R}{\rm Hom}_{R}(V\otimes_{S}U\otimes_{R}C,R)$, such that $v_{<-1>}h(v_{<0>}\otimes_{S}u\otimes_{R}c)=h(v\otimes_{S}u\otimes_{R}c_{(1)})c% _{(2)}$ $\sum e^{1}\,h(e^{2}\otimes_{S}u\otimes_{R}c)=u\varepsilon(c)$ $\sum h(v\otimes_{S}e^{1}\otimes_{R}e^{2}_{<-1>})e^{2}_{<0>}=v$ for all $v\in V$, $u\in U$, $c\in C$ is called a comodule dual basis of first kind for $(U,V)$.
Definition 2 We say that it is representable if for every $T\in{\cal T}(n)$ there exists a family $F_{x}^{T}\in Funct_{S_{T}}(n)(S_{T})$ and an isomorphism of families of $k$- vector spaces $P_{T}(\{X_{i}\},Y)\rightarrow Hom_{{\cal C}}(F_{x}^{T}(\{X_{i}\}),Y)$ which is compatible with the operadic composition on both families. It is also required that for the only tree $e\in{\cal T}(1)$ we have: $Hom_{{\cal C}}(F_{x}^{e}(Y),Y)$ corresponds to ${\bf 1}_{Y}$ under this isomorphism.
Definition 1 A pseudo-monoidal category ${\cal C}$ of ringed spaces is given by the following data: a) a class ${\cal C}$ called class of objects; b) for every $T\in{\cal T}(n)$ , a sequence ${\{X_{i}\}},1\leq i\leq n$ of objects of ${\cal C}$ and an object $Y\in{\cal C}$ a family of $k$-vector spaces $P_{T}(\{X_{i}\},Y)$ over $S_{T}$ (operations from $\{X_{i}\}$ to $Y$); c) for every morphism $f:T^{\prime}\rightarrow T$ a morphism $\phi_{f}:P_{T^{\prime}}(\{X_{i}\},Y)\rightarrow(l_{f})^{*}P_{T}(\{X_{i}\},Y)$ of families over $S_{T^{\prime}}$; d) for an operadic composition of spaces $\gamma:S_{T}\times\prod_{i}S_{T_{i}}\rightarrow S_{T(T_{1},...,T_{n})}$ we are given a morphism of families on $S_{T}\times\prod_{i}S_{T_{i}}$ (composition of operations): $\Phi_{\gamma}:P_{T}(\{X_{i}\},Y)\times\prod_{i}P_{T_{i}}(\{K_{j_{i}}\},X_{i})% \rightarrow\gamma^{*}P_{T(T_{1},...,T_{n})}(\{K_{j}\},Y)$ , $\Phi_{\gamma}(\phi,(\psi_{i}))=\phi(\psi_{i})$. Here $\{X_{i}\}$ are parametrized by the tails of $T$, $\{K_{j}\}$ are parametrized by the tails of $T(T_{1},...,T_{n})$ and $\{K_{j_{i}}\}$ corresponds to the subsequence of objects parametrized by the tails of $T_{i}$; e) for every object $X\in{\cal C}$ there exists a family ${\bf 1}_{X}\in P_{e}(\{X\},X)$ on $S_{e}$ such that $\phi({\bf 1}_{X_{i}})=\gamma^{*}(\phi)$ and ${\bf 1}_{X}(\phi)=\eta^{*}(\phi)$. Here $\phi\in P_{T}(\{X_{i}\},X)$ and we use the notations $\gamma$ and $\eta$ for the natural operadic morphisms of spaces (see d)). The composition morphisms $\Phi_{\gamma}$ from d) are required to be associative in the following sense: $\Phi_{\gamma}(id\times\Phi_{\delta})=\Phi_{\delta}(\Phi_{\gamma}\times id)$ (when applied to the corresponding families).
Definition 3 We say that we are given an infinitesimal chiral braiding on ${\cal C}_{X}$ if a) the above-mentioned isomorphisms are functorial with respect to $M_{i}$; b) they are compatible with the natural embeddings $X^{n}\to X^{m},n\leq m$ in the sense that if $\sigma\in S_{m}$ permutes a subsequence of $\{1,...,m\}$ consisting of $n$ elements then $c_{\sigma},\sigma\in S_{m}$ acts as $c_{\bar{\sigma}}\otimes id$ where $\bar{\sigma}$ is the corresponding element of $S_{n}$. If the above mentioned structure exists globally on the complement to each main diagonal $\{x_{1}=x_{2}=...=x_{n}\}$ then we say that ${\cal C}_{X}$ carries a chiral braiding.
Definition \thetheorem Let $\cal E$ be an exact category. A sequence $x\longrightarrow y\longrightarrow z$ in $\cal E$ is called exact at $y$ if \thetheorem.1 The map $x\longrightarrow y$ factors as $x\longrightarrow y^{\prime}\longrightarrow y,$ with $x\longrightarrow y^{\prime}$ an admissible epi, and $y^{\prime}\longrightarrow y$ an admissible mono. \thetheorem.2 The map $y\longrightarrow z$ factors as $y\longrightarrow y^{\prime\prime}\longrightarrow z,$ with $y\longrightarrow y^{\prime\prime}$ an admissible epi, and $y^{\prime\prime}\longrightarrow z$ an admissible mono. \thetheorem.3 $y^{\prime}\longrightarrow y\longrightarrow y^{\prime\prime}$ is an admissible short exact sequence.
Definition \thetheorem The bisimplicial set $Gr^{b}({\cal E})^{\hbox{\begin{picture}(21.0,21.0) \put(2.0,5.0){\line(0,1){16.0}} \put(0.0,21.0){\oval(4.0,4.0)[br]} \put(4.0,21.0){\oval(4.0,4.0)[bl]} \put(5.0,2.0){\line(1,0){16.0}} \put(21.0,4.0){\oval(4.0,4.0)[bl]} \put(21.0,0.0){\oval(4.0,4.0)[tl]} \end{picture}}}$ is defined as follows. A $(p,q)$–simplex is a diagram in $Gr^{b}({\cal E})$ $\vdots$$\vdots$$\cdots$$\cdots$${X_{00}}$${X_{0q}}$${X_{p0}}$${X_{pq}}$ together with a coherent differential $X_{pq}\longrightarrow\Sigma X_{00}$. The condition is that, for every $0\leq i\leq i^{\prime}\leq p$, $0\leq j\leq j^{\prime}\leq q$, the sequence $\Sigma^{-1}X_{i^{\prime}j^{\prime}}\longrightarrow X_{ij}\longrightarrow X_{i^% {\prime}j}\oplus X_{ij^{\prime}}\longrightarrow X_{i^{\prime}j^{\prime}}% \longrightarrow\Sigma X_{ij}$ gives, in each degree, an exact sequence in $\cal E$.
Definition 3.1. A sequence of objects $(e_{1},\dots,e_{n})$ is called numerically semiorthonormal if $\chi(e_{i},e_{i})=1$ and $\chi(e_{j},e_{i})=0$ whenever $j>i$. It is called a numerically semiorthonormal basis if moreover $e_{1},\dots,e_{n}$ is a basis of $\mathrm{K}_{0}(Z)/(\mathrm{tors})$. This is equivalent to $n=\mathrm{rk}(\mathrm{K}_{0}(Z)/(\mathrm{tors}))$.
Definition 2.3. A semiorthogonal decomposition $\mathcal{T}=\langle\mathcal{S}_{1},\dots,\mathcal{S}_{n}\rangle$ is called maximal if each $\mathcal{S}_{i}$ does not have any nontrivial semiorthogonal decompositions. The category $\mathcal{T}$ satisfies the Jordan-Hölder property if, firstly, $\mathcal{T}$ satisfies the Noetherian property, and, secondly, if $\mathcal{T}=\langle\mathcal{S}_{1},\dots,\mathcal{S}_{n}\rangle$ and $\mathcal{T}=\langle\mathcal{U}_{1},\dots,\mathcal{U}_{m}\rangle$ are two maximal semiorthogonal decompositions, then $m=n$ and there exists a bijection $\sigma\colon\{1,\dots,n\}\xymatrix@1@=15pt{\ar[r]&}\{1,\dots,m\}$ such that $\mathcal{S}_{i}$ is equivalent to $\mathcal{U}_{\sigma(i)}$.
Definition 2.1. A subcategory $\mathcal{S}\subset\mathcal{T}$ is called admissible if the inclusion functor has a left and right adjoint. A sequence of admissible subcategories $(\mathcal{S}_{1},\dots,\mathcal{S}_{n})$ of $\mathcal{T}$ is called a semiorthogonal decomposition of $\mathcal{T}$ if $\mathrm{Hom}(S_{j},S_{i})=0$ for all objects $S_{j}\in\mathcal{S}_{j}$, $S_{i}\in\mathcal{S}_{i}$ with $j>i$, and, moreover, the subcategories $\mathcal{S}_{i}$ generate $\mathcal{T}$ in the sense that the smallest triangulated subcategory containing all of them is equivalent to $\mathcal{T}$. One writes $\mathcal{T}=\langle\mathcal{S}_{1},\dots,\mathcal{S}_{n}\rangle.$
Definition 2.5. Let $\mathfrak{D}$ be a maximal semiorthogonal decomposition of $\mathcal{T}=\mathrm{D}^{b}(X)$ where $X$ is a smooth projective variety of dimension $n$: $\mathcal{T}=\langle\mathcal{S}_{1},\dots,\mathcal{S}_{n}\rangle.$ The Clemens-Griffiths component $\mathrm{CG}(\mathfrak{D})$ of $\mathfrak{D}$ is defined as the smallest triangulated subcategory of $\mathcal{T}$ which contains all those $\mathcal{S}_{i}$ with $\mathrm{ess.dim}(\mathcal{S}_{i})\geq n-1$.
Definition 3.9. Consider the lattice $\Lambda:=\left\{x+y_{0}h+y_{1}e_{1}+\dots+y_{8}e_{8}+\frac{1}{2}zp\right\}% \subset\mathrm{CH}^{*}(S)[1/2]$ where $(x,y_{0},y_{1},\dots,y_{8},z)\in\mathbb{Z}^{11}$ and $p$ is the class of a point. We set $v=(x,y,z)$, where $y=y_{0}h+y_{1}e_{1}+\dots+y_{8}e_{8}.$
Definition 2.2. The category $\mathcal{T}$ satisfies the Noetherian property if every increasing sequence $\mathcal{S}_{1}\subset\mathcal{S}_{2}\subset\dots$ of admissible subcategories becomes stationary.
Definition 2.6. An object $E\in\mathrm{D}^{b}(X)$ is called exceptional if $\mathrm{RHom}^{\bullet}(E,E)\simeq\mathbb{C}$. An exceptional sequence is a sequence of exceptional objects $(E_{1},\dots,E_{m})$ with $\mathrm{RHom}^{\bullet}(E_{j},E_{i})=0$ for $j>i$. The sequence is called unextendable if there does not exist an exceptional object $F$ in $\mathrm{D}^{b}(X)$ such that $(E_{1},\dots,E_{m},F)$ is an exceptional sequence.
Definition 2.4. The essential dimension $\mathrm{ess.dim}(\mathcal{T})$ of $\mathcal{T}$ is the minimum integer $n$ such that there exists a fully faithful embedding $\mathcal{T}\xymatrix@1@=15pt{\ar@{{}^{(}->}[r]&}\mathrm{D}^{b}(X)$ with admissible image, where $X$ is a smooth projective variety of dimension $n$. We set $\mathrm{ess.dim}(\mathcal{T})=\infty$ if no such integer exists.
Definition 4.6. A ringed category is a pair $(\mathcal{X},\mathcal{O})$ where $\mathcal{X}$ is a category with finite products and $\mathcal{O}$ is a ring object in $\mathcal{X}$; we refer to $\mathcal{O}$ as the structure ring object of $(\mathcal{X},\mathcal{O})$. A morphism of ringed categories $(\mathcal{X},\mathcal{O}_{\mathcal{X}})\to(\mathcal{Y},\mathcal{O}_{\mathcal{Y% }})$ is a pair $(f_{*},\underline{f})$ where $f_{*}\colon\mathcal{X}\to\mathcal{Y}$ is a functor in $\mathsf{fpCat}$ and $\underline{f}\colon\mathcal{O}_{\mathcal{Y}}\to f_{*}\mathcal{O}_{\mathcal{X}}$ is a morphism in $\mathsf{RingObj}(\mathcal{Y})$. Given morphisms $(f_{*},\underline{f})\colon(\mathcal{X},\mathcal{O}_{\mathcal{X}})\to(\mathcal% {Y},\mathcal{O}_{\mathcal{Y}})$ and $(g_{*},\underline{g})\colon(\mathcal{Y},\mathcal{O}_{Y})\to(\mathcal{Z},% \mathcal{O}_{\mathcal{Z}})$, we define their composite to be the morphism $(g_{*},\underline{g})\circ(f_{*},\underline{f})=(g_{*}\circ f_{*},\underline{g% }\circ g_{*}(\underline{f})).$ This operation is readily seen to be associative. The category $\mathsf{RingedCat}$ has ringed categories for objects, with morphisms and composition rule as above. The assignment $(\mathcal{X},\mathcal{O}_{\mathcal{X}})\mapsto\Gamma(\mathcal{X},\mathcal{O}_{% X})$ defines a functor $\Gamma\colon\mathsf{RingedCat}^{\mathrm{op}}\to\mathsf{Ring}$, which we also refer to as the global sections functor.
Definition 2.1. An object $X$ of $\widehat{\mathbf{C}}$ is trivial if $X:\mathbf{C}^{op}\rightarrow\mathbf{sSet}$ factors as $X:\mathbf{C}^{op}\rightarrow\ast\rightarrow\mathbf{sSet}$, i.e., $X$ is a constant functor.
Definition 2.13. Let $G$ be simplicial monoid, we define $G^{\star}$ the simplicial monoid of homotopy invertible elements as the pullback: $\xymatrix{G^{\star}\ar[r]\ar[d]&G\ar[d]\\ ^{\star}\ar[r]&[\pi_{0}G]}$ where $[\pi_{0}G]^{\star}$ is the set of invertible elements of the discrete monoid $[\pi_{0}G]$.
Definition 2.4. Suppose that $X$ is a trivial object in $\widehat{\mathbf{C}}$, we denote by $\mathbf{X}_{\downarrow\Delta}$ the category where • Object are maps $\Delta^{n}\rightarrow X$ in $\widehat{\mathbf{C}}$. • Morphisms are commutative diagrams in $\widehat{\mathbf{C}}$ $\xymatrix{\Delta^{n}\ar[rr]^{-}{f}\ar[dr]&&\Delta^{m}\ar[dl]\\ &X&}$
Definition 3.5 (Categorical classifying space). Let $G$ a topological monoid group like, define the category $\mathbf{BG}$ as follows: • Objects are cofibrant $G$-spaces equivalent to $G$ (in the projective model structure $\widehat{\mathbf{G}}$). • Morphisms are $G$-maps $f:X\rightarrow Y$ which are weak equivalence.
Definition 2.3. (M-principle bundle) Let $X$ be a space and $\mathbf{M}$ a simplicial category with one object $\ast$ i.e., it is equivalent to give a simplicial monoid $M=\mathbf{M}(\ast,\ast)$ of endomorphisms. We call $M$-principle bundle any object $f:E\rightarrow X$ of $\mathbf{Ext}_{\mathbf{M}}(M,X)$ such that $f$ is a fibration of spaces.
Definition 3.3 ($G$-space). A $G$-space $X$ is a topological space with a continues action of $G$.
Definition 2.2. Let $X$ a trivial object of $\widehat{\mathbf{C}}$ and $F$ be any objects of $\widehat{\mathbf{C}}$, define a new category $\mathbf{Ext}_{\mathbf{C}}(F,X)$, where objets are maps $E\rightarrow X$ in $\widehat{\mathbf{C}}$, such that the homotopy pullback $\mathrm{holim}(\ast\rightarrow X\leftarrow E)$ is equivalent to $F$. Morphisms are commutative diagrams: $\xymatrix{E\ar[rr]^{-}{f}_{\sim}\ar[dr]&&E^{{}^{\prime}}\ar[dl]\\ &X&}$ where $f:E\rightarrow E^{{}^{\prime}}$ is a weak equivalence in $\widehat{\mathbf{C}}$.
Definition 2.5. Let $\mathbf{M}$ a model category and $X\in\mathbf{M}$ any object, define $\mathbf{M}_{X}^{we}$ to be the subcategory of $\mathbf{M}$ whose objects are equivalent to $X$ and morphisms are weak equivalences.
