Definition 2.2. Let $\mathcal{M}$ be a biclosed monoidal category, $\mathcal{M}^{op}$ a contragradient category as above. For $M\in\text{w-}\mathcal{Z}_{\mathcal{M}}(\mathcal{M}^{op})$ let $\sigma_{M}:M\to M$ denote the image of $Id_{M}$ under $\mathop{\rm Hom}\nolimits_{\mathcal{M}}(M,M)\simeq\mathop{\rm Hom}\nolimits_{% \mathcal{M}}(1,M\blacktriangleright M)\to\mathop{\rm Hom}\nolimits_{\mathcal{M% }}(1,M\blacktriangleleft M)\simeq\mathop{\rm Hom}\nolimits_{\mathcal{M}}(M,M),$ where the map in the middle is postcomposition with $\tau$ and the isomorphisms come from definitions of actions in $\mathcal{M}^{op}$.
Definition 3.1. A quasi-bialgebra is a collection $(A,\Delta,\varepsilon,\Phi)$, where $A$ is an associative $k$-algebra with unity, $\Delta:A\rightarrow A\otimes A$ and $\varepsilon:A\rightarrow k$ are homomorphisms of algebras, $\Phi\in A\otimes A\otimes A$ is an invertible elements, such that the following equalities hold: $(\text{id}\otimes\Delta)\ (\Delta(a))=\Phi\cdot((\Delta\otimes\text{id})\ (% \Delta(a)))\cdot\Phi^{-1}\qquad\forall a\in A$ $[(id\otimes id\otimes\Delta)(\Phi)]\cdot[(\Delta\otimes id\otimes id)(\Phi)]=(% 1\otimes\Phi)\cdot[(id\otimes\Delta\otimes id)(\Phi)]\cdot(\Phi\otimes 1)$ $(\varepsilon\otimes id)\ (\Delta(a))=a,\qquad(id\otimes\varepsilon)\ (\Delta(a% ))=a,\qquad\forall a\in\mathcal{A}$ $id\otimes\varepsilon\otimes id\ (\Phi)=1\otimes 1$
Definition 3.4. Let $(H,\Delta,\varepsilon,\Phi)$ be a quasi-bialgebra. Then it is called a quasi-Hopf algebra if there exist $\alpha,\ \beta\in H$ and anti-automorphism $S:H\rightarrow H$, such that $S(h^{1})\alpha h^{2}=\epsilon(h)\alpha,$ $h^{1}\beta S(h^{2})=\epsilon(h)\beta.$ If one keeps notation as in Remark 3.2, then there should be equalities $X\beta S(Y)\alpha Z=1,$ $S(P)\alpha Q\beta R=1.$
Definition 2.1. The weak center $\text{w-}\mathcal{Z}_{\mathcal{M}}(\mathcal{N})$ of a $\mathcal{M}$-bimodule category $\mathcal{N}$ consists of objects that are pairs $(N,\tau_{N,-})$, where $N\in\mathcal{N}$ and $\tau_{N,-}$ is a family of natural morphisms such that for $V\in\mathcal{M}$ we have: $\tau_{N,V}:V\triangleright N\to N\triangleleft V$, satisfying the hexagon axiom as in the usual definition of center (see [5]).
Definition 4.8. Let $H$ be a quasi-Hopf algebra. A pair $(M,\lambda)$, where $M$ is a left $H$-module and $\rho:M\rightarrow M\otimes H$ is a $k$-linear map, written as $\rho(m)=m_{[0]}\otimes m_{[1]}$, is called a left-right anti-Yetter-Drinfeld module of type II, if it satisfies the equalities (4.6), (4.8) and (4.5).
Definition 5.11. A confluent type theory is a type theory $T$ with a reduction system such that equivalent conditions of Corollary 5.9 hold for every pair $(\varphi,V)$.
Definition 2.4. A Morita equivalence between theories $T_{1}$ and $T_{2}$ is a morphism $f:T_{1}\to T_{2}$ such that for every cofibrant model $X$ of $T_{1}$, the unit $\eta_{X}:X\to f^{*}(f_{!}(X))$ of the adjunction $f_{!}\dashv f^{*}$ is a weak equivalence. We will say that $f$ is a strict Morita equivalence if $\eta_{X}$ is a weak equivalence for every $X$. We will say that $f$ is a syntactic equivalence if $\eta_{X}$ is a weak equivalence when $X$ is the initial model.
Definition 5.4. Let $T$ be a theory with separated axioms. A reduction system on $T$ is a choice of an abstract reduction system $\Rightarrow_{T,\varphi}$ on $\mathrm{Term}^{d}_{T,\varphi}$ for every pair $(\varphi,V)\in P_{M}$ such that the following conditions hold: (1) The system $\Rightarrow_{T,\varphi}$ contains $\Rightarrow_{\varphi}$. (2) For every pair of terms $t$ and $s$ such that $t\Rightarrow_{T,\varphi}s$, the sequent $\varphi\sststile{}{V}t=s$ is derivable. (3) For every substitution $\rho$, every term $c$, and every axiom $\psi\sststile{}{V^{\prime}}t=s$ in $\mathcal{A}_{e}$ such that sequent $\varphi\sststile{}{V}\psi[\rho]\land c[t[\rho]/x]=c[s[\rho]/x]$ is derivable, terms $c[t[\rho]/x]$ and $c[s[\rho]/x]$ are equivalent in the system $(\mathrm{Term}_{T,\varphi}^{d},\Rightarrow_{T,\varphi})$.
Definition 8. Let $\mathbf{C}$ be a pointed category with finite products and finite colimits, $V$ a set, $\{A_{v}\}_{v\in V}$ a family of objects of $\mathbf{C}$ indexed by $V$, and $G$ a simple graph with vertex set $V$. Let $D\colon F(IC(G))\to\mathbf{C}$ be the diagram defined by $v\mapsto A_{v}$, $\{u,v\}\mapsto A_{u}\times A_{v}$, and the morphism $(u,\{u,v\})$ is mapped to the inclusion $A_{u}\to A_{u}\times A_{v}$ as above. The $G$ Green product of $\{A_{v}\}_{v\in V}$ is the colimit of $D$ in $\mathbf{C}$, $G^{\mathbf{C}}(A_{v})=\operatorname{colim}D.$ If $\mathbf{C}=\mathbf{Mon}$, we denote the Green product simply as $G(A_{v})$.
Definition 3.15. Let $\mathcal{S}$ be a symmetric monoidal $(\infty,N)$-category. The diagram $\mathcal{S}^{\Box}$ is $\otimes$-sifted-cocomplete if it is an $N$-fold simplicial diagram of $\otimes$-sifted-cocomplete categories and $\otimes$-sifted-cocontinuous functors.
Definition 2.2. Let $\mathcal{C}$ be a bicategory and $R:\mathscr{R}\to\mathscr{S}$ and $L:\mathscr{S}\to\mathscr{R}$ two 1-morphisms in $\mathcal{C}$. We say that $L$ is a left adjoint of $R$ and $R$ is a right adjoint of $L$ if there are two 2-morphisms $u:\operatorname{id}_{\mathscr{S}}\Rightarrow R\circ L\quad\text{and}\quad c:L% \circ R\Rightarrow\operatorname{id}_{\mathscr{R}}$ called the unit and counit of the adjunction, respectively, such that $\displaystyle L=L\circ\operatorname{id}_{\mathscr{S}}\xRightarrow{% \operatorname{id}_{L}\circ u}$ $\displaystyle L\circ R\circ L\xRightarrow{c\circ\operatorname{id}_{L}}% \operatorname{id}_{\mathscr{R}}\circ L\quad\text{and}$ $\displaystyle R=\operatorname{id}_{\mathscr{S}}\circ R\xRightarrow{u\circ% \operatorname{id}_{R}}$ $\displaystyle R\circ L\circ R\xRightarrow{\operatorname{id}_{R}\circ c}R\circ% \operatorname{id}_{\mathscr{R}}$ are identities. (We will also refer to this as snake identities.)
Definition 3.1. Let $0\leq b\leq a\leq 1$ such that $(b,a)\neq(0,1)$. Let $\varrho^{b}_{a}:[0,1]\to[0,1]$ be the piecewise linear map which “collapses” the interval $[b,a]$ to a point and rescales the complement back to $[0,1]$. More precisely, if $b<a$, let $\varrho^{b}_{a}(x)=\begin{cases}\frac{x}{1-(a-b)},&x\leq b,\\ \frac{b}{1-(a-b)},&b\leq x\leq a,\\ \frac{x-(a-b)}{1-(a-b)},&a\leq x.\end{cases}$ If $a=b$, let $\varrho^{b}_{a}=id_{[0,1]}$.
Definition 2.1. Let $(\mathcal{C},\otimes,\mathbbm{1})$ be a category $\mathcal{C}$ with symmetric monoidal structure $\otimes$ and monoidal unit $\mathbbm{1}$. For an object $\mathscr{R}$ in $\mathcal{C}$ a dual is an object $\mathscr{R}^{\vee}$ together with an evaluation map $\mathrm{ev}:\mathscr{R}^{\vee}\otimes\mathscr{R}\to\mathbbm{1}$ and a coevaluation map $\mathrm{coev}:\mathbbm{1}\to\mathscr{R}\otimes\mathscr{R}^{\vee}$ such that the snake identities $\displaystyle(\operatorname{id}_{\mathscr{R}}\otimes\operatorname{ev})\circ(% \operatorname{coev}\otimes\operatorname{id}_{\mathscr{R}})$ $\displaystyle=\operatorname{id}_{\mathscr{R}},\quad\text{and}$ $\displaystyle(\operatorname{ev}\otimes\operatorname{id}_{\mathscr{R}})\circ(% \operatorname{id}_{\mathscr{R}}\otimes\operatorname{coev})$ $\displaystyle=\operatorname{id}_{\mathscr{R}}.$ hold. In this case, we say that $\mathscr{R}$ is dualizable or 1-dualizable.
Definition 3.5. Let $\emptyset=X_{-1}\subset X_{0}\subset X_{1}\subset\cdots\subset X_{n}=(0,1)^{n}$ be a stratification of $(0,1)^{n}$. A factorization algebra $\mathcal{F}$ on the stratified space $X$ is called constructible (or locally constant with respect to the stratification) if for any inclusion of two disks $D_{1}\hookrightarrow D_{2}$ that are each diffeomorphic to the same basic stratified disk, the structure map $\mathcal{F}(D_{1})\xrightarrow{\simeq}\mathcal{F}(D_{2})$ is an equivalence. If the stratification is the empty stratification, then a constructible factorization algebra is called locally constant.
Definition 2.5. A symmetric monoidal $(\infty,N)$-category $\mathcal{C}$ is said to be fully $n$-dualizable if every object is dualizable and for every $1\leq k<n$, every $k$-morphism has a left and right adjoint.
Definition 3.7. Let $p:X\to Y$ be a continuous map and $\mathcal{F}$ a factorization algebra on $X$. The pushforward factorization algebra $p_{*}\mathcal{F}$ on $Y$ is given by the formula $p_{*}\mathcal{F}(U)=\mathcal{F}(p^{-1}(U)),$ which fully determines the structure maps in terms of those of $\mathcal{F}$.
Definition 4.3. For any locally constant factorization $\mathcal{F}$ on $(0,1)^{2}$, let $\mathcal{F}^{\operatorname{rev}}$ denote the pushforward $rev_{*}\mathcal{F}$, where $rev=r\times\operatorname{id}:(0,1)^{2}\to(0,1)^{2}$ sends $(x,y)$ to $(1-x,y)$.
Definition 3.4. Let $(0,1)^{n}$ be equipped with a stratification. A stratified disk $D$ of index $k$ is an open disk in $(0,1)^{n}$ that is diffeomorphic as stratified spaces to a standard stratified disk of index $k$.
Definition 3.17. The symmetric monoidal $(\infty,n+N)$-category $\operatorname{Alg}_{n}(\mathcal{S})$ is called the factorization higher Morita category.
Definition 2.3. Let $\mathcal{C}$ be an $(\infty,N)$-category. • A 1-morphism $f:\mathscr{R}\to\mathscr{S}$ has a left (or right) adjoint if it has a left (or right) adjoint in the homotopy bicategory $h_{2}\mathcal{C}$. • If $k\geq 2$, fix two $(k-2)$-morphisms $\mathscr{R}$ and $\mathscr{S}$ in $\mathcal{C}$ and let $\tt{A}$ and $\tt{B}$ be two $(k-1)$-morphisms from $\mathscr{R}$ to $\mathscr{S}$. A $k$-morphism $f:\tt{A}\to\tt{B}$ has a left (or right) adjoint if it has left (or right) adjoint in the homotopy bicategory $h_{2}\mathcal{C}(\mathscr{R},\mathscr{S})$. • If furthermore $\mathcal{C}$ is symmetric monoidal, an object $\mathscr{R}$ is dualizable if it is dualizable in the homotopy category of $\mathcal{C}$.
Definition 2.1. Let $\mathcal{K}$ be a category. We say that a morphism $h:K\to L$ in $\mathcal{K}$ is $\aleph_{0}$-pure (or, more simply, pure) if for any commutative square
Definition 3.8. $($[1, Page 144]$)$ A subfunctor $F^{\prime}\subseteq F$ in $\mathbb{M}(\mathcal{M},\mathcal{G})$ is said to be a pure subfunctor if the quotient functor $\dfrac{F}{F^{\prime}}\in\mathbb{M}(\mathcal{M},\mathcal{G})$. A mono functor is called absolutely pure if and only if whenever it appears as a subfunctor of a mono functor it is a pure subfunctor. We denote by $\mathcal{L}_{2}(\mathcal{M},\mathcal{G})\subseteq\mathbb{M}(\mathcal{M},% \mathcal{G})$ the full subcategory of absolutely pure functors.
Definition 2.1. ([11, Definition 2.4]) Let $\mathcal{M}$ be an additive category. A left $n$-exact sequence in $\mathcal{M}$ is a complex $X^{0}\overset{d^{0}}{\rightarrow}X^{1}\overset{d^{1}}{\rightarrow}\cdots% \overset{d^{n-1}}{\rightarrow}X^{n}\overset{d^{n}}{\rightarrow}X^{n+1}$ such that $(d^{0},\ldots,d^{n-1})$ is an $n$-kernel of $d^{n}$. The concept of right $n$-exact sequence is defined dually. An $n$-exact sequence is a sequence which is both a right $n$-exact sequence and a left $n$-exact sequence.
Definition 3.1. [13] Let $\mathcal{M}$ be an $n$-abelian category, $\mathcal{A}$ an abelian category and $F:\mathcal{M}\rightarrow\mathcal{A}$ a covariant functor. (i) $F$ is called left $n$-exact if for any left $n$-exact sequence $X^{0}\overset{f^{0}}{\rightarrow}X^{1}\overset{f^{1}}{\rightarrow}\cdots% \overset{f^{n-1}}{\rightarrow}X^{n}\overset{f^{n}}{\rightarrow}X^{n+1}$ in $\mathcal{M}$, $0\rightarrow F(X^{0})\rightarrow F(X^{1})\rightarrow\cdots\rightarrow F(X^{n})% \rightarrow F(X^{n+1})$ is an exact sequence of $\mathcal{A}$. (ii) $F$ is called right $n$-exact if for any right $n$-exact sequence $X^{0}\overset{f^{0}}{\rightarrow}X^{1}\overset{f^{1}}{\rightarrow}\cdots% \overset{f^{n-1}}{\rightarrow}X^{n}\overset{f^{n}}{\rightarrow}X^{n+1}$ in $\mathcal{M}$, $F(X^{0})\rightarrow F(X^{1})\rightarrow\cdots\rightarrow F(X^{n})\rightarrow F% (X^{n+1})\rightarrow 0$ is an exact sequence of $\mathcal{A}$. (iii) $F$ is called $n$-exact if for any $n$-exact sequence $X^{0}\overset{f^{0}}{\rightarrow}X^{1}\overset{f^{1}}{\rightarrow}\cdots% \overset{f^{n-1}}{\rightarrow}X^{n}\overset{f^{n}}{\rightarrow}X^{n+1}$ in $\mathcal{M}$, $0\rightarrow F(X^{0})\rightarrow F(X^{1})\rightarrow\cdots\rightarrow F(X^{n})% \rightarrow F(X^{n+1})\rightarrow 0$ is an exact sequence of $\mathcal{A}$.
Definition 2.5. $($[8, Definition 2.11]$)$ Let $\mathcal{M}$ be an additive category, $X:X^{0}\overset{d^{0}}{\rightarrow}X^{1}\overset{d^{1}}{\rightarrow}\cdots% \overset{d^{n-2}}{\rightarrow}X^{n-1}\overset{d^{n-1}}{\rightarrow}X^{n}$ a complex in $\mathcal{M}$ and $f^{0}:X^{0}\rightarrow Y^{0}$ a morphism in $\mathcal{M}$. An $n$-pushout diagram of $X$ along $f^{0}$ is a morphism of complexes $X$$X^{0}$$X^{1}$$\ldots$$X^{n-1}$$X^{n}$$Y$$Y^{0}$$Y^{1}$$\ldots$$Y^{n-1}$$Y^{n}$$f$$f^{0}$ such that the mapping cone $C=C(f)$ of $f$ is a right $n$-exact sequence. The concept of $n$-pullback diagram is defined dually.
Definition 2.3. $($[8, Definition 3.14]$)$ Let $\mathcal{A}$ be an abelian category and $\mathcal{M}$ be a generating-cogenerating full subcategory of $\mathcal{A}$. $\mathcal{M}$ is called an $n$-cluster tilting subcategory of $\mathcal{A}$ if $\mathcal{M}$ is functorially finite in $\mathcal{A}$ and $\displaystyle\mathcal{M}$ $\displaystyle=\{X\in\mathcal{A}\mid\forall i\in\{1,\ldots,n-1\},\operatorname{% Ext}^{i}(X,\mathcal{M})=0\}$ $\displaystyle=\{X\in\mathcal{A}\mid\forall i\in\{1,\ldots,n-1\},\operatorname{% Ext}^{i}(\mathcal{M},X)=0\}.$ Note that $\mathcal{A}$ itself is the unique 1-cluster tilting subcategory of $\mathcal{A}$.
Definition 2.2. $($[8, Definition 3.1]$)$ An $n$-abelian category is an additive category $\mathcal{M}$ which satisfies the following axioms: (A0) The category $\mathcal{M}$ is idempotent complete. (A1) Every morphism in $\mathcal{M}$ has an $n$-kernel and an $n$-cokernel. (A2) For every monomorphism $d^{0}:X^{0}\rightarrow X^{1}$ in $\mathcal{M}$ and for every $n$-cokernel $(d^{1},\ldots,d^{n})$ of $d^{0}$, the following sequence is $n$-exact: $X^{0}\overset{d^{0}}{\rightarrow}X^{1}\overset{d^{1}}{\rightarrow}\cdots% \overset{d^{n-1}}{\rightarrow}X^{n}\overset{d^{n}}{\rightarrow}X^{n+1}.$ (A3) For every epimorphism $d^{n}:X^{n}\rightarrow X^{n+1}$ in $\mathcal{M}$ and for every $n$-kernel $(d^{0},\ldots,d^{n-1})$ of $d^{n}$, the following sequence is $n$-exact: $X^{0}\overset{d^{0}}{\rightarrow}X^{1}\overset{d^{1}}{\rightarrow}\cdots% \overset{d^{n-1}}{\rightarrow}X^{n}\overset{d^{n}}{\rightarrow}X^{n+1}.$
Definition 2.1.1. Let $S^{1}=\operatorname{\mathbb{U}}[1]$ be the shift of the unit object. Then $\mathscr{T}$ is called $\otimes$-triangulated if there is a natural isomorphism $(-)[1]\cong-\otimes S^{1}$ and for each $X\in\mathscr{T}$, the functor $X\otimes-$ preserves distinguished triangles. Let $\mathscr{A}$ be a symmetric monoidal abelian category. A Künneth functor is a homological functor $H\colon\mathscr{T}\rightarrow\mathscr{A}$ which is symmetric strong monoidal.
Definition 3.1.3. Let $A$, $B$ be objects of $\mathscr{A}$ and consider them as objects of $\operatorname{\mathrm{K}^{\mathrm{b}}}(\mathscr{A})$ concentrated in degree zero. Consider a small diagram $C\colon\mathscr{I}\rightarrow\operatorname{\mathrm{K}^{\mathrm{b}}}(\mathscr{A})$, a cone $(f_{i}\colon A\rightarrow C_{i})$ and a cone $(w_{i}\colon B\rightarrow C_{i})$. The morphisms $\xymatrix@C=45pt{HA\ar[r]^{-}{\cong}&H_{\ast}\operatorname{\mathrm{K}^{\mathrm% {b}}}(H)(A)\ar[r]^{H_{\ast}\operatorname{\mathrm{K}^{\mathrm{b}}}(H)(f_{i})}&H% _{\ast}\operatorname{\mathrm{K}^{\mathrm{b}}}(H)(C_{i})}$ of graded $K$-vector spaces induce morphisms $\widetilde{Hf}\colon HA\rightarrow\operatorname{colim}_{\mathscr{I}}\bigl{(}H_% {\ast}\operatorname{\mathrm{K}^{\mathrm{b}}}(H)(C_{i})\bigr{)}\quad\text{and}% \quad\widetilde{Hw}\colon HB\rightarrow\operatorname{colim}_{\mathscr{I}}\bigl% {(}H_{\ast}\operatorname{\mathrm{K}^{\mathrm{b}}}(H)(C_{i})\bigr{)}$ and we call the pair $\bigl{(}(f_{i}),(w_{i})\bigr{)}$ a homological morphism from $A$ to $B$ if $\widetilde{Hw}$ is an isomorphism. We denote homological morphisms by $(f,w)\colon A\rightsquigarrow B$, leaving the diagram implicit. We write $\mathrm{H}(A,B)$ for the set of homological morphisms from $A$ to $B$.
Definition 2.1.3. A morphism $p\colon X\rightarrow Y$ in $\mathscr{T}$ is called an $H$-epimorphism if $Hp$ is an epimorphism.
Definition 3.1.1. Let $\mathscr{A}$ be a small additive rigid symmetric monoidal category with finite direct sums. Let $K$ be a field and let $H\colon\mathscr{A}\rightarrow(\mathbb{Z},\varepsilon)\mbox{-}\operatorname{% \mathbf{Vect}}_{K}$ be a symmetric strong monoidal additive functor. The factorization $\xymatrix@C=10pt{\mathscr{A}\ar[rd]_{(}0.45){H}\ar[rr]^{-}{[-]}&\ar@{}[d]|(0.4% ){\cong}&\ar[ld]^{(}0.45){\bar{H}}\mathscr{M}_{H}\\ &(\mathbb{Z},\varepsilon)\mbox{-}\operatorname{\mathbf{Vect}}_{K}}$ of symmetric strong monoidal functors constructed above is called the basic Tannakian factorization of $H$.
Definition 2.2.3. Let $\mathscr{T}$ be rigid $\otimes$-triangulated and let $H\colon\mathscr{T}\rightarrow(A,\varepsilon)\mbox{-}\operatorname{\mathbf{Vect% }}_{K}$ be a Künneth functor. We write $\mathscr{M}(H)$ for the full symmetric monoidal subcategory of $\mathrm{Sh}_{H}(\mathscr{T})$ consisting of finitely presentable objects. We write $[-]\colon\mathscr{T}\rightarrow\mathscr{M}(H)$ for the symmetric strong monoidal functor $LY$. We write $\bar{H}$ for the restriction of $-\mathop{{\otimes}_{\mathscr{T}}}H$ to $\mathscr{M}(H)$.
Definition 2.21 Let $R$ be a ring (with or without unit), and let $Q_{\bullet}$ and $P_{\bullet}$ be chain complexes of left $R$-modules and right $R$-modules, respectively. The tensor product bicomplex $Q_{\bullet}\otimes_{R}P_{\bullet}$ has terms $Q_{q}\otimes_{R}P_{p}$, with vertical differentials given by $\mathds{1}^{q}\otimes\partial^{P}$ and horizontal differentials given by $(-1)^{p}(\partial^{Q}\otimes\mathds{1}^{p})$. ${}_{\blacksquare}$
Definition 2.2 Let $M$ be a von Neumann algebra with n.s.f. trace $\tau$. A right (resp. left) $M$-module $H$ is locally finite if for all $\xi\in H$ there exists a projection $p\in M$ such that $\tau(p)<\infty$ and $\xi p=\xi$ (resp. $p\xi=\xi$). ${}_{\blacksquare}$
Definition 2.7 Let $M$ be a von Neumann algebra with n.s.f. trace $\tau$. A $\tau$-filtration is a family of projections $\mathcal{F}$ in $M$ satisfying • $\tau(p)<\infty$ for all $p\in\mathcal{F}$; • $p\vee q\in\mathcal{F}$ for all $p,q\in\mathcal{F}$; • $\bigvee_{p\in\mathcal{F}}p=\mathds{1}$. Given a left $M$-submodule $K$ of the $M$-$M$-bimodule $L^{2}(M)$, we say that $\mathcal{F}$ is compatible with $K$ if $Kp\subset K$ for all $p\in\mathcal{F}$. The space $K^{\mathcal{F}}=\{\xi\in K\mid\exists p\in\mathcal{F}:\xi p=\xi\}$ is then a left $M$-submodule of $K$. ${}_{\blacksquare}$
Definition 4.12 Consider a Hilbert space $\mathcal{H}$ equipped with a continuous right action of $G$ by unitaries, and a 1-cocycle $c:G\to\mathcal{H}$. Then • $c$ is inner if there exists $\xi$ in $\mathcal{H}$ such that $c(g)=\xi-\xi g$ for all $x\in G$ ; • $c$ is approximately inner if there exists a sequence $(\xi_{n})_{n\in\mathbb{N}}$ in $\mathcal{H}$ such that $c(x)-(\xi_{n}-\xi_{n}g)\to 0$ for $g\in G$ uniformly on compact subsets of $G$ [Gui80, § III.2.3] ; • $c$ is proper if $g\mapsto\|c(g)\|$ is a proper map, i.e. if for any $\kappa>0$ the set of $x\in G$ such that $\|c(x)\|\leq\kappa$ is compact. ${}_{\blacksquare}$
Definition 2.3 Let $M$ be a von Neumann algebra with n.s.f. trace $\tau$, and let $H$, $K$ be (left or right) $M$-modules. Then a left $M$-module map $\varphi:K\to H$ is a dimension monomorphism if $\ker\varphi$ is zero-dimensional, a dimension epimorphism if $\operatorname{coker}\varphi$ is zero-dimensional and a dimension isomorphism if both conditions are satisfied. A sequence $H\stackrel{{\scriptstyle\varphi}}{{\to}}K\stackrel{{\scriptstyle\psi}}{{\to}}L$ is dimension exact if $\ker\psi/\operatorname{im}\varphi$ is zero-dimensional. ${}_{\blacksquare}$
Definition 3.1 A full $C^{*}$-tensor subcategory $\mathcal{C}$ of a rigid $C^{*}$-tensor category $\mathcal{D}$ is said to be almost normal if $\mathcal{C}\alpha\mathcal{C}$ is a union of finitely many left (resp. right4The left and right versions are equivalent.) cosets for all $\alpha\in\operatorname{Irr}(\mathcal{D})$. If $\mathcal{C}\alpha=\alpha\mathcal{C}$ for all $\alpha\in\operatorname{Irr}(\mathcal{D})$, we call the inclusion normal. ${}_{\blacksquare}$
Definition 2.1 ([Pet12]) Let $M$ be a von Neumann algebra with n.s.f. trace $\tau$, and $H$ a right $M$-module. Then define $\displaystyle\dim_{-M}(H)=\sup\{(\operatorname{Tr}\otimes\tau)(P)\mid$ $\displaystyle P\in M_{n}(\mathbb{C})\otimes M,(\operatorname{Tr}\otimes\tau)(P% )<\infty,$ $\displaystyle P(\mathbb{C}^{n}\otimes M)\hookrightarrow H\text{ as }M\text{-% modules}\}\,.$ The definition for left modules is analogous. ${}_{\blacksquare}$
Definition 3.17 Let $\mathcal{C}$ be a full tensor subcategory of a rigid $C^{*}$-tensor category $\mathcal{D}$ and $V$ a nondegenerate right $\mathcal{A}_{\mathcal{D}}$-module. The first quadrant bicomplex $C_{\bullet,\bullet}(V)$ is defined as the $\mathcal{A}_{\mathcal{D}}$-tensor product complex of $V\otimes_{\mathcal{A}_{\mathcal{D}}}P_{\mathcal{D}/\mathcal{C}}^{\bullet}$ and $P_{\mathcal{D}}^{\bullet}\otimes_{\mathcal{A}_{\mathcal{D}}}\mathbb{C}$, i.e. $C_{p,q}(V)\cong V\otimes_{\widetilde{\mathcal{A}}_{\mathcal{C}}}\mathcal{A}_{% \mathcal{D}}^{\otimes_{\widetilde{\mathcal{A}}_{\mathcal{C}}}q}\otimes_{% \widetilde{\mathcal{A}}_{\mathcal{C}}}\mathcal{A}_{\mathcal{D}}^{\otimes_{% \mathcal{B}}(p+1)}\cdot p_{\varepsilon}$ for $p,q\geq 0$. Here, $\mathcal{B}$ denotes the $*$-algebra defined in (2.3), i.e. the $\ast$-subalgebra of $\mathcal{A}_{\mathcal{D}}$ spanned by the projections $p_{i}$, $i\in\operatorname{Irr}(\mathcal{D})$. We also define an augmented version $\widetilde{C}_{\bullet,\bullet}(V)$ of $C_{\bullet,\bullet}(V)$ by putting $\widetilde{C}_{p,q}(V)=C_{p,q}(V)$ for $p,q\geq 0$ and $\widetilde{C}_{p,-1}(V)=V\otimes_{\mathcal{B}}\mathcal{A}_{\mathcal{D}}^{% \otimes_{\mathcal{B}}p}\cdot p_{\varepsilon}$ for $p\geq 0$. The extra differentials $\widetilde{C}_{p,0}(V)\to\widetilde{C}_{p,-1}(V)$ are induced by the module map $\sigma:V\otimes_{\widetilde{\mathcal{A}}_{\mathcal{C}}}\mathcal{A}_{\mathcal{D% }}\to V$. The vertical differentials on $\widetilde{C}_{p,-1}(V)$ are the usual ones for inhomogeneous chains. See Figure 1 for a diagram of $\widetilde{C}_{\bullet,\bullet}$. ${}_{\blacksquare}$
Definition 2.18 A bicomplex in an abelian category $\mathcal{C}$ is a triple $((C_{p,q})_{p,q\in\mathbb{Z}},\partial^{h},\partial^{v})$ where $\partial^{h}:C_{p,q}\to C_{p,q-1}$ and $\partial^{v}:C_{p,q}\to C_{p-1,q}$ are referred to as the horizontal and vertical differentials, respectively. As usual, we suppress the sub-indices on the differentials, and we simply write $C_{\bullet,\bullet}$ to refer to the entire triple if there is no danger of confusion. The differentials should satisfy $\partial^{h}\partial^{h}=0$, $\partial^{v}\partial^{v}=0$ and $\partial^{h}\partial^{v}=-\partial^{v}\partial^{h}$, i.e. all columns $C_{\bullet,q}$ and all rows $C_{p,\bullet}$ should be chain complexes in $\mathcal{C}$. A first quadrant bicomplex is a bicomplex where $C_{p,q}=0$ whenever $p<0$ or $q<0$. ${}_{\blacksquare}$
Definition 4.11 ([PSV15]) Consider an $S$-$S$-bimodule $\mathcal{K}$ and a 1-cocycle $c:\mathcal{S}\to\mathcal{K}$. Then • $c$ is inner if there exists a $T$-central vector $\xi$ in $\mathcal{K}$ such that $c(x)=x\xi-\xi x$ for all $x\in\mathcal{S}$ ; • $c$ is approximately inner if there exists a sequence $(\xi_{n})_{n\in\mathbb{N}}$ of $T$-central vectors in $\mathcal{K}$ such that $c(x)-(x\xi_{n}-\xi_{n}x)\to 0$ for all $x\in\mathcal{S}$ ; • $c$ is proper if for any $\kappa>0$ the set of $x\in\mathcal{S}$ such that $\|c(x)\|\leq\kappa\|x\|_{2}$ is contained in a bifinite $T$-$T$-subbimodule of $L^{2}(S)$. ${}_{\blacksquare}$
Definition 2.14 Let $T\subset S$ be an inclusion of type $\mathrm{II}_{1}$ factors. Define the quasi-normaliser of $T$ inside $S$ by $\displaystyle\mathrm{QN}_{S}(T)$ $\displaystyle=\Big{\{}x\in S\,\Big{|}\,\exists a_{1},\ldots,a_{n},b_{1},\ldots% ,b_{m}\in S:xT\subset\sum_{i=1}^{n}Ta_{i},Tx\subset\sum_{j=1}^{m}b_{j}T\Big{\}% }\,.$ We say that $T\subset S$ is quasi-regular if $S=\mathrm{QN}_{S}(T)^{\prime\prime}$. In what follows, we always consider irreducible quasi-regular inclusions, i.e. $T^{\prime}\cap S=\mathbb{C}\mathds{1}$. ${}_{\blacksquare}$
Definition 2.24 Given a chain map $\varphi_{\bullet}:C_{\bullet}\to D_{\bullet}$, define $\operatorname{cone}(\varphi)_{n}=C_{n-1}\oplus D_{n}$ with differentials $\partial:\operatorname{cone}(\varphi)_{n}\to\operatorname{cone}(\varphi)_{n-1}% :\xi\oplus\eta\mapsto-\partial(\xi)\oplus(\partial(\eta)-\varphi_{n-1}(\xi))\,.$ The complex $\operatorname{cone}(\varphi)_{\bullet}$ is called the mapping cone of $\varphi$. ${}_{\blacksquare}$
Definition 2.11 (Cf. §I,2.4 of [Gra74]). A lax-natural transformation $\alpha\colon F\Rightarrow G$ of 2-functors $F,G\colon\mathfrak{K}\rightrightarrows\mathfrak{L}$ consists of a family of arrows $\alpha_{A}\colon FA\to GA$ of $\mathfrak{L}$ indexed over the objects $A\in\mathfrak{K}_{0}$ together with, for each arrow $f$ of $\mathfrak{K}$, a distinguished 2-cell $FA$$FB$$GA$$GB$$\alpha_{f}$$\Rightarrow$$Ff$$\alpha_{A}$$\alpha_{B}$$Gf$ satisfying the following two compatibility conditions. 1. For any composable arrows $f$ and $g$ of $\mathfrak{K}$, there is an equality of 2-cells $FA$$FB$$GA$$GB$$\alpha_{f}$$\Rightarrow$$FC$$GC$$\alpha_{g}$$\Rightarrow$$FA$$FB$$GA$$GB$$\alpha_{gf}$$\Rightarrow$$=$$Ff$$\alpha_{A}$$\alpha_{B}$$Gf$$Fg$$Gg$$\alpha_{C}$$Ff$$\alpha_{A}$$\alpha_{B}$$Gf$ 2. For any 2-cell $\theta\colon f\Rightarrow g$ of $\mathfrak{K}$, there is an equality of 2-cells as depicted in the diagram $FA$$FB$$GA$$GB$$\Uparrow F\theta$$\Rightarrow$$\alpha_{f}$$=$$FA$$FB$$FA$$FB$$\alpha_{g}$$\Rightarrow$$\Uparrow G\theta$$\alpha_{B}$$\alpha_{A}$$Fg$$Ff$$Gf$$Fg$$\alpha_{A}$$Gg$$Gf$$\alpha_{B}$ A lax-natural transformation is pseudo natural if the cells $\alpha_{f}$ are invertible. If they are identities, the transformation is 2-natural.
Definition 3.4. A split discrete 2-fibration is a 2-functor $E\colon\mathfrak{E}\to\mathfrak{C}$ such that 1. the underlying functor $|E|\colon|\mathfrak{E}|\to|\mathfrak{B}|$ is a split fibration; 2. $E$ itself is locally a discrete fibration, in that each functor $E\colon\mathfrak{E}(X,Y)\to\mathfrak{C}(EX,EY)$ is a discrete fibration. Dually, a discrete 2-opfibration is a 2-functor $E\colon\mathfrak{E}\to\mathfrak{B}$ whose underlying functor of 1-categories is a split opfibration and which is locally a discrete opfibration.
Definition 3.21 (Cf. Defn. 2.3 of [Her99], §2.1.6 of [Buc14]). A 2-functor $P\colon\mathfrak{E}\to\mathfrak{B}$ is a 2-fibration if 1. for each arrow $f\colon B\to PE$ of $\mathfrak{B}$ there is a cartesian arrow $g\colon A\to E$ of $\mathfrak{E}$ with $Pg=f$; 2. locally $P$ is an opfibration in that each $P_{A,B}\colon\mathfrak{E}(A,B)\to\mathfrak{B}(PA,PB)$ is an opfibration; 3. and finally opcartesian 2-cells are closed under horizontal composition. Such 2-fibrations will always assumed to be split in the sense of Definition 3.22 below. The (split) 2-fibrations over $\mathfrak{B}$ are objects of a 3-category $\mathbf{2Fib}(\mathfrak{B})$ whose arrows are splitting-preserving 2-functors over $\mathfrak{B}$, together with vertical 2-natural transformations, and vertical modifications between them.
Definition 1.1. A discrete fibration over $\mathscr{C}$ is a functor $F\colon\mathscr{F}\to\mathscr{C}$ such that for each morphism $f\colon C\to FX$ with $X\in\mathscr{F}$, there is a unique morphism $Y\to X$ of $\mathscr{F}$ above $f$. A functor $E\colon\mathscr{E}\to\mathscr{C}$ is a discrete opfibration if $E^{op}$ is a discrete fibration. A morphism of discrete fibrations $F\colon\mathscr{F}\to\mathscr{C}$ and $G\colon\mathscr{G}\to\mathscr{C}$ is a functor $H\colon\mathscr{F}\to\mathscr{G}$ such that $GH=F$ holds. Let $\mathbf{DFib}(\mathscr{C})$ denote the category of discrete fibrations over $\mathscr{C}$ and $\mathbf{DOpf}(\mathscr{C})$ denote the category of discrete opfibrations over $\mathscr{C}$.
Definition 2.20. A lax 3-category is a category enriched in $|\mathbf{2Cat}_{lax}|$.
Definition 4.2 (Omnibus Definition). A 2-monad on a 2-category $\mathfrak{K}$ is a 2-functor $T\colon\mathfrak{K}\to\mathfrak{K}$ with 2-natural transformations $\eta\colon 1\Rightarrow T$ and $\mu\colon TT\Rightarrow T$ such that $\mu T\mu=\mu\mu T$ and $\mu T\eta=1=\mu\eta T$ all hold, as usual. An algebra for such a 2-monad is an object $A\in\mathfrak{K}_{0}$ with a structure map $a\colon TA\to A$ satisfying the usual equations, namely, $a\mu_{A}=aTa$ and $a\eta_{A}=1$. A morphism of algebras $(A,a)$ and $(B,b)$ is a morphism of the 2-category $h\colon A\to B$ that preserves the unit and preserves the action. A 2-cell of morphisms of algebras $h,k\colon A\rightrightarrows B$ is a 2-cell $\theta\colon h\Rightarrow k$ of the 2-category satisfying the compatibility condition $\theta\ast a=b\ast T\theta$. With these definitions $T\mathbf{Alg}$ denotes the 2-category of $T$-algebras, their morphisms and 2-cells. A 2-category is monadic over $\mathfrak{K}$ if it is equivalent to $T\mathbf{Alg}$ for some 2-monad $T$ on $\mathfrak{K}$.
