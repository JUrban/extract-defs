Definition 4.5. We call $\mathcal{T}$ supportive if $T=0$ if and only if $\mathop{\mathbf{supp}}\nolimits T=\emptyset$.
Definition 2.6. Let $\mathbf{X}$ be an arbitrary topological space. (1) Denoted by ${\rm\bf skula}\!\left(\mathbf{X}\right)$, the Skula or front topology on $\mathbf{X}$ is the weakest topology where every open set of $\mathbf{X}$ is open and closed. (2) Let $\mathbf{f}_{\mathbf{X}}\colon{\rm\bf skula}\!\left(\mathbf{X}\right)\to\mathbf% {X}$ be the set theoretic identity function. (3) The space $\mathbf{X}$ satisfies the separation axiom $\mathop{\rm T}\nolimits_{\frac{1}{2}}$ if for every point $\mathfrak{p}\in\mathbf{X}$ there is an open set $\mathbf{V}\subseteq\mathbf{X}$ and a closed set $\mathbf{U}\subseteq\mathbf{X}$ such that $\{\mathfrak{p}\}=\mathbf{V}\cap\mathbf{U}$.
Definition 5.3. For a module $M\in\mathop{\rm Mod}\nolimits(R)$, a prime $\mathfrak{p}\in\mathop{\mathbf{Spec}}\nolimits R$ is weakly associated to a module $M$ if there exists an element $m\in M$ such that $\mathfrak{p}$ is minimal amongst primes containing $\mathop{\rm ann}\nolimits(m)$. Let $\mathop{\widetilde{\rm Ass}}\nolimits_{R}M$ denote the the set of weakly associated primes of $M$. Let $\mid\mathop{\widetilde{\rm Ass}}\nolimits_{R}M$ be the minimal weakly associated primes.
Definition 3.1. (1) A subcategory $\mathcal{I}\subseteq\mathcal{T}$ is a thick tensor ideal if (a) $\mathcal{I}$ is sub triangulated i.e. contains 0 and is closed under mapping cones (b) $\mathcal{I}$ is closed under direct summands (c) for every $S\in\mathcal{I}$ and $T\in\mathcal{T}$, $S\otimes T\in\mathcal{I}$. (2) A thick tensor ideal $\mathcal{I}\subseteq\mathcal{T}$ is radical if $T\otimes\cdots\otimes T\in\mathcal{I}$ implies $T\in\mathcal{I}$. (3) A thick tensor ideal $\mathcal{P}\subseteq\mathcal{T}$ is prime if $\mathcal{I}\otimes\mathcal{J}\subseteq\mathcal{P}$ implies either $\mathcal{I}\subseteq\mathcal{P}$ or $\mathcal{J}\subseteq\mathcal{P}$ for any thick tensor ideals $\mathcal{I},\mathcal{J}\subseteq\mathcal{T}$. Equivalently, prime thick tensor ideals are the meet irreducible elements of the lattice of radical thick tensor ideals. (4) For an object $T\in\mathcal{T}$, let $\mathop{\mathbf{Supp}}\nolimits T$ denote the set of primes which do not contain $T$. (5) Let $\mathop{\mathbf{Spc}}\nolimits\mathcal{T}$ be the set of prime thick tensor ideals of $\mathcal{T}$. We consider the weakest topology such that $\mathop{\mathbf{Supp}}\nolimits T$ is closed for all $T\in\mathcal{T}$. This topological space is called the Balmer Spectrum.
Definition 7.4. (1) For a space $\mathbf{X}$ and a subspace $\mathbf{S}\subseteq\mathbf{X}$, a point $\mathfrak{p}\in\mathbf{S}$ is weakly isolated if there exists an open set $\mathbf{V}\subseteq\mathbf{X}$ such that $\mathfrak{p}\in\mathbf{V}\cap\mathbf{S}\subseteq\bar{\mathfrak{p}}.$ (2) Let $\mathbb{X}$ be a frame. (i) A prime $\mathbbm{p}\in\mathbb{X}$ is minimal over $\mathbbm{x}$ if it is minimal amongst primes containing $\mathbbm{x}$. Let $\min(\mathbbm{x})$ denote the the minimal primes of $\mathbbm{x}$. (ii) A prime $\mathbbm{p}\in\min(\mathbbm{x})$ is essential over $\mathbbm{x}$ if $\mathbbm{x}=\bigwedge_{\mathbbm{q}\in\min(\mathbbm{x})}\mathbbm{q}\quad\quad% \mbox{and}\quad\quad\mathbbm{x}\neq\bigwedge_{\begin{subarray}{c}\mathbbm{q}% \in\min(\mathbbm{x})\\ \mathbbm{q}\neq\mathbbm{p}\end{subarray}}\mathbbm{q}$
Definition 3.7. Recall from Example 2.5 that for any prime $\mathfrak{p}\in\mathop{\mathbf{Spc}}\nolimits\mathcal{T}$, the set $\mathbf{Z}\!\left(\mathfrak{p}\right)$ is Thomason. Let $T$ be an object in $\mathcal{T}$. (1) Set $T_{\mathfrak{p}}=L_{\mathbf{Z}\!\left(\mathfrak{p}\right)}$. (2) Let $\mathop{\mathbf{Supp}}\nolimits T$ be the primes $\mathfrak{p}\in\mathop{\mathbf{Spc}}\nolimits\mathcal{T}^{c}$ such that $T_{\mathfrak{p}}\neq 0$.
Definition (C0-system, Voevodsky definition 2.1 in [9]) A C0-system is a pre-category $CC$ with additional structure of the form 1. a function $l:Ob(CC)\rightarrow{\bf N\rm}$, 2. an object $pt$, 3. a map $ft:Ob(CC)\rightarrow Ob(CC)$, 4. for each $X\in Ob(CC)$ a morphism $p_{X}:X\rightarrow ft(X)$, 5. for each $X\in Ob(CC)$ such that $l(X)>0$ and each morphism $f:Y\rightarrow ft(X)$ an object $f^{*}X$ and a morphism $q(f,X):f^{*}X\rightarrow X$, which satisfies the following conditions: 1. $l^{-1}(0)=\{pt\}$ 2. for $X$ such that $l(X)>0$ one has $l(ft(X))=l(X)-1$ 3. $ft(pt)=pt$ 4. $pt$ is a final object, 5. for $X\in Ob(CC)$ such that $l(X)>0$ and $f:Y\rightarrow ft(X)$ one has $l(f^{*}(X))>0$, $ft(f^{*}X)=Y$ and the square $\begin{CD}f^{*}X@>{q(f,X)}>{}>X\\ @V{p_{f^{*}X}}V{}V@V{}V{p_{X}}V\\ Y@>{f}>{}>ft(X)\end{CD}$ commutes, 6. for $X\in Ob(CC)$ such that $l(X)>0$ one has $id_{ft(X)}^{*}(X)=X$ and $q(id_{ft(X)},X)=id_{X}$, 7. for $X\in Ob(CC)$ such that $l(X)>0$, $g:Z\rightarrow Y$ and $f:Y\rightarrow ft(X)$ one has $(gf)^{*}(X)=g^{*}(f^{*}(X))$ and $q(gf,X)=q(g,f^{*}X)q(f,X)$. ${}_{\blacksquare}$
Definition (C-system, Voevodsky definition 2.3 in [9]) A C-system is a C0-system together with an operation $f\mapsto s_{f}$ defined for all $f:Y\rightarrow X$ such that $l(X)>0$ and such that 1. $s_{f}:Y\rightarrow(ft(f))^{*}(X)$, 2. $s_{f}\circ p_{(ft(f))^{*}(X)}=Id_{Y}$, 3. $f=s_{f}\circ q(ft(f),X)$, 4. if $X=g^{*}(U)$ where $g:ft(X)\rightarrow ft(U)$ then $s_{f}=s_{f\circ q(g,U)}$. ${}_{\blacksquare}$
Definition 11. A poloid action of a poloid $P$ on a set $X$ is a poloid homomorphism from $P$ to a transformation poloid $\mathscr{P}_{\!X}$.
Definition 14. A partial pseudotransformation partial magma $\mathscr{G}_{\!X}$ on $X$ is a set $\mathscr{G}_{\!X}$ of non-empty partial pseudotransformations $\mathfrak{f}:X\nrightarrow X$, equipped with the partial binary operation $\circ:\mathscr{G}_{\!X}\times\mathscr{G}_{\!X}\nrightarrow\mathscr{G}_{\!X},% \qquad\left(\mathfrak{f},\mathfrak{g}\right)\mapsto\mathfrak{f}\circ\mathfrak{% g},$ where $\mathrm{dom}\!\left(\circ\right)=\left\{\left(\mathfrak{f},\mathfrak{g}\right)% \mid\mathrm{dom}\!\left(\mathfrak{f}\right)\supseteq\mathsf{im}\!\left(% \mathfrak{g}\right)\right\}$ and $\mathfrak{f}\circ\mathfrak{g}$ if defined is given by $\mathsf{\mathrm{dom}}\!\left(\mathfrak{f}\circ\mathfrak{g}\right)=\mathrm{dom}% \!\left(\mathfrak{g}\right)$ and $\left(\mathfrak{f}\circ\mathfrak{g}\right)\!\left(x\right)=\left(f\circ g% \right)\!\left(x\right)$ for all $x\!\in\!\mathrm{dom}\!\left(\mathsf{\mathfrak{f}\circ\mathfrak{g}}\right)$.
Definition 3. A semigroupoid is a partial magma $P$ such that for any $x\prec y\prec z\in P$, $\left(xy\right)\!z$ and $x\!\left(yz\right)$ are defined and $\left(xy\right)\!z=x\!\left(yz\right)$.
Definition 7. A transformation semigroupoid $\mathscr{S}_{X}$ on $X$ is a transformation magma $\mathscr{F}_{X}$ such that if $\mathrm{dom}\!\left(\mathsf{f}\right)\supseteq\mathsf{im}\!\left(\mathsf{g}\right)$ for some $\mathsf{f},\mathsf{g}\in\mathscr{F}_{X}$ then $\mathrm{dom}\!\left(\mathsf{f}\right)=\mathsf{\mathrm{cod}}\!\left(\mathsf{g}\right)$.
Definition 13. A one-sided (right-sided) poloid is a one-way (right-directed) semigroupoid $P$ such that for any $x\in P$ there is a unique left unit $\epsilon_{x}$ such that $x\epsilon_{x}$ is defined and $x\epsilon_{x}=x$.
Definition 9. A subpoloid of a poloid $P$ is a poloid that is a submagma of $P$ regarded as a partial magma.
Definition 1. A partial function $\mathsf{f}:X\nrightarrow Y$ is a (total) function $f:\mathsf{X}\rightarrow\mathsf{Y}$ and sets $X,Y$ such that $\mathsf{X}\subseteq X$ and $\mathsf{Y}\subseteq Y$. We call $\mathsf{X}$ the domain of $\mathsf{f}$, denoted $\mathrm{dom}\!\left(\mathsf{f}\right)$, $\mathsf{Y}$ the codomain of $\mathsf{f}$, denoted $\mathrm{cod}\!\left(\mathsf{f}\right)$ and $f\!\left(\mathsf{X}\right)$ the image of $\mathsf{f}$, denoted $\mathrm{im}\!\left(\mathsf{f}\right)$. A partial function $\mathsf{f}$ such that $\mathrm{dom}\!\left(\mathsf{f}\right)\neq\emptyset$ is said to be non-empty. The restriction of $\mathsf{f}:X\nrightarrow Y$ to $\mathsf{X}^{\prime}\subset X$ is the partial function $\left.\mathsf{f}\right|_{X^{\prime}}:X^{\prime}\nrightarrow Y$ such that $\mathrm{dom}\!\left(\left.\mathsf{f}\right|_{X^{\prime}}\right)=\mathrm{dom}\!% \left(\mathsf{f}\right)\cap X^{\prime}$, $\mathrm{cod}\!\left(\mathsf{f}\raise-2.15pt\hbox{$|$}_{X^{\prime}}\right)=% \mathrm{cod}\!\left(\mathsf{f}\right)$ and $\mathsf{f}\raise-2.15pt\hbox{$|$}_{X^{\prime}}\!\left(x\right)=f\!\left(x\right)$ for all $x\in\mathrm{dom}\!\left(\left.\mathsf{f}\right|_{X^{\prime}}\right)$. A partial transformation on $X$ is a partial function $\mathsf{f}:X\nrightarrow X$.
Definition 10. Let $P$ and $Q$ be poloids. A poloid homomorphism from $P$ to $Q$ is a function $F:P\rightarrow Q$ such that: (1) If $x,y\in P$ and $xy$ is defined then $F\!\left(x\right)\!F\!\left(y\right)$ is defined and $F\!\left(xy\right)=F\!\left(x\right)\!F\!\left(y\right)$. (2) If $e\in P$ and $e$ is a unit then $F\!\left(e\right)$ is a unit.
Definition 5. A groupoid is a poloid $P$ such that for every $x\in P$ there is a unique $x^{-1}\in P$ such that $xx^{-1}$ and $x^{-1}x$ are defined and units.
Definition 8. A transformation poloid $\mathscr{P}_{\!X}$ is a transformation semigroupoid $\mathscr{S}_{\!X}$ such that if $\mathsf{f}\in\mathscr{S}_{\!X}$ then $\mathsf{Id}_{\mathrm{dom}\left(\mathsf{f}\right)},\mathsf{Id}_{\mathrm{cod}% \left(\mathsf{f}\right)}\in\mathscr{S}_{\!X}$.
Definition 6. Let $X$ be a non-empty set. A partial transformation partial magma $\mathscr{F}_{\!X}$ on $X$ is a set $\mathscr{F}_{X}$ of non-empty partial transformations $\mathsf{f}:X\nrightarrow X$, equipped with the partial binary operation $\circ:\mathscr{F}_{\!X}\times\mathscr{F}_{\!X}\nrightarrow\mathscr{F}_{\!X},% \qquad\left(\mathsf{f},\mathsf{g}\right)\mapsto\mathsf{f}\circ\mathsf{g},$ where $\mathrm{dom}\!\left(\circ\right)=\left\{\left(\mathsf{f},\mathsf{g}\right)\mid% \mathrm{dom}\!\left(\mathsf{f}\right)\supseteq\mathsf{im}\!\left(\mathsf{g}% \right)\right\}$ and $\mathsf{f}\circ\mathsf{g}$ if defined is given by $\mathsf{\mathrm{dom}}\!\left(\mathsf{f}\circ\mathsf{g}\right)=\mathrm{dom}\!% \left(\mathsf{g}\right)$, $\mathrm{cod}\!\left(\mathsf{f}\circ\mathsf{g}\right)=\mathrm{cod}\!\left(% \mathsf{f}\right)$ and $\left(\mathsf{f}\circ\mathsf{g}\right)\!\left(x\right)=\left(f\circ g\right)\!% \left(x\right)$ for all $x\!\in\!\mathrm{dom}\!\left(\mathsf{f}\circ\mathsf{g}\right)$. The full partial transformation partial magma on $X$, denoted $\mathscr{\overline{F}}_{\!X}$, is the partial transformation partial magma whose underlying set is the set of all non-empty partial transformations $\mathsf{f}:X\nrightarrow X$.
Definition 4. A poloid is a semigroupoid $P$ such that for any $x\in P$ there are units $\epsilon_{\!x},\varepsilon_{\!x}\in P$ such that $\epsilon_{x}x$ and $x\varepsilon_{\!x}$ are defined.
Definition 2. A partial binary operation on a set $X$ is a partial function $\uppi:X\times X\nrightarrow X,\qquad\left(x,y\right)\mapsto xy$ such that $\mathrm{cod}\!\left(\uppi\right)=X$. A partial magma $P$ is a non-empty set $\left|P\right|$ equipped with a partial binary operation on $\left|P\right|$. A submagma $P^{\prime}$ of a partial magma $P$ is a set $\left|P^{\prime}\right|\subseteq\left|P\right|$ such that if $x,y\in\left|P^{\prime}\right|$ then $xy\in\left|P^{\prime}\right|$, with the restriction of $\uppi$ to $\left|P^{\prime}\right|\times\left|P^{\prime}\right|$ as partial binary operation.
Definition 12. A one-way (right-directed) semigroupoid is a partial magma $P$ such that, for any $x,y,z\in P$, if $\left(xy\right)\!z$ is defined or $xy$ and $yz$ are defined then $\left(xy\right)\!z$ and $x\!\left(yz\right)$ are defined and $x\!\left(yz\right)=\left(xy\right)\!z$.
Definition 4. Let $K,L\subset M$ be regular submanifolds such that every point $p\in K\cap L$ satisfies $T_{p}K+T_{p}L=T_{p}M.$ Then $K,L$ are transverse manifolds.
Definition 38. There is a category $\mathbf{Symp}$ whose objects are symplectic manifolds and morphisms are Poisson maps.
Definition 12. A Poisson algebra is a commutative associative algebra $(f,g)$ with Poisson bracket $\{f,g\}$ such that the four properties from above hold.
Definition 49. A monoidal natural transformation $\alpha$ from a lax symmetric monoidal functor $(F,\varphi)\colon(\mathbf{RiSurSub},\times)\to(\mathbf{Set},\times)$ to a lax symmetric monoidal functor $(G,\gamma)\colon(\mathbf{SympSurj},\times)\to(\mathbf{Set},\times)$ is a natural transformation $\alpha:F\Rightarrow G$ such that the following diagram $G(A)\times G(B)$$F(A)\times F(B)$$F(A\times B)$$G(A\times B)$$\varphi_{A,B}$$\alpha_{A}\times\alpha_{B}$$\alpha_{A\times B}$$\gamma_{A,B}$ commutes.
Definition 17. An almost Poisson structure $\{\cdot,\cdot\}_{\Pi}$ on a manifold $M$ is called a Poisson structure if it satisfies the Jacobi identity. Thus, a Poisson manifold $(M,\{\cdot,\cdot\})$ is equipped with a Poisson structure $\{\cdot,\cdot\}$ and the corresponding bivector field $\Pi$ is then called a Poisson tensor [5].
Definition 30. A span from $M$ to $M^{\prime}$ in a category $\mathscr{C}$ is an object $S$ in $\mathscr{C}$ with a pair of morphisms $f\colon S\to M$ and $g\colon S\to M^{\prime}$. $M$ and $M^{\prime}$ are known as feet and $S$ is known as the apex of the span. $M$$M^{\prime}$$S$$g$$f$
Definition 51. Let $\mathscr{C}$ be a locally small category. A functor $\mathcal{F}\colon\mathscr{C}\to\mathbf{Set}$ is said to be representable if it is naturally isomorphic to $Hom(C,-)$ for some object $C$ of $\mathscr{C}$.
Definition 23. In any category $\mathscr{C},$ given morphisms $f\colon K\to M$ and $g\colon L\to M$, a pullback of $f$ and $g$ or fibered product often denoted as $K\times_{M}L$ consists of morphisms $p,q$ such that $f\circ p=g\circ q$ and the universal property holds. That is given any object in our category, $Q$, which we call the competitor and morphisms $p^{\prime}\colon Q\to K,$ $q^{\prime}\colon Q\to L$ such that $f\circ p^{\prime}=g\circ q^{\prime}$ there exists a unique morphism $\psi\colon Q\to K\times_{M}L$ with $p^{\prime}=p\circ\psi$ and $q^{\prime}=q\circ\psi.$
Definition 15. Let $(M,\{\cdot,\cdot\})$ be a Poisson manifold. Then for $f,g\in C^{\infty}(M),$ by the Leibniz law we can define $\{f,g\}=\Pi(df,dg)$ where $\Pi$ is a field of skew-symmetric bilinear forms on $T^{*}M.$ We say that $\Pi\in\Gamma((T^{*}M\wedge T^{*}M)^{*})=\Gamma(TM\wedge TM)=\Gamma(\Lambda^{2}TM)$ is a bivector field [5].
Definition 7. The exterior algebra over a vector space $V$ denoted $\Lambda V$ is the algebra generated by $V$ with the relation $v\wedge u=-u\wedge v$ for vectors $u,v\in V$ where $\wedge$ is known as the wedge product [2].
Definition 2. Let $f\colon K\to M$ be a smooth map of manifolds $K$ and $M$. A point $p\in K$ is called a regular point of f if $df_{p}\colon T_{p}K\to T_{f(p)}M$ is surjective. A point $c\in M$ is called a regular value of f if every level set $f^{-1}(c)$ is a regular point.
Definition 32. An isomorphism class of spans is an equivalence class of spans where the equivalence relation is isomorphism.
Definition 18. A Poisson manifold of even dimension $M$ equipped with a closed nondegenerate $2-$form $\omega$ satisfying $\{f,g\}=\omega(v_{f},v_{g})$ where $v_{f}$ is the vector field with $v_{f}(h)=\{h,f\}$ is a symplectic manifold.
Definition 1. Let $f\colon K\to M$ be a smooth map of manifolds, if the smooth linear map $df_{p}\colon T_{p}K\to T_{f(p)}M$ is surjective for all points $p\in K$, then we say that the function $df_{p}$ is a submersion.
Definition 22. Two maps $f\colon K\to M$ and $g\colon L\to M$ of manifolds are called transverse when $\textnormal{Im}(df)+\textnormal{Im}(dg)=T_{p}M$ for all $a,b,p$ such that $f(a)=g(b)=p$ [9].
Definition 6. We define the smooth bijective map $\flat\colon T_{p}M\to T^{*}_{p}M$ called flat map where $v\mapsto\omega(v,\cdot)$ and $\sharp\colon T^{*}_{p}M\to T_{p}M$ to be called sharp map where $\omega(v,\cdot)\mapsto v.$ Note that this shows that these maps are isomorphisms.
Definition 14. A Poisson manifold is a manifold $M$ such that $C^{\infty}(M)$ with ordinary multiplication of functions, and some $\{\cdot,\cdot\}$ is a Poisson algebra.
Definition 8. We define the differential forms on $M$, denoted $\Omega(M)$, to be the algebra generated by $\Omega^{1}(M)$ with the relations $\omega\wedge\mu=-\mu\wedge\omega$ for all $\omega,\mu\in\Omega^{1}(M).$ Elements that are linear combinations of products of $k$ $1-$forms are called $k-$forms and the space is denoted by $\Omega^{k}(M)$. Moreover, $\Omega(M)=\oplus_{k}\Omega^{k}(M)$ [2].
Definition 3. Let $f\colon K\to M$ be a smooth map of manifolds, if the smooth linear map $df_{p}\colon T_{p}K\to T_{f(p)}M$ is surjective for all points $p\in K$, then we say that the function $df_{p}$ is a submersion.
Definition 31. A map of spans is a morphism $j\colon S\to S^{\prime}$ in a category $\mathscr{C}$ between apices of two spans ${}_{M}S_{M^{\prime}}$ and ${}_{M}S^{\prime}_{M^{\prime}}$ such that both the following triangles commute. In particular, when $j$ is an isomorphism, we have an isomorphism of spans. $M$$M^{\prime}$$S$$S^{\prime}$$g$$f$$g^{\prime}$$f^{\prime}$$\cong$$j$
Definition 9. In particular when $k=2$ $\omega$ is a $2-$form. If $d\omega=0,$ then we say that $\omega$ is a closed $2-$form. We say that $\omega$ is a nondegenerate $2-$form if for any nonzero $v$ there exists $u$ such that $\omega(v,u)\neq 0$ where $u,v\in T_{x}M$ [1].
Definition 5. A 1-form $\omega,$ on any manifold $M$ is a map from the set of vector fields on $M$ called $\textnormal{Vect}(M)$ to $C^{\infty}(M)$ that is linear over $C^{\infty}(M).$ In other words, for any $u,v\in\textnormal{Vect}(M)$ and $g\in C^{\infty}(M)$ (1) $\omega(u+v)=\omega(u)+\omega(v)$ (2) $\omega(gv)=g\omega(v)$ The space of all $1-$forms on a manifold $M$ will be denoted by $\Omega^{1}(M)$ [2].
Definition 16. The sections of $\Lambda^{k}TM$ are called k-vector fields or multivector fields for unspecified $k$ on $M.$ The space is denoted as $\Gamma(\Lambda^{k}TM)$ [5].
Definition 46. Let $M$ be a symplectic manifold of dimension $2n.$ We define the Hamiltonian to be a smooth function, $H$, with $H\colon M\to\mathbb{R}.$ By Darboux’s theorem there exist local coordinates for $M,$ $(q_{1},...,q_{n},p_{1},...p_{n})$. The following equations are referred to as Hamilton’s equations: $\frac{dq_{i}}{dt}=\frac{\partial H}{\partial p_{i}}$ and $\frac{dp_{i}}{dt}=-\frac{\partial H}{\partial q_{i}}.$ Hamilton’s equations are the equations that determine the flow associated to the Hamiltonian vector field, which is the vector field $v_{H}$ where $v_{H}(f)=\{f,H\}.$ Note that if $x(t)$ is a curve satisfying Hamilton’s equations, then $\left.\dfrac{{\rm d}}{{\rm d}t}\right|_{t=t_{0}}x(t)=v_{H}(x(t_{0})).$ Furthermore, the properties of the Poisson bracket guarantee that $\omega(v_{H},\cdot)=dH.$
Definition 11. Given functions $f,g,h\in C^{\infty}(M)$ and $a,b\in\mathbb{R},$ a Poisson bracket on a manifold $M$ is a binary operation $C^{\infty}(M)\times C^{\infty}(M)\to C^{\infty}(M),$ $(f,g)\mapsto\{f,g\},$ that satisfies the following: (1) Antisymmetry $\{f,g\}=-\{g,f\}$ (2) Bilinearity $\{f,ag+bh\}=a\{f,g\}+b\{f,h\}$ (3) Jacobi Identity $\{f,\{g,h\}\}+\{\{g,h\},f\}+\{h,\{f,g\}\}=0.$ (4) Leibniz Law $\{fg,h\}=\{f,h\}g+f\{g,h\}$ [6].
Definition 19. Let $\left(M,\left\{\cdot,\cdot\right\}_{M}\right)$ and $\left(N,\left\{\cdot,\cdot\right\}_{N}\right)$ be Poisson manifolds. We say that a map $\Phi\colon M\to N$ is a Poisson map if, for any $f,g\in C^{\infty}(N)$ $\left\{f,g\right\}_{N}\circ\Phi\ =\ \left\{f\circ\Phi,g\circ\Phi\right\}_{M}.$
Definition 33. Composition of spans is given by pullback over a shared foot.
Definition 4.1. Let $D$ be a small $\mathcal{V}$-category. The enriched category of objects of $D$ is the $\mathcal{V}$-category $\operatorname{Ob}D$ with the same objects as $D$, in which the hom-objects are given by $\begin{array}[]{lcr}\operatorname{Hom}_{\operatorname{Ob}D}(d,d^{\prime})=% \varnothing,&\text{ and }&\operatorname{Hom}_{\operatorname{Ob}D}(d,d)=I,\end{array}$ for every $d\neq d^{\prime}\in D$, and the identity morphisms are given by $\operatorname{id}_{d}=\operatorname{id}_{I}\colon I\longrightarrow% \operatorname{Hom}_{\operatorname{Ob}D}(d,d)=I,$ for every $d\in D$. There is an inclusion $i\colon\operatorname{Ob}D\to D$ given by the $\mathcal{V}$-functor which is the identity on objects, and with $i_{d,d^{\prime}}\colon\operatorname{Hom}_{\operatorname{Ob}D}(d,d^{\prime})=% \varnothing\longrightarrow\operatorname{Hom}_{D}(d,d^{\prime})$ corresponding to the unique morphism in $\mathcal{V}$ from the initial object to $\operatorname{Hom}_{D}(d,d^{\prime})$, for every $d\neq d^{\prime}\in D$, and $i_{d,d}\colon\operatorname{Hom}_{\operatorname{Ob}D}(d,d)=I\longrightarrow% \operatorname{Hom}_{D}(d,d)$ corresponding to the identity morphism $\operatorname{id}_{d}$ in $D$, for every $d\in D$.
