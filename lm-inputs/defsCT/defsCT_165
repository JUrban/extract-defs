Definition. A left Quillen presheaf on a small category $I$ is a contravariant functor $M_{\bullet}\colon I\rightarrow\mathbf{Cat}$, written as $i\mapsto M_{i}$ such that for every $i\in\textrm{Ob}(I)$ the category $M_{i}$ is a model category and for every map $f\colon i\rightarrow j$ in $I$ the map $f^{*}\colon M_{j}\rightarrow M_{i}$ is left Quillen. (One can similarly define right Quillen presheaves.)
Definition. We call homotopy locally constant a presheaf $\mathscr{F}$ such that there is a cover $U_{i}$ such that all the restrictions $\mathscr{F}|_{U_{i}}$ are weakly equivalent to constant sheaves. (In particular the transition functions between $\mathscr{F}(U_{i})|_{U_{ij}}$ and $\mathscr{F}(U_{j})|_{U_{ij}}$ are weak equivalences.) Then we denote by $LC_{H}(X)$ the subcategory of homotopy locally constant hypersheaves of perfect chain complexes. This is a dg-category and the hom-spaces are derived hom-spaces of complexes of sheaves.
Definition. Let $M_{\bullet}$ be a left Quillen presheaf of model categories. We define a left section to be a tuple consisting of $(X_{i},\phi_{f})$ for $i\in\textrm{Ob}(I)$ and $f\in\textrm{Mor}(I)$ where $X_{i}\in M_{i}$ and $\phi_{f}\colon f^{*}X_{j}\rightarrow X_{i}$, satisfies $\phi_{g}\circ(g^{*}\phi f)=\phi_{f\circ g}\colon(f\circ g)^{*}X_{k}\rightarrow X% _{i}$ for composable pairs $g\colon X_{i}\rightarrow X_{j}$, $f\colon X_{j}\rightarrow X_{k}$.t A morphism of sections consists of $m_{i}\colon X_{i}\rightarrow Y_{i}$ in $M_{i}$ making the obvious diagrams commute. We write the category of sections of $M_{\bullet}$ as $\operatorname*{psect}(I,M_{\bullet})$. The levelwise weak equivalences make it into a homotopical category.
Definition 1.1. An axiomatic stable homotopy theory is a presentable, symmetric monoidal stable $\infty$-category $(\mathcal{C},\otimes,\mathbf{1})$ where the tensor product commutes with all colimits.
Definition 2.2. Let $\mathrm{Cat}_{\infty}$ be the $\infty$-category of $\infty$-categories. Given $\infty$-categories $\mathcal{C},\mathcal{D}$, the mapping space $\mathrm{Hom}_{\mathrm{Cat}_{\infty}}(\mathcal{C},\mathcal{D})$ is the maximal $\infty$-groupoid contained in the $\infty$-category $\mathrm{Fun}(\mathcal{C},\mathcal{D})$ of functors $\mathcal{C}\to\mathcal{D}$.
Definition 2.3. We define an $\infty$-category $\mathrm{Cat}_{\infty}^{\mathrm{st}}$ of (small) stable $\infty$-categories where: (1) The objects of $\mathrm{Cat}_{\infty}^{\mathrm{st}}$ are the stable $\infty$-categories which are idempotent complete.1This can be removed, but will be assumed for convenience. (2) Given $\mathcal{C},\mathcal{D}\in\mathrm{Cat}_{\infty}^{\mathrm{st}}$, the mapping space $\mathrm{Hom}_{\mathrm{Cat}_{\infty}^{\mathrm{st}}}(\mathcal{C},\mathcal{D})$ is a union of connected components in $\mathrm{Hom}_{\mathrm{Cat}_{\infty}}(\mathcal{C},\mathcal{D})$ spanned by those functors which preserve finite limits (or, equivalently, colimits). Such functors are called exact.
Definition 2.1. A stable homotopy theory is a presentable, symmetric monoidal stable $\infty$-category $(\mathcal{C},\otimes,\mathbf{1})$ where the tensor product commutes with all colimits.
Definition 5. A bi-closed category is a monoidal category in which for all object $B$, the functor $\_\otimes B$ has a right adjoint $\_/B$ and the functor $B\otimes\_$ has a right adjoint $B\backslash\_$.
Definition 10. A free self-dual pivotal category is the free pivotal category generated by a category $\mathcal{C}$ where for each object $A\in\mathcal{C}$, $A\simeq A^{*}$.
Definition 14. Let $(\mathcal{C},S)$ be the syntactic type system, $(\mathcal{C}^{\prime},S^{\prime})$ be the semantic type system and $F$ be a morphism from $(\mathcal{C},S)$ to $(\mathcal{C}^{\prime},S^{\prime})$. We call grammar induction the problem of, given a training sample $T$ for the type system $(\mathcal{C}^{\prime},S^{\prime})$, find a lexicon $h:W\times Ob(\mathcal{C}^{\prime})\rightarrow\mathcal{C}$ such that $\text{for all pair }(w_{i},t_{i})\in T,F(h(w_{i},t_{i}))\simeq t_{i}\text{ and% $h(w_{i},t_{i})$ is functional}$ $\text{and for all sentence }(w_{1},t_{1}),\dots,(w_{n},t_{n})\in T,\mathcal{C}% (h(w_{1},t_{1})\otimes\dots\otimes h(w_{n},t_{n}),S)\text{ is not empty}$
Definition 7. A pivotal category is an autonomous category with a monoidal natural isomorphism between $A^{r}$ and $A^{l}$. We set $A^{*}=A^{l}$.
Definition 12. Let $\mathcal{L}$ be the free bi-closed monoidal category. The set $P\subset Ob(L)$ is the closure by $/$ and $\backslash$ of the set of basic types. Given a type system $(\mathcal{C},S)$ and a bi-closed functor $F:\mathcal{L}\rightarrow\mathcal{C}$ the set of functional types in $\mathcal{C}$ is $F(P)$.
Definition 2. A lexicon $l$ over a set of words $W$ and a type system $(\mathcal{C},S)$ is a function $l:W\rightarrow\mathcal{C}$.
Definition 15. A training sample is said exact for some syntactic type $t$ when it contains a word-type pair $(w,F(t))$ such that for all solutions $h$ of this training sample, $h(w,F(t))=t$. We say that a grammar induction problem has exact samples when there exists exact samples for each syntactic type $t$.
Definition 8. A compact closed category is pivotal category which is symmetric, i.e. for each objects $A$ and $B$ there is a monoidal natural isomorphism $s_{A,B}:A\otimes B\rightarrow B\otimes A$ such that $s_{A,B}^{-1}=s_{A,B}$.
Definition 13. A training sample for a type system $(\mathcal{C},S)$ and a finite set of words $W$ is a finite set of sentences, where each sentence is a finite sequence of the form $(w_{1},t_{1}),\dots,(w_{n},t_{n})$, where $w_{i}\in W$ and $t_{i}\in\mathcal{C}$ is functional, and such that all the sentences are grammatical for their respective type assignment.
Definition 9. A self-dual compact closed category is a compact closed category with a monoidal natural isomorphism between $A$ and $A^{*}$.
Definition 4. A functor of type systems from $(\mathcal{C}_{1},S_{1})$ to $(\mathcal{C}_{2},S_{2})$ is a functor of monoidal categories $F:\mathcal{C}_{1}\rightarrow\mathcal{C}_{2}$ such that $F(S_{1})=S_{2}$.
Definition 1. A type system $(\mathcal{C},S)$ is a strict monoidal category $\mathcal{C}$ with a distinguished object $S$ in $\mathcal{C}$.
Definition 11. A symmetric closed category is a symmetric bi-closed category. For all objects $A$ and $B$, $B\backslash A\simeq A/B$, so we note $A|B=A/B$.
Definition 6. An autonomous category1Some authors use the name compact closed category instead, but this term has been used for both symmetric and planar categories. As we want to insist on the fact that these categories are not symmetric (contrary to some other ones in this article), we follow the terminology of [23]. is a monoidal category where for each object $A$, there are two objects, the left ($A^{l}$) and right ($A^{r}$) adjoints, equipped with four morphisms $\epsilon^{l}_{A}:A^{l}\otimes A\rightarrow 1$, $\epsilon^{r}_{A}:A\otimes A^{r}\rightarrow 1$, $\eta^{l}_{A}:1\rightarrow A\otimes A^{l}$ and $\eta^{r}_{A}:1\rightarrow A^{r}\otimes A$ satisfying the following equalities : $\displaystyle(\epsilon^{r}_{A}\otimes 1_{A})\circ(1_{A}\otimes\eta^{r}_{A})=1_% {A}$ $\displaystyle(\epsilon^{l}_{A}\otimes 1_{A^{l}})\circ(1_{A^{l}}\otimes\eta^{l}% _{A})=1_{A^{l}}$ $\displaystyle(1_{A}\otimes\epsilon^{l}_{A})\circ(\eta^{l}_{A}\otimes 1_{A})=1_% {A}$ $\displaystyle(1_{A^{r}}\otimes\epsilon^{r}_{A})\circ(\eta^{r}_{A}\otimes 1_{A^% {r}})=1_{A^{r}}$
Definition 3. A sequence of words $w_{1},\dots,w_{n}\in W$ is grammatical for a lexicon $l$ when $\mathcal{C}(l(w_{1})\otimes\dots\otimes l(w_{n}),S)$ is not empty.
Definition 3.1. Let $(\mathcal{K},U)$ be an accessible category with concrete directed colimits. We say that $\mathcal{K}$ is coherent if for each commutative triangle
Definition. Let $\mathscr{C}$ be a presheaf with values in a model category $\mathscr{M}$ and let $\mathscr{C}^{\#}$ be a fibrant replacement for $\mathscr{C}$ in the local model category $\mathscr{M}^{J}_{\check{\tau}}$ defined above. Then we define global sections as $R\Gamma(X,\mathscr{C})=\mathscr{C}^{\#}(X)$
Definition. We will call a cover in $(\mathbf{Set}^{Op(X)^{\textrm{op}}},\tau)$ a good cover if all its elements and all their finite intersections are contractible. Correspondingly a good hypercover is a hypercover such that all its open sets $U_{n}^{(i)}$ are contractible.
Definition. We define the dg-category of $\infty$-local systems on a simplicial set $K$ as ${\mathbf{Ch}_{pe}}^{K}$. We write $\mathscr{Y}(K)$ for ${\mathbf{Ch}_{pe}}^{K}$. For a topological space $X$ we recall the (unpointed) singular simplicial set $\operatorname{\textrm{Sing}_{*}}(X)$ and define $\mathscr{Y}(X)\coloneqq\mathscr{Y}(\operatorname{\textrm{Sing}_{*}}(X))$. We also define $\mathscr{Y}^{u}(K)={\mathbf{Ch}_{dg}}^{K}$ and $\mathscr{Y}^{u}(X)={\mathbf{Ch}_{dg}}^{\operatorname{\textrm{Sing}_{*}}(X)}$.
Definition. Let the set of hypercovers in $\mathscr{M}^{J}$ be defined as $\check{H}_{\tau}=\{\operatorname*{hocolim}_{I}(U_{*}\cdot\mathbf{1}_{\mathscr{% M}})\rightarrow h_{W}\cdot\mathbf{1}_{\mathscr{M}}\ |\ U_{*}\rightarrow h_{W}% \textrm{ a hypercover\}}$ where we take the levelwise tensor and the homotopy colimit in $\mathscr{M}^{J}$ with the projective model structure. Since disjoint union commutes with cofibrant replacement we could equivalently take the limit of $U_{n}$ over $\Delta^{\textrm{op}}$, the opposite of the simplex category.
Definition. We call a presheaf $\mathscr{F}$ a hypersheaf if it satisfies $\mathscr{F}(W)\simeq\operatorname*{holim}_{I^{\textrm{op}}}\mathscr{F}(U_{*})% \textrm{ for every hypercover }U_{*}\textrm{ of every open }W\subset X$ The limit is over $I^{\textrm{op}}=\cup I_{n}$; we could write it $\operatorname*{holim}_{n}\operatorname*{holim}_{i\in I_{n}}\mathscr{F}(U_{n}^{% (i)})$ which can be considered as $\operatorname*{holim}_{n\in\Delta}\mathscr{F}(U_{n})$ using the convention above. This condition is also called descent with respect to hypercovers.
Definition. Let $\mathscr{M}^{J}_{\tau}\coloneqq(\mathscr{M}^{J}_{proj})_{H_{\tau}}$ denote the left Bousfield localization of $\mathscr{M}^{J}_{proj}$ with respect to $H_{\tau}=\{S\cdot\mathbf{1}_{\mathscr{M}}\rightarrow h_{W}\cdot\mathbf{1}_{% \mathscr{M}}\ |\ S\rightarrow h_{W}\in\tau\}$ Here $h_{-}$ denotes the covariant Yoneda embedding $X\mapsto\mbox{Hom}(-,X)$.
Definition. Consider the presheaf $\underline{k}$ that is constant with value $k\in\mathbf{dgCat}$ and let $\underline{k}^{\#}$ be a fibrant replacement for $\underline{k}$. Then we define Morita cohomology as $R\Gamma(X,\underline{k})\coloneqq R\Gamma(X,{\underline{\mathbf{Ch}}{}_{pe}})=% \underline{k}^{\#}(X)$ in $Ho(\mathbf{dgCat}_{Mor})$. We write $\mathscr{H}^{M}(X)\coloneqq R\Gamma(X,\underline{k})$.
Definition. We denote by $\mathbf{dgCat}_{Mor}$ the category of dg-categories with the Morita model structure, i.e. the Bousfield localization of $\mathbf{dgCat}_{DK}$ along functors that induce equivalences of the derived categories, see Chapter 2 of [33].
Definition. A hypercover of an open set $W\subset X$ is a simplicial presheaf $U_{*}$ on the topological space $W$ such that: (1) For all $n\geq 0$ the presheaf $U_{n}$ is isomorphic to a disjoint union of a small family of presheaves representable by open subsets of $W$. We can write $U_{n}=\amalg_{i\in I_{n}}h_{U_{n}^{(i)}}$ for a set $I_{n}$ where the $U_{n}^{(i)}\subset W$ are open. (2) The map $U_{0}\rightarrow*$ is in $\tau$, i.e. the $U_{0}^{(i)}$ form an open cover of $W$. (3) For every $n\geq 0$ the map $U_{n+1}\rightarrow(cosk_{n}U_{*})_{n+1}$ is in $\tau$. Here $(cosk_{n}U)_{n+1}=M_{n}^{W}U$ is the $n$-th matching object computed in simplicial presheaves over $W$.
Definition. With this equivalence in mind we can define the Morita homology $\mathscr{H}_{M}(K)$ of a simplicial set $K$ as ${\mathbf{Ch}_{pe}}\otimes K$.
Definition. Fix a basis of contractible sets $\mathfrak{U}^{s}$ for $X$. Let $\underline{P}$ be a constant presheaf with fiber $P\in\mathscr{M}$ and define a presheaf $\mathscr{L}^{s}_{\underline{P}}$ by $\mathscr{L}^{s}_{\underline{P}}(U)=\operatorname*{holim}_{V\subset U,V\in% \mathfrak{U}^{s}}\underline{RP}(V)$ where $P\rightarrow RP$ is a fibrant replacement in $\mathscr{M}$. Denote the natural map by $\lambda\colon\underline{P}\rightarrow\mathscr{L}^{s}_{\underline{P}}$. The restriction maps are induced by inclusion of diagrams.
Definition. Let the left Bousfield localization of $\mathscr{M}^{J}_{\tau}$ at the hypercovers $\check{H}_{\tau}$ be denoted by $\mathscr{M}^{J}_{\check{\tau}}$ and call it the local model structure.
Definition 5.20 1. A DCO is a pair $(A,\mathpzc{F}_{A})$ where $A$ is a set, and ${\mathpzc{F}_{A}}$ is a set of partial endofunctions on $A$ such that $\mathrm{id}_{A}\in{\mathpzc{F}_{A}}$, and for all $\alpha,\beta\in{\mathpzc{F}_{A}}$ there exists a $\gamma\in{\mathpzc{F}_{A}}$ with $\beta\circ\alpha\subseteq\gamma$. 2. A monotone map between $(A,\mathpzc{F}_{A})$ and $(B,\mathpzc{F}_{B})$ is a function $f:A\rightarrow B$ such that for all $\alpha\in{\mathpzc{F}_{A}}$ there exists a $\beta\in{\mathpzc{F}_{B}}$ with $f\circ\alpha\subseteq\beta\circ f$. 3. For monotone $f,g:(A,\mathpzc{F}_{A})\rightarrow(B,\mathpzc{F}_{B})$, we define $f\leq g$ if there exists a $\beta\in{\mathpzc{F}_{B}}$ with $\beta\circ f=g$.
Definition 5.27 A cartesian DCO $(A,\mathpzc{F}_{A})$ is called functionally complete, if there exists an $@\in{\mathpzc{F}_{A}}$ (called the universal function) such that for all $\alpha\in{\mathpzc{F}_{A}}$ there exists a total $\widetilde{\alpha}\in{\mathpzc{F}_{A}}$ satisfying $\alpha(a\wedge b)\preceq@(\widetilde{\alpha}(a)\wedge b)$. for all $a,b\in A$.
Definition 3.13 Let $\mathbb{X}$ be a locally small exact category such that $\Gamma:\mathbb{X}\rightarrow{\mathbf{Set}}$ has a right adjoint $\nabla:{\mathbf{Set}}\rightarrow\mathbb{X}$. 1. An object $A\in\mathbb{X}$ is called separated, if $\eta_{A}:A\rightarrow\nabla\Gamma A$ is monic. 2. $f:X\rightarrow Y$ is called prone, if the naturality square $\xymatrix@R-3mm{X\save*!/dr-1.2pc/dr:(-1,1)@^{|-}\restore\ar[r]_{f}\ar[d]_{% \eta_{A}}&Y\ar[d]^{\eta_{B}}\\ \nabla\Gamma X\ar[r]^{-}{\nabla\Gamma f}&\nabla\Gamma Y}$ is a pullback (observe that $X$ is separated whenever $Y$ is separated and $f$ is prone). 3. $D\in\mathbb{X}$ is called discrete if it is right orthogonal to all prone regular epis, i.e. if in any diagram $\xymatrix@R-3mm{X\ar@{->>}[d]_{e}\ar[r]|(.45)f&D\\ Y\ar@{-->}[ur]_{h}}$ where $e$ is a prone regular epi, there exists a (unique) $h$ making the triangle commute. \definitionSymbol
Definition 2.1 The total category (or Grothendieck construction) of an indexed preorder $\mathcal{A}:{\mathbf{Set}}^{\mathsf{op}}\rightarrow\mathbf{Ord}$ is the category $\textstyle\int\!\mathcal{A}$ where • objects are pairs $(I\in{\mathbf{Set}},\varphi\in\mathcal{A}(I))$, and • morphisms $(I,\varphi)\rightarrow(J,\psi)$ are functions $f:I\rightarrow J$ such that $\varphi\leq f^{*}(\psi)$ in $\mathcal{A}(I)$. $\diamondsuit$
Definition 5.22 Let $\mathcal{D}:{\mathbf{Set}}^{\mathsf{op}}\rightarrow\mathbf{Ord}$ be an indexed preorder. 1. A predicate $\mu\in\mathcal{D}(A)$ is called discrete, if for any span $I\xeepil{e}J\xrightarrow{f}A$ of functions where $e$ is surjective, and any $\varphi\in\mathcal{D}(I)$ with $e^{*}(\varphi)\leq f^{*}(\mu)$, there exists an $h:I\rightarrow A$ such that $he=f$ and $\varphi\leq g^{*}(\mu)$. 2. $\mu\in\mathcal{D}(A)$ is called a generic predicate, if for any $\varphi\in\mathcal{D}(I)$ there exists a (not necessarily unique) function $f:I\rightarrow A$ with $\varphi\cong f^{*}(\mu)$. \definitionSymbol
Definition 4.16 A polynomial over a PAS $(\mathcal{A},\cdot)$ is a term $t[x_{1},\dots,x_{n}]$ built up from variables $x_{1},\dots,x_{n}$ ($n\geq 0$), constants in $\mathcal{A}$, and application. We write $t[a_{1},\dots,a_{n}]$ for the (possibly undefined) evaluation of $t[x_{1},\dots,x_{n}]$ with elements $a_{1},\dots,a_{n}\in\mathcal{A}$ substituted for the variables.
Definition 2.3 A cartesian indexed preorder $\mathcal{A}$ is called shallow, if either of the following equivalent conditions holds. 1. The inclusion $\Gamma\subseteq\lvert\cdot\rvert:\textstyle\int\!\mathcal{A}\rightarrow{% \mathbf{Set}}$ of functors is an equality. 2. $\textstyle\int\!\mathcal{A}$ is well pointed. 3. $\mathcal{A}(1)$ is equivalent to the terminal preorder. \definitionSymbol
Definition 4.14 1. A partial applicative structure (PAS) is pair $(\mathcal{A},\cdot)$ of a set $\mathcal{A}$ and a partial binary application operation $(-\,\mathclose{\cdot}\,-):\mathcal{A}\times\mathcal{A}\rightharpoonup\mathcal{A}$. 2. A partial combinatory algebra (PCA) is a pas $(\mathcal{A},\cdot)$ which contains elements $\mathsf{k},\mathsf{s}\in\mathcal{A}$ satisfying $\text{(i) }\mathsf{k}\mathclose{\cdot}a\mathclose{\cdot}b=a\qquad\text{(ii) }% \mathsf{s}\mathclose{\cdot}a\mathclose{\cdot}b\mathclose{\downarrow}\qquad% \quad\text{(iii) }a\mathclose{\cdot}c\mathclose{\cdot}(b\mathclose{\cdot}c)% \preceq\mathsf{s}\mathclose{\cdot}a\mathclose{\cdot}b\mathclose{\cdot}c$ for all $a,b,c\in\mathcal{A}$. \definitionSymbol
Definition 5.21 Let $(\mathcal{A},\cdot)$ be a PCA. The set $\mathpzc{F}_{\mathcal{A}}$ of computable functions over $\mathcal{A}$ is the set of partial functions $\phi_{a}:\mathcal{A}\rightharpoonup\mathcal{A}\quad\text{defined by}\quad\phi_% {a}(b)\simeq a\mathclose{\cdot}b$ for $a\in\mathcal{A}$. The pair $(\mathcal{A},\mathpzc{F}_{\mathcal{A}})$ is a DCO by combinatory completeness, and we call it the DCO induced by $(\mathcal{A},\cdot)$.
Definition 3.8 A weak proof classifier in a finite-limit category $\mathbb{C}$ is a map $t:T\rightarrow\Sigma$ such that for any $f:J\rightarrow I$ there exists a $\chi:I\rightarrow\Sigma$ and maps $h:J\rightarrow P$ and $k:P\rightarrow J$ $\xymatrix@R-1mm@C+1mm{J\ar@/_{1}mm/[rd]_{f}\ar@/^{2}.5mm/[r]|h&P\ar[r]\save*!/% dr-1.2pc/dr:(-1,1)@^{|-}\restore\ar[d]_{g}\ar@/^{1}.5mm/[l]|k&T\ar[d]^{t}\\ &I\ar[r]^{\chi}&\Sigma}$ satisfying $gh=f$ and $fk=g$ where $g$ is the pullback of $t$ along $\chi$.
Definition 5.24 (Partitioned assemblies) Analogous to PCAs, we define the category $\mathbf{PAsm}(A,\mathpzc{F}_{A})$ of partitioned assemblies over a DCO $(A,\mathpzc{F}_{A})$ to be the total category of $\mathrm{fam}(A,\mathpzc{F}_{A})$. Thus, objects are pairs $(I\in{\mathbf{Set}},\varphi:I\rightarrow A)$, and morphisms from $(I,\varphi)$ to $(J,\psi)$ are functions $f:I\rightarrow J$ such that there exists an $\alpha\in{\mathpzc{F}_{A}}$ – called a realizer of $f$ – satisfying $\alpha\circ\varphi=\psi\circ f$.
Definition 4.19 Let $(\mathcal{A},\cdot)$ be a PCA. The realizability topos $\mathbf{RT}(\mathcal{A},\cdot)$ is the exact completion of $\mathbf{PAsm}(\mathcal{A},\cdot)$.
Definition 7.1. Let $T$ be a monad on a model category ${\mathcal{M}}$ such that ${\mathcal{M}}^{T}$ has a transferred model structure. We say that a homotopical localization $(L,l)$ on ${\mathcal{M}}$ preserves $T$-algebras if there is a natural transformation $g\colon K\to L^{\prime}$ of endofunctors on ${\text{\rm Ho}}({\mathcal{M}}^{T})$ together with natural isomorphisms (i) $h\colon{\rm Id}\to K$ in ${\text{\rm Ho}}({\mathcal{M}}^{T})$ and (ii) $\beta\colon LU\to UL^{\prime}$ in ${\text{\rm Ho}}({\mathcal{M}})$ such that $\beta\circ lU\simeq U(g\circ h)$, where $U\colon{\text{\rm Ho}}({\mathcal{M}}^{T})\to{\text{\rm Ho}}({\mathcal{M}})$ is the forgetful functor.
Definition 3. Let $S^{\bullet}({{\bf k}_{\varepsilon}})$ be a semijective resolution of the left $A^{\sharp}_{0}$–module ${\bf k}_{\varepsilon}$. Consider the complex $S_{A}\otimes^{N_{0}}_{B_{0}}S^{\bullet}({{\bf k}_{\varepsilon}})$ of left $A^{\sharp}$–modules, where $A_{0}$ acts on the semiregular bimodule $S_{A}$ by right semiregular action and the structure of a left $A^{\sharp}$–module on $S_{A}\otimes^{N_{0}}_{B_{0}}S^{\bullet}({{\bf k}_{\varepsilon}})$ is induced by the left semiregular action of $A^{\sharp}$ on $S_{A}$. The double graded algebra ${\rm Hk}^{\frac{\infty}{2}+\bullet}(A,A_{0},{\varepsilon})={\rm hom}_{{D}(A^{% \sharp})_{0}}^{{\bullet}}(S_{A}\otimes^{N_{0}}_{B_{0}}S^{\bullet}({{\bf k}_{% \varepsilon}}),S_{A}\otimes^{N_{0}}_{B_{0}}S^{\bullet}({{\bf k}_{\varepsilon}}))$ is called the semi–infinite Hecke algebra of the triple $(A,A_{0},{{\varepsilon}})$.
Definition 2. The semiproduct $M\otimes^{N}_{B}M^{\prime}$ of modules $M\in{\rm mod}-A$ and $M^{\prime}\in A^{\sharp}-{\rm mod}$ is the image of the subspace $M\otimes^{N}M^{\prime}\subset M\otimes M^{\prime}$ under the canonical projection $M\otimes M^{\prime}\rightarrow M\otimes_{B}M^{\prime}$, $M\otimes^{N}_{B}M^{\prime}={\rm Im}(M\otimes^{N}M^{\prime}\rightarrow M\otimes% _{B}M^{\prime}).$
