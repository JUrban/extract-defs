Definition A.23. Let $T$ be a topological space equipped with a stratification $\mathcal{S}$, and let $f\colon T^{\prime}\to T$ be a morphism, where $T^{\prime}$ is a topological space or stack. We will say that $f$ is a stratified fibration with respect to $\mathcal{S}$ if the restrictions of $f$ to the strata of $\mathcal{S}$ are fibrations (in our case, this will always mean “locally the projection from a product”).
Definition 7.4. Let $X$ be a log scheme over a ground ring $k.$ Then the profinite homotopy type of $X$ is the profinite étale homotopy type of its infinite root stack $\sqrt[\infty]{X}.$
Definition 2.11. A space $X$ in $\mathcal{S}$ is $\pi$-finite if all its homotopy groups are finite, it has only finitely many non-trivial homotopy groups, and finitely many connected components.
Definition A.5. A log scheme $X$ is quasi-coherent if there is an étale cover $U_{i}$ of $X$, monoids $P_{i}$ and morphisms of log schemes $f_{i}\colon U_{i}\to\mathrm{Spec}\,k[P_{i}]$ that are strict, i.e. the log structure on $U_{i}$ is pulled back from $\mathrm{Spec}\,k[P_{i}]$ via $f_{i}$. The monoid $P_{i}$ and the map $f_{i}$ are a chart for the log structure over $U_{i}$. A log scheme $X$ is coherent (resp. fine, resp. fine and saturated) if the monoids $P_{i}$ above can be taken finitely generated (resp. finitely generated and integral, resp. finitely generated, integral and saturated).
Definition 2.2. If $\mathscr{E}$ is any accessible $\infty$-category with finite limits, then we define the $\infty$-category of pro-objects of $\mathscr{E},$ $\operatorname{Pro}\left(\mathscr{E}\right),$ to be the full subcategory of $\operatorname{Fun}\left(\mathscr{E},\operatorname{Gpd}_{\infty}\right)^{op}$ on those functors $F:\mathscr{E}\to\operatorname{Gpd}_{\infty}$ which are accessible and preserve finite limits.
Definition A.16. The infinite root stack $\sqrt[\infty]{X}$ of the log scheme $X$ is the pro-object in algebraic stacks $(\sqrt[n]{X})_{n\in\mathbb{N}}$.
Definition 3.23. Denote by $\mathscr{C}_{KN}\left(X\right)$ the following category: the objects consist of a triple $\left(T,p,s\right)$ where • $T$ is a topological space in $\mathbf{Top}_{\mathbb{C}}^{s},$ • $p$ is a continuous map $p:T\to X_{{an}},$ • and $s$ is a morphism of sheaves of abelian groups $s:p^{*}M^{\mathrm{gp}}\rightarrow\underline{S}^{1}_{T}$ such that $s(f)=\frac{f}{|f|}$ for $f\in\mathcal{O}_{X}^{\times}$. The morphisms $\left(T,p,s\right)\to\left(s,q,r\right)$ are continuous maps $f:T\to S$ such that $f^{*}\left(r\right)=s.$
Definition A.22. By a stratification of a topological space $T$ we mean a collection of closed subsets $\mathcal{S}=\{S_{i}\subseteq T\}_{i\in I}$ where $I$ is partially ordered, and the following are satisfied: • if $i\leq j$, then $S_{i}\subseteq S_{j}$, and • the stratification is locally finite: every point $t\in T$ has an open neighborhood $U$ such that only finitely many of the intersections $U\cap S_{i}$ are non-empty. The locally closed subsets $S_{j}\setminus S_{i}$ will be called the strata of the stratification.
Definition 3.16. We denote the composite from Proposition 3.15 by $\Pi_{\infty}:\mathbb{H}\mathrm{ypSh}_{\infty}\left(\mathbf{Top}_{\mathbb{C}}% \right)\to\mathcal{S}.$ For $F$ a hypersheaf on $\mathbf{Top}_{\mathbb{C}},$ we call $\Pi_{\infty}\left(F\right)$ its fundamental $\infty$-groupoid.
Definition A.9. For a monoid $P$ we denote by $\mathbb{C}(P)$ the analytification of the spectrum of the monoid algebra $\mathrm{Spec}\,\mathbb{C}[P]$.
Definition 2.12. Let $\mathcal{S}^{fc}$ denote the full subcategory of the $\infty$-category $\mathcal{S}$ on the $\pi$-finite spaces. $\mathcal{S}^{fc}$ is essentially small and idempotent complete (and hence accessible). The $\infty$-category of profinite spaces is defined to be the $\infty$-category $\operatorname{Prof}\left(\mathcal{S}\right)\mathrel{\vbox{\hbox{\scriptsize.}% \hbox{\scriptsize.}}}=\operatorname{Pro}\left(\mathcal{S}^{fc}\right).$
Definition 2.1. A category with order, or categorder, is a category $\mathscr{C}$ together with an ordering of each hom set $\mathscr{C}(C,C^{\prime})$ such that for all morphisms $\xymatrix{C\ar[r]^{c_{1},c_{2}}&C^{\prime}\ar[r]^{c_{1}^{\prime},c_{2}^{\prime% }}&C^{\prime\prime}}$, if $c_{1}\leq c_{2}$ and $c_{1}^{\prime}\leq c_{2}^{\prime}$, then $c_{1}^{\prime}\circ c_{1}\leq c_{2}^{\prime}\circ c_{2}$. A prefunctor $F\colon\mathscr{C}\to\mathscr{D}$ between two categorders consists of an assignment $F\colon{\rm ob}\mathscr{C}\to{\rm ob}\mathscr{D}$ and, for all $C,C^{\prime}\in{\rm ob}\mathscr{C}$, a function $F\colon\mathscr{C}(C,C^{\prime})\to\mathscr{D}(FC,FC^{\prime})$. The prefunctor $F\colon\mathscr{C}\to\mathscr{D}$ is said to be monotone if $Fc\leq Fc^{\prime}$ whenever $c\leq c^{\prime}$, for all $C\xrightarrow{c,c^{\prime}}C^{\prime}$. Finally, a monotone prefunctor $F\colon\mathscr{C}\to\mathscr{D}$ is subfunctorial if the conditions • $F({\rm id}_{C})\leq{\rm id}_{FC}$, for all $C\in{\rm ob}\mathscr{C}$ • $F(c^{\prime}\circ c)\leq Fc^{\prime}\circ Fc$ for all morphisms $\xymatrix{C\ar[r]^{c}&C^{\prime}\ar[r]^{c^{\prime}}&C^{\prime\prime}}$ in $\mathscr{C}$ hold. A subfunctorial monotone prefunctor will be called a functorder.
Definition 4.1. Let $\mathbf{A}$ be an Abelian category. We define the homological dimension of $A\in\mathbf{A}$ to be $\operatorname{hdim}(A):=\sup\big{\{}n\in\mathbf{Z}_{\geq 0}\mid\exists B\in% \mathbf{A}\ [\operatorname{ext}^{n}_{\mathbf{A}}(A,\,B)\neq 0]\big{\}}\in% \mathbf{Z}_{\geq 0}\cup\{\infty\}$ and the homological dimension of $\mathbf{A}$ to be $\operatorname{hdim}(\mathbf{A}):=\sup\{\operatorname{hdim}(A)\mid A\in\mathbf{% A}\}\in\mathbf{Z}_{\geq 0}\cup\{\infty\}$.
Definition 3.1. Let $\mathbf{A}$ be a $\mathfrak{U}$-small Abelian category. (i) The category $\operatorname{\mathcal{C}px}^{\mathrm{b}}(\mathbf{A})$ (resp. $\operatorname{\mathcal{C}px}(\mathbf{A})$) of bounded (resp. unbounded) cochain complexes in $\mathbf{A}$ is a $\mathfrak{U}$-small differential graded category. Its differential graded nerve $\operatorname{\mathcal{K}}^{\mathrm{b}}(\mathbf{A}):=\operatorname{\mathfrak{N% }}_{\mathrm{dg}}(\operatorname{\mathcal{C}px}^{\mathrm{b}}(\mathbf{A}))$ (resp. $\operatorname{\mathcal{K}}(\mathbf{A}):=\operatorname{\mathfrak{N}}_{\mathrm{% dg}}(\operatorname{\mathcal{C}px}(\mathbf{A}))$), as defined in [Lur14, 1.3.1.6, 1.3.4.5], is a stable qcategory ([Lur14, 1.3.2.10]) and its homotopy category is the usual bounded (resp. unbounded) cochain homotopy category of $\mathbf{A}$ ([Lur14, 1.3.1.11]). (ii) Let $\iota:\operatorname{\mathcal{A}c}(\mathbf{A})\hookrightarrow\operatorname{% \mathcal{K}}^{\mathrm{b}}(\mathbf{A})$ denote the inclusion of the full subqcategory spanned by the acyclic complexes. The bounded derived qcategory of $\mathbf{A}$ is the Verdier quotient $\operatorname{\mathcal{D}}^{\mathrm{b}}(\mathbf{A}):=\operatorname{\mathcal{K}% }^{\mathrm{b}}(\mathbf{A})/\operatorname{\mathcal{A}c(A)}$. By 1.3, the homotopy category of $\operatorname{\mathcal{D}}^{\mathrm{b}}(\mathbf{A})$ is the usual bounded derived category of $\mathbf{A}$. (iii) Suppose $\mathbf{A}^{\otimes}$ is a symmetric monoidal structure on $\mathbf{A}$. Then $\operatorname{\mathcal{C}px}^{\mathrm{b}}(\mathbf{A})$ (resp. $\operatorname{\mathcal{C}px}(\mathbf{A})$) inherits a symmetric monoidal structure given informally by $(K\otimes L)^{n}:=\bigoplus_{r\in\mathbf{Z}}(K^{r}\otimes L^{n-r}),\qquad% \operatorname{d}(x\otimes y):=\operatorname{d}(x)\otimes y+(-1)^{\deg(x)}x% \otimes\operatorname{d}(y).$ By [Lur14, 1.3.4.5, 4.1.3.4], there is a canonical symmetric monoidal structure $\operatorname{\mathcal{K}}^{\mathrm{b}}(\mathbf{A})\mathstrut{}^{\otimes}$ with respect to which the localization $\operatorname{\mathcal{C}px}^{\mathrm{b}}(\mathbf{A})\mathstrut{}^{\otimes}\to% \operatorname{\mathcal{K}}^{\mathrm{b}}(\mathbf{A})\mathstrut{}^{\otimes}$ is symmetric monoidal, and similarly for $\operatorname{\mathcal{C}px}(\mathbf{A})\mathstrut{}^{\otimes}\to\operatorname% {\mathcal{K}}(\mathbf{A})\mathstrut{}^{\otimes}$. (iv) In particular, in the setting of $(iii)$, if $(-)\otimes(-)$ is exact separately in each variable, $K$ and $L$ are two bounded-above cochain complexes in $\mathbf{A}$ and $L$ is acyclic, then $K\otimes L$, being the total complex of a double complex with acyclic columns and bounded diagonals, is acyclic.
Definition 1.5. With the notation and hypotheses of 1.4, we refer to $\pi^{\otimes}:\mathcal{C}^{\otimes}\to(\mathcal{C}/\mathcal{A})^{\otimes}$ as a symmetric monoidal Verdier quotient of $\mathcal{C}^{\otimes}$ by $\mathcal{A}$.
Definition 2.1. Let $\Lambda$ be a unital commutative ring. (i) We let $\operatorname{dg\mathcal{C}at}_{\Lambda}$ denote the category of $\mathfrak{U}$-small $\Lambda$-linear differential graded categories and $\Lambda$-linear differential graded functors, i.e., the category of $\mathfrak{U}$-small categories enriched in $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))$. Given an object $\boldsymbol{\mathcal{C}}\in\operatorname{dg\mathcal{C}at}_{\Lambda}$, we let $\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{C}}}(-,\,-):\boldsymbol{% \mathcal{C}}^{\mathrm{op}}\times\boldsymbol{\mathcal{C}}\to\operatorname{% \mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(\operatorname{\mathcal{A% }b}))$ denote $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}$-enriched $\operatorname{hom}$-object bifunctor and, for each $n\in\mathbf{Z}$, we let $\operatorname{hom}^{n}_{\boldsymbol{\mathcal{C}}}(-,\,-):\boldsymbol{\mathcal{% C}}^{\mathrm{op}}\times\boldsymbol{\mathcal{C}}\to\operatorname{\mathcal{M}od}% _{\Lambda}(\operatorname{\mathcal{A}b})$ denote the component of $\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{C}}}(-,\,-)$ of degree $n$. (ii) The underlying category $\mathrm{Z}^{0}(\boldsymbol{\mathcal{C}})$ of $\boldsymbol{\mathcal{C}}\in\operatorname{dg\mathcal{C}at}_{\Lambda}$ is the category with the same objects as $\boldsymbol{\mathcal{C}}$ and morphisms given by $\operatorname{mor}_{\mathrm{Z}^{0}(\boldsymbol{\mathcal{C}})}(C,\,C^{\prime}):% =\ker(\operatorname{hom}^{0}_{\boldsymbol{\mathcal{C}}}(C,\,C^{\prime})\to% \operatorname{hom}^{1}_{\boldsymbol{\mathcal{C}}}(C,\,C^{\prime}))$, i.e., the closed morphisms of degree zero. Note that the category $\mathrm{Z}^{0}(\boldsymbol{\mathcal{C}})$ carries a natural $\Lambda$-linear, i.e., $\operatorname{\mathcal{M}od}_{\Lambda}(\operatorname{\mathcal{A}b})\mathstrut{% }^{\otimes}$-enriched, structure. (iii) The homotopy category $\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})$ of $\boldsymbol{\mathcal{C}}\in\operatorname{dg\mathcal{C}at}_{\Lambda}$ is the category with the same objects as $\boldsymbol{\mathcal{C}}$ and morphisms given by $\operatorname{mor}_{\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})}(C,\,C^{\prime}):% =\mathfrak{h}^{0}\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{C}}}(C,\,C% ^{\prime})$. Each $\Lambda$-linear differential graded functor $F:\boldsymbol{\mathcal{C}}\to\boldsymbol{\mathcal{D}}$ induces a functor $\mathrm{H}^{0}F:\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})\to\mathrm{H}^{0}(% \boldsymbol{\mathcal{D}})$. (iv) A $\Lambda$-linear differential graded functor $F:\boldsymbol{\mathcal{C}}\to\boldsymbol{\mathcal{D}}$ is homotopically fully faithful if it induces a quasi-isomorphism $\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{C}}}(C,\,C^{\prime})% \xrightarrow{F}\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{D}}}(FC,\,FC% ^{\prime})$ for each $(C,C^{\prime})\in\boldsymbol{\mathcal{C}}^{2}$. (v) A $\Lambda$-linear differential graded functor $F:\boldsymbol{\mathcal{C}}\to\boldsymbol{\mathcal{D}}$ is a Dwyer-Kan equivalence if it is homotopically fully faithful and $\mathrm{H}^{0}F$ is essentially surjective. We let $\mathfrak{W}_{\mathrm{DK}}$ denote the class of Dwyer-Kan equivalences in $\operatorname{dg\mathcal{C}at}_{\Lambda}$. (vi) The category $\operatorname{dg\mathcal{C}at}_{\Lambda}$ inherits a symmetric monoidal structure, denoted by $\operatorname{dg\mathcal{C}at}^{\otimes}_{\Lambda}$, from $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}$ in which the tensor product $\boldsymbol{\mathcal{C}}\otimes\boldsymbol{\mathcal{D}}$ of two objects is the $\Lambda$-linear differential graded category whose class of objects is the product $\boldsymbol{\mathcal{C}}\times\boldsymbol{\mathcal{D}}$ with $\operatorname{hom}^{\bullet}_{\boldsymbol{\mathcal{C}}\otimes\boldsymbol{% \mathcal{D}}}((C,D),\,(C^{\prime},D^{\prime})):=\operatorname{hom}^{\bullet}_{% \boldsymbol{\mathcal{C}}}(C,\,C^{\prime})\otimes\operatorname{hom}^{\bullet}_{% \boldsymbol{\mathcal{D}}}(D,\,D^{\prime})$ and with composition given by $(g\otimes g^{\prime})(f\otimes f^{\prime}):=(-1)^{\deg(g^{\prime})\deg(f)}(gf)% \otimes(g^{\prime}f^{\prime})$. A $\Lambda$-linear symmetric monoidal differential graded category (resp. functor) is an object (resp. morphism) of $\operatorname{\mathcal{CA}lg}(\operatorname{dg\mathcal{C}at}^{\otimes}_{% \Lambda})$. (vii) As explained in [Lur14, 1.3.1.6, 1.3.1.11, 1.3.1.20], there is a differential graded nerve functor $\operatorname{\mathfrak{N}}_{\mathrm{dg}}:\operatorname{dg\mathcal{C}at}_{% \Lambda}[\mathfrak{W}^{-1}_{\mathrm{DK}}]\to\operatorname{\mathcal{QC}at}$ which assigns to each $\Lambda$-linear differential graded category $\boldsymbol{\mathcal{C}}$ a qcategory $\operatorname{\mathfrak{N}}_{\mathrm{dg}}(\boldsymbol{\mathcal{C}})$ such that, in particular, $\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})\simeq\operatorname{ho}(\operatorname{% \mathfrak{N}}_{\mathrm{dg}}(\boldsymbol{\mathcal{C}}))$. We now recall a useful variant of $\operatorname{\mathfrak{N}}_{\mathrm{dg}}$, which we denote by $\operatorname{\overline{\mathfrak{N}}}_{\mathrm{dg}}:\operatorname{dg\mathcal{% C}at}_{\Lambda}[\mathfrak{W}^{-1}_{\mathrm{DK}}]\to\operatorname{\mathcal{QC}at}$ and which will allow us to obtain stable symmetric monoidal qcategories from $\Lambda$-linear pretriangulated symmetric monoidal differential graded categories. This functor $\operatorname{\overline{\mathfrak{N}}}_{\mathrm{dg}}$ is implicit in [Lur14, 1.3.1.16] and also studied in [Hin15, §3.2] and [Fao15]. By [Lur14, 1.3.1.17], for each $\boldsymbol{\mathcal{C}}\in\operatorname{dg\mathcal{C}at}_{\Lambda}$, there is an equivalence $\operatorname{\overline{\mathfrak{N}}}_{\mathrm{dg}}(\boldsymbol{\mathcal{C}})% \to\operatorname{\mathfrak{N}}_{\mathrm{dg}}(\boldsymbol{\mathcal{C}})$ and hence $\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})\simeq\operatorname{ho}(\operatorname{% \overline{\mathfrak{N}}}_{\mathrm{dg}}(\boldsymbol{\mathcal{C}}))$. We have a sequence of lax symmetric monoidal right adjoint functors $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}\xrightarrow{\mathfrak{t}^{% \leq 0,\otimes}}\operatorname{\mathcal{C}px}^{\leq 0}(\operatorname{\mathcal{M% }od}_{\Lambda}(\operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}\xrightarrow% {N^{\otimes}}\operatorname{s\mathcal{M}od}_{\Lambda}(\operatorname{\mathcal{A}% b})^{\otimes}\xrightarrow{\upsilon^{\otimes}}\operatorname{\mathcal{S}et}_{% \Delta}^{\times},$ where $\mathfrak{t}^{\leq 0}$ is the truncation functor sending a cochain complex $M$ to the cochain complex $\cdots\to M^{-2}\xrightarrow{\operatorname{d}^{-2}}M^{-1}\xrightarrow{% \operatorname{d}^{-1}}\ker(\operatorname{d}^{0})\to 0\to 0\to\cdots,$ $N$ is half of the Dold-Kan correspondence and $\upsilon$ is the forgetful functor. The composite lax monoidal functor induces a functor from the category of $\mathfrak{U}$-small $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}$-enriched categories, i.e., $\operatorname{dg\mathcal{C}at}_{\Lambda}$, to that of $\mathfrak{U}$-small $\operatorname{\mathcal{S}et}_{\Delta}^{\times}$-enriched categories. Unwinding the construction, one sees that this functor sends Dwyer-Kan equivalences of differential graded categories to Dwyer-Kan equivalences of $\operatorname{\mathcal{S}et}_{\Delta}^{\times}$-enriched categories. The $\operatorname{\mathcal{S}et}_{\Delta}^{\times}$-category obtained from a $\operatorname{s\mathcal{M}od}_{\Lambda}(\operatorname{\mathcal{A}b})\mathstrut% {}^{\otimes}$-category is necessarily fibrant ([Lur09, 1.1.5.5]) and composing with the simplicial nerve functor $\operatorname{\mathfrak{N}_{\Delta}}$ therefore provides a functor $\operatorname{\overline{\mathfrak{N}}}_{\mathrm{dg}}:\operatorname{dg\mathcal{% C}at}_{\Lambda}[\mathfrak{W}^{-1}_{\mathrm{DK}}]\to\operatorname{\mathcal{QC}at}$. (viii) An object $\boldsymbol{\mathcal{C}}\in\operatorname{dg\mathcal{C}at}_{\Lambda}$ is pretriangulated if its image under the $\operatorname{\mathcal{C}px}(\operatorname{\mathcal{M}od}_{\Lambda}(% \operatorname{\mathcal{A}b}))\mathstrut{}^{\otimes}$-enriched Yoneda embedding is stable up to homotopy equivalence under translations and mapping cones of closed morphisms of degree zero. We let $\operatorname{dg\mathcal{C}at}^{\mathrm{pretr}}_{\Lambda}\subseteq% \operatorname{dg\mathcal{C}at}_{\Lambda}$ denote the full subcategory spanned by the pretriangulated differential graded categories and we abusively let $\operatorname{dg\mathcal{C}at}^{\mathrm{pretr}}_{\Lambda}[\mathfrak{W}^{-1}_{% \mathrm{DK}}]$ denote its localization with respect to $\operatorname{dg\mathcal{C}at}^{\mathrm{pretr}}_{\Lambda}\cap\mathfrak{W}_{% \mathrm{DK}}$. By [BK91, Proposition 1], if $\boldsymbol{\mathcal{C}}$ is pretriangulated, $\mathrm{H}^{0}(\boldsymbol{\mathcal{C}})$ admits a natural triangulated-category structure. The distinguished triangles are given by the cofiber sequences, i.e., the sequences of the form $X\xrightarrow{f}Y\to\operatorname{cone}(f)\to X[1]$, with $f:X\to Y$ a closed morphism of degree zero.
Definition 3.3. Let $\mathbf{A}$ be a locally $\mathfrak{U}$-small Abelian category. (i) The category $\mathbf{A}$ is $\mathfrak{U}$-Grothendieck Abelian if it is an Abelian, locally $\mathfrak{U}$-presentable category in which $\aleph_{0}$-filtered $\mathfrak{U}$-colimits preserve finite limits. By [Bek00, 3.10], this is equivalent to the classical definition as an AB5 category with a generator. If $\mathbf{A}$ is essentially $\mathfrak{U}$-small, then its ind-completion $\operatorname{\mathcal{I}nd}(\mathbf{A})$ is $\mathfrak{U}$-Grothendieck Abelian. (ii) If $\mathbf{A}$ is $\mathfrak{U}$-Grothendieck Abelian, then $\operatorname{\mathcal{C}px}(\mathbf{A})$ admits a $\mathfrak{U}$-combinatorial model structure ([Lur09, A.2.6.1]) whose cofibrations and weak equivalences are the monomorphisms and quasi-isomorphisms, respectively ([Bek00, 3.13]), called the injective model structure and denoted by $\operatorname{\mathcal{C}px}(\mathbf{A})\mathstrut{}_{\mathrm{inj}}$. Its homotopy category is the unbounded derived category of $\mathbf{A}$. Let $\operatorname{\mathcal{D}}(\mathbf{A})$ denote the stable, locally $\mathfrak{U}$-presentable qcategory underlying $\operatorname{\mathcal{C}px}(\mathbf{A})\mathstrut{}_{\mathrm{inj}}$ ([Lur14, 1.3.4.22]). (iii) If $\mathbf{A}$ is essentially $\mathfrak{U}$-small and $\mathbf{A}^{\otimes}$ is a symmetric monoidal structure such that $(-)\otimes(-)$ is exact separately in each variable, then there is a canonical symmetric monoidal structure $\operatorname{\mathcal{I}nd}(\mathbf{A})\mathstrut{}^{\otimes}$ such that the Yoneda embedding $\mathbf{A}^{\otimes}\hookrightarrow\operatorname{\mathcal{I}nd}(\mathbf{A})% \mathstrut{}^{\otimes}$ is symmetric monoidal and $(-)\otimes(-)$ is exact and $\mathfrak{U}$-cocontinuous separately in each variable on $\operatorname{\mathcal{I}nd}(\mathbf{A})$ ([SGA72, Exposé I, 8.9.8], [Lur14, 4.8.1.13]). (iv) An object $A\in\mathbf{A}$ is Noetherian if each set of subobjects of $A$ contains a maximal element, and $\mathbf{A}$ is Noetherian if each of its objects is Noetherian.
Definition 4.4. Let $\mathcal{C}$ be a stable qcategory and $\mathcal{G}\subseteq\mathcal{C}$ a full subqcategory. If $X\in\mathcal{C}$ is a zero object whenever $\operatorname{map}_{\mathcal{C}}(G[n],\,X)$ is contractible for each $G\in\mathcal{G}$ and each $n\in\mathbf{Z}$, then we say that $\mathcal{G}$ generates $\mathcal{C}$ as a stable qcategory or we say that $\mathcal{G}$ is a generating family in $\mathcal{C}$. This definition—a direct translation of the notion of a generating family in a triangulated category as developed in [Nee01]—is not equivalent to the one introduced before Corollary 1.4.4.2 in [Lur14]. Assuming $\mathcal{C}$ admits sufficiently large coproducts, a generating family $\mathcal{G}$ in our sense induces a generator $G:=\coprod_{\mathcal{G}\times\mathbf{Z}}G[n]$ in J. Lurie’s sense. The classical definition is better adapted to a discussion of presentability: if $\mathcal{C}$ is locally $\mathfrak{U}$-presentable and each $G\in\mathcal{G}$ is $\aleph_{0}$-presentable, then $X$ is almost never $\aleph_{0}$-presentable, even if $\mathcal{G}$ is spanned by a single object.
Definition 1.1. If $\mathcal{C}$ is a $\mathfrak{U}$-small stable qcategory and $\iota:\mathcal{A}\hookrightarrow\mathcal{C}$ the inclusion of a stable subqcategory, then the Verdier quotient $\mathcal{C}/\mathcal{A}$ of $\mathcal{C}$ by $\mathcal{A}$ is the cofiber $\operatorname{cofib}(\iota)$ in the qcategory $\operatorname{\mathcal{QC}at}^{\mathrm{Ex}}$ of $\mathfrak{U}$-small stable qcategories and exact functors.
Definition 2.5. [[Hir03] Definition 15.3.3] Let $\mathcal{C}$ be a Reedy category and $\mathcal{M}$ a model category. Let $f:\mathcal{X}\to\mathcal{Y}$ be a map of $\mathcal{C}$-diagrams, then 1. $f$ is a Reedy weak equivalence if for every object $\alpha$ of $\mathcal{C}$, the map $f_{\alpha}:\mathcal{X}_{\alpha}\to\mathcal{Y}_{\alpha}$ is a weak equivalence in $\mathcal{M}$. 2. $f$ is a Reedy cofibration if for every object $\alpha$ of $\mathcal{C}$, the relative latching map $\mathcal{X}_{\alpha}\coprod_{L_{\alpha}\mathcal{X}}L_{\alpha}\mathcal{Y}\to% \mathcal{Y}_{\alpha}$ is a cofibration in $\mathcal{M}$. 3. $f$ is a Reedy fibration if for every object $\alpha$ of $\mathcal{C}$, the relative matching map $\mathcal{X}_{\alpha}\to\mathcal{Y}_{\alpha}\times_{M_{\alpha}\mathcal{Y}}M_{% \alpha}\mathcal{X}$ is a fibration in $\mathcal{M}$. This defines a model structure on $\mathcal{M}^{\mathcal{C}}$, called the Reedy model category structure.
Definition 3.3. For any map $\sigma:[n]\to[m]$ in the simplicial category $\bf{\Delta}$, we can define the induced map $\sigma^{*}:\Delta_{m}(\mathcal{B})\to\Delta_{n}(\mathcal{B})$. Actually for an object $(\mathcal{E}=\{E_{0},\ldots,E_{m}\},\phi)$ of $\Delta_{m}(\mathcal{B})$, we define $\sigma_{*}(\mathcal{E})=\{E_{\sigma(0)},\ldots,E_{\sigma(n)}\}$ and $(\sigma_{*}(\phi))^{k,1-k}_{i_{0}\ldots i_{k}}=\phi^{k,1-k}_{\sigma(i_{0})% \ldots\sigma(i_{k})}.$ $\sigma_{*}$ of morphisms are defined similarly.
Definition 4.2 ([DHI04] Definition 4.13). A simplicial space $X_{\bullet}$ is said to be split, or to have free degeneracies, if there exist subspaces $N_{k}\hookrightarrow X_{k}$ such that the canonical maps $N_{k}\coprod L_{k}X\to X_{k}$ are isomorphisms for all $k\geq 0$, where $L_{k}X$ is the $k$-th latching object of $X_{\bullet}$. This is equivalent to requiring that the canonical map $\coprod_{\sigma}N_{\sigma}\to X_{k}$ is an isomorphism, where the variable $\sigma$ ranges over all surjective maps in $\bf{\Delta}$ of the form $[k]\to[n]$, $N_{\sigma}$ denotes a copy of $N_{n}$, and the map $N_{\sigma}\to X_{k}$ is the one induced by $\sigma^{*}:X_{n}\to X_{k}$.
Definition 2.9 ([Hir03] Definition 18.6.3 and 19.8.1 or [Rie14] Example 4.3.1 ). Let $\mathcal{X}=\{X^{\bullet}\}$ be a cosimplicial object in a framed model category $\mathcal{M}$. The totalization of $\mathcal{X}$ $\text{Tot}(\mathcal{X})$ is the equalizer of the maps $\prod_{[n]\in Ob(\mathbf{\Delta})}(X^{n})^{\Delta[n]}\underset{\psi}{\overset{% \phi}{\rightrightarrows}}\prod_{(\sigma:[n]\to[m])}(X^{n})^{\Delta[n]}$ where the projection of the map $\phi$ on the factor $\sigma:[n]\to[m]$ is the composition of the natural projection from the product with the map $\Delta_{n}(\sigma_{*}):(X^{n})^{\Delta[n]}\to(X^{m})^{\Delta[n]}$ and the projection of the map $\psi$ on the factor $\sigma:[n]\to[m]$ is the composition of the natural projection from the product with the map $\sigma^{*}(X^{m}):(X^{m})^{\Delta[m]}\to(X^{m})^{\Delta[n]}.$
Definition 4.3 ([OTT81b] Definition 1.3 or [Wei15] Definition 5). Let $(X,\mathcal{R})$ be a ringed space and $\mathcal{U}=\{U_{i}\}$ be a locally finite open cover of $X$. A twisted complex consists of a graded sheaves $E^{\bullet}_{i}$ of $\mathcal{R}$-modules on each $U_{i}$ together with $a=\sum_{k\geq 0}a^{k,1-k}$ where $a^{k,1-k}\in C^{k}(\mathcal{U},\text{Hom}^{1-k}(E,E))$ such that they satisfy the Maurer-Cartan equation $\delta a+a\cdot a=0.$ More explicitly, for $k\geq 0$ and all multi-index $(i_{0}\ldots i_{k})$ we have $\sum_{j=1}^{k-1}(-1)^{j}a^{k-1,2-k}_{i_{0}\ldots\widehat{i_{j}}\ldots i_{k}}+% \sum_{l=0}^{k}(-1)^{(1-l)(k-l)}a^{l,1-l}_{i_{0}\ldots i_{l}}a^{k-l,1-k+l}_{i_{% l}\ldots i_{k}}=0.$ Moreover we impose the following non-degenerate condition: for each $i$, the chain map $a^{1,0}_{ii}:(E^{\bullet}_{i},a^{0,1}_{i})\to(E^{\bullet}_{i},a^{0,1}_{i})% \text{ is invertible up to homotopy.}$ The twisted complexes on $(X,\mathcal{R},\{U_{i}\})$ form a dg-category: the objects are the twisted complexes $\mathcal{E}=(E^{\bullet}_{i},a)$ and the morphism from $\mathcal{E}=(E^{\bullet}_{i},a)$ to $\mathcal{F}=(F^{\bullet}_{i},b)$ are $C^{\bullet}(\mathcal{U},\text{Hom}^{\bullet}(E,F))$ with the total degree. Moreover, the differential on a morphism $\phi$ is given by $d\phi=\delta\phi+b\cdot\phi-(-1)^{|\phi|}\phi\cdot a.$ We denote the dg-category of twisted complexes on $(X,\mathcal{R},\{U_{i}\})$ by Tw$(X,\mathcal{R},\{U_{i}\})$. If there is no risk of confusion we can simply denote it by Tw$(X)$.
Definition 3.1. The differential on $\phi\in\prod_{p+q=k}C^{p}(\mathcal{B},\text{Hom}^{q}(\mathcal{E},\mathcal{F}))$ is defined by the following formula. $(\text{D}\phi)^{p,q+1}_{i_{0}\ldots i_{p}}:=(-1)^{p}d_{\mathcal{B}}(\phi^{p,q}% _{i_{0}\ldots i_{p}})+\sum_{j=1}^{p-1}(-1)^{j}\phi^{p-1,q+1}_{i_{0}\ldots% \widehat{i_{j}}\ldots i_{p}}.$ It is easy to check that $\text{D}\circ\text{D}=0$. Moreover, we define the shuffled multiplication of two elements $\phi\in C^{p}(\mathcal{B},\text{Hom}^{q}(\mathcal{F},\mathcal{G}))$ and $\eta\in C^{r}(\mathcal{B},\text{Hom}^{s}(\mathcal{E},\mathcal{F}))$ as $(\phi\cdot\eta)^{p+r,q+s}_{i_{0}\ldots i_{p+r}}=(-1)^{qr}\phi^{p,q}_{i_{0}% \ldots i_{p}}\circ\eta^{r,s}_{i_{p}\ldots i_{p+r}}$ From now on the notation "$\cdot$" will be preserved for the shuffled multiplication. We can also check that the differential and multiplication satisfy the Leibniz rule, i.e. $\text{D}(\phi\cdot\eta)=(\text{D}\phi)\cdot\eta+(-1)^{|\phi|}\phi\cdot(\text{D% }\eta).$
Definition 2.7 ([Hir03] Definition 16.3.1). If $\mathcal{Y}=Y_{\bullet}$ is a simplicial object in $\mathcal{M}$ and $K$ is a simplicial set, then $\mathcal{Y}^{K}$ is defined to be the object of $\mathcal{M}$ that is the limit of the $(\Delta^{op}K)$-diagram in $\mathcal{M}$ that takes the object $\Delta[n]\to K$ of $(\Delta^{op}K)=(\Delta\downarrow K)^{op}$ to $Y_{n}$ and takes the commutative triangle $\xymatrix{\Delta[n]\ar[rr]^{\alpha}\ar[dr]&&\Delta[k]\ar[dl]\\ &K}$ to the map $\alpha^{*}:Y_{k}\to Y_{n}$. It is clear that the construction is functorial in both $\mathcal{Y}$ and $K$.
Definition 4.1. A dg pseudo-functor $\mathcal{F}$ on $\mathcal{S}$ consists of the following data 1. For each object $X$ of $\mathcal{S}$ a dg-category $\mathcal{F}(X)$; 2. For each morphism $f:X\to Y$ a dg-functor $f^{*}:\mathcal{F}(Y)\to\mathcal{F}(X)$; 3. For each object $X$ of $\mathcal{S}$ a (strict) isomorphism $\epsilon_{X}:id_{X}^{*}\overset{\cong}{\to}id_{\mathcal{F}(X)}$ in the dg-category $\text{Fun}(\mathcal{F}(X),\mathcal{F}(X))$; 4. For each pair of arrows $X\overset{f}{\to}Y\overset{g}{\to}Z$ a (strict) isomorphism $\alpha_{f,g}:f^{*}g^{*}\overset{\cong}{\to}(gf)^{*}\text{ in }\text{Fun}(% \mathcal{F}(Z),\mathcal{F}(X)).$ These data are required to satisfy the following conditions. a. If $f:X\to Y$ is a morphism in $\mathcal{S}$ and $\eta$ is an object in $\mathcal{F}(Y)$, then we have $\alpha_{id_{X},f}(\eta)=\epsilon_{X}(f^{*}\eta)\text{ in }\text{Hom}_{\mathcal% {F}(X)}(id_{X}^{*}f^{*}\eta,f^{*}\eta)$ and $\alpha_{f,id_{Y}}(\eta)=f^{*}\epsilon_{Y}(\eta)\text{ in }\text{Hom}_{\mathcal% {F}(Y)}(f^{*}id_{Y}^{*}\eta,f^{*}\eta).$ b. For any morphisms $X\overset{f}{\to}Y\overset{g}{\to}Z\overset{h}{\to}W$ and an object $\theta$ in $\mathcal{F}(W)$, , the diagram $\begin{CD}f^{*}g^{*}h^{*}\theta @>{\alpha_{f,g}(h^{*}\theta)}>{}>(gf)^{*}h^{*}% \theta\\ @V{}V{f^{*}\alpha_{g,h}(\theta)}V@V{}V{\alpha_{gf,h}(\theta)}V\\ f^{*}(hg)^{*}\theta @>{\alpha_{f,hg}(\theta)}>{}>(hgf)^{*}\theta\end{CD}$ strictly commutes.
Definition 2.4. [[Hir03] Definition 15.3.2] Let $\mathcal{C}$ be a Reedy category and $\mathcal{M}$ a category closed under limits and colimits. Let $f:\mathcal{X}\to\mathcal{Y}$ be a map of $\mathcal{C}$-diagrams. Let $\alpha$ be an object of $\mathcal{C}$. 1. The relative latching map of $f$ at $\alpha$ is the map $\mathcal{X}_{\alpha}\coprod_{L_{\alpha}\mathcal{X}}L_{\alpha}\mathcal{Y}\to% \mathcal{Y}_{\alpha}$. 2. The relative matching map of $f$ at $\alpha$ is the map $\mathcal{X}_{\alpha}\to\mathcal{Y}_{\alpha}\times_{M_{\alpha}\mathcal{Y}}M_{% \alpha}\mathcal{X}$.
Definition 3.2. [See [Hol14] Definition 3.2] Let $\mathcal{B}$ be a (small) dg-category. Then $\Delta_{n}(\mathcal{B})$ is a dg-category with objects given by pairs $(\mathcal{E},\phi)$ where $\mathcal{E}$ is a collection $E_{0},E_{1},\ldots,E_{n}$ of objects in $\mathcal{B}$ and $\phi$ is a collection $\phi^{k,1-k}_{I}\in\text{Hom}^{1-k}_{\mathcal{B}}(E_{i_{k}},E_{i_{0}})$ for all multi-indices $I=(i_{0},\ldots,i_{k})$, $0\leq i_{0}\leq\ldots\leq i_{k}\leq n$. These pairs must satisfy the Maurer-Cartan equation $\text{D}\phi+\phi\cdot\phi=0.$ More precisely, for any $I=\{i_{0}\ldots i_{k}\}$, we have $(-1)^{k}d_{\mathcal{B}}(\phi^{k,1-k}_{i_{0}\ldots i_{k}})+\sum_{j=1}^{k-1}(-1)% ^{j}\phi^{k-1,2-k}_{i_{0}\ldots\widehat{i_{j}}\ldots i_{k}}+\sum_{j=1}^{k-1}(-% 1)^{(1-j)(k-j)}\phi^{j,1-j}_{i_{0}\ldots i_{j}}\circ\phi^{k-j,1-k+j}_{i_{j}% \ldots i_{k}}=0.$ Moreover we require that all $\phi^{1,0}_{ij}\in\text{Hom}^{0}_{\mathcal{B}}(E_{i},E_{j})$ (including $\phi^{1,0}_{ii}$) are homotopically invertible and in particular $\phi^{1,0}_{ii}=id$. On the other hand we require all $\phi^{k,1-k}_{i_{0}\ldots i_{k}}$’s equal to $0$ if they have repeated indices and $k\geq 2$. Morphisms from $(\mathcal{E},\phi)$ to $(\mathcal{F},\psi)$ are given as follows. $\text{Hom}^{m}_{\Delta_{n}(\mathcal{B})}((\mathcal{E},\phi),(\mathcal{F},\psi)% )=\prod_{k}\prod_{\begin{subarray}{c}(i_{0}\ldots i_{k})\\ 0\leq i_{0}\leq\ldots\leq i_{k}\leq n\end{subarray}}\text{Hom}^{m-k}_{\mathcal% {B}}(E_{i_{k}},F_{i_{0}})$ and the differential $d$ on a degree $m$ morphism $\theta$ is given by $d\theta=\text{D}\theta+\psi\cdot\theta-(-1)^{m}\theta\cdot\phi.$
Definition 4.4. A twisted perfect complex $\mathcal{E}=(E^{\bullet}_{i},a)$ is the same as twisted complex in Definition 4.3 except that each $E^{\bullet}_{i}$ is a strictly perfect complex on $U_{i}$. The twisted perfect complexes also form a dg-category and we denote it by $\text{Tw}_{\text{perf}}(X,\mathcal{R},\{U_{i}\})$ or simply $\text{Tw}_{\text{perf}}(X)$. Obviously $\text{Tw}_{\text{perf}}(X)$ is a full dg-subcategory of Tw$(X)$.
Definition 2.8 ([Hir03] Definition 19.1.5). Let $\mathcal{M}$ be a framed model category and $\mathcal{C}$ be a small category. If $\mathcal{X}$ is a $\mathcal{C}$-diagram in $\mathcal{M}$, then the homotopy limit $\operatorname*{holim}(\mathcal{X})$ of $\mathcal{X}$ is defined to be the equalizer of the maps $\prod_{c\in Ob(\mathcal{C})}(\widehat{X_{c}})^{N(\mathcal{C}\downarrow c)}% \underset{\psi}{\overset{\phi}{\rightrightarrows}}\prod_{(\sigma:c\to c^{% \prime})\in Mor(\mathcal{C})}(\widehat{X_{c^{\prime}}})^{N(\mathcal{C}% \downarrow c)}$ where $\widehat{X_{c}}$ is the natural simplicial frame on $X_{c}$ and $N(\mathcal{C}\downarrow c)$ is the nerve of the over category $\mathcal{C}\downarrow c$. The projection of the map $\phi$ on the factor $\sigma:c\to c^{\prime}$ is the composition of a natural projection from the product with the map $\sigma_{*}^{id_{N(\mathcal{C}\downarrow c)}}:(\widehat{X_{c}})^{N(\mathcal{C}% \downarrow c)}\to(\widehat{X_{c^{\prime}}})^{N(\mathcal{C}\downarrow c)}$ and the projection of the map $\psi$ on the factor $\sigma:c\to c^{\prime}$ is the composition of a natural projection from the product with the map $(id_{\widehat{X_{c^{\prime}}}})^{N(\sigma_{*})}:(\widehat{X_{c^{\prime}}})^{N(% \mathcal{C}\downarrow c^{\prime})}\to(\widehat{X_{c^{\prime}}})^{N(\mathcal{C}% \downarrow c)}$ where $\sigma_{*}^{id_{N(\mathcal{C}\downarrow c)}}$ and $(id_{\widehat{X_{c^{\prime}}}})^{N(\sigma_{*})}$ come from the functoriality in Definition 2.7.
Definition 2.2. [[Hir03] Definition 15.2.3] Let $\mathcal{C}$ be a Reedy category and let $\alpha$ be an object of $\mathcal{C}$. 1. The latching category $\partial(\overrightarrow{\mathcal{C}}\downarrow\alpha)$ of $\mathcal{C}$ at $\alpha$ is the full subcategory of the overcategory $(\overrightarrow{\mathcal{C}}\downarrow\alpha)$ containing all the objects except the identity map of $\alpha$. 2. The matching category $\partial(\alpha\downarrow\overleftarrow{\mathcal{C}})$ of $\mathcal{C}$ at $\alpha$ is the full subcategory of the undercategory $(\alpha\downarrow\overleftarrow{\mathcal{C}})$ containing all the objects except the identity map of $\alpha$.
Definition 2.6 ([Hir03] Definition 16.6.1). Let $X$ be an object in a model category $\mathcal{M}$. A simplicial frame on $X$ is a simplicial object $\widehat{X}$ in $\mathcal{M}$ together with a weak equivalence $cs_{*}X\to\widehat{X}$ in the Reedy model category structure on $\mathcal{M}^{\Delta^{op}}$ such that 1. the induced map $X\to\widehat{X}_{0}$ is an isomorphism; 2. if $X$ is a fibrant object of $\mathcal{M}$, then $\widehat{X}$ is a fibrant object in the Reedy model category $\mathcal{M}^{\Delta^{op}}$.
Definition 2.1. [[Hir03] Definition 15.1.2] A Reedy category is a (small) category $\mathcal{C}$ equipped with two subcategories $\overrightarrow{\mathcal{C}}$ (the direct category) and $\overleftarrow{\mathcal{C}}$ (the inverse category), both of which contain all the objects of $\mathcal{C}$, in which every object can be assigned a nonnegative integer (called the degree) such that 1. Every non-identity morphism of $\overrightarrow{\mathcal{C}}$ raises degree. 2. Every non-identity morphism of $\overleftarrow{\mathcal{C}}$ lowers degree. 3. Every morphism $g$ in $\mathcal{C}$ has a unique factorization $g=\overrightarrow{g}\overleftarrow{g}$ where $\overrightarrow{g}$ is in $\overrightarrow{\mathcal{C}}$ and $\overleftarrow{g}$ is in $\overleftarrow{\mathcal{C}}$.
Definition 2.3. [[Hir03] Definition 15.2.5] Let $\mathcal{C}$ be a Reedy category and $\mathcal{M}$ a category closed under limits and colimits. Let $\mathcal{X}$ be a $\mathcal{C}$-diagram in $\mathcal{M}$, i.e. $\mathcal{X}$ is a functor $\mathcal{C}\to\mathcal{M}$. Let $\alpha$ be an object of $\mathcal{C}$. 1. The latching object of $\mathcal{X}$ at $\alpha$ is $L_{\alpha}\mathcal{X}=\operatorname*{colim}_{\partial(\overrightarrow{\mathcal% {C}}\downarrow\alpha)}\mathcal{X}$ and the latching map of $\mathcal{X}$ at $\alpha$ is the natural map $L_{\alpha}\mathcal{X}\to\mathcal{X}_{\alpha}$. 2. The matching object of $\mathcal{X}$ at $\alpha$ is $M_{\alpha}\mathcal{X}=\lim_{\partial(\alpha\downarrow\overleftarrow{\mathcal{C% }})}\mathcal{X}$ and the matching map of $\mathcal{X}$ at $\alpha$ is the natural map $\mathcal{X}_{\alpha}\to M_{\alpha}\mathcal{X}$.
Definition 3.4. For any $k\geq 1$ we call the morphism $\phi^{k,1-k}_{01\ldots k}$ in $\mathcal{B}^{k}$ the $k$th standard morphism.
Definition 1.5. A coalgebra in a monoidal r-category is transitive if it satisfies the equivalent conditions of Proposition 1.4
Definition 2.11. The polyad $\widetilde{T}$ is called the lift of the polyad $T$.
Definition 2.15. Given a comonoidal polyad $T=(D,\mathcal{C},T,\mu,\eta)$, the left (resp. right) fusion operator of $T$, denoted $H^{l}$ (resp $H^{r}$) is a collection of natural tranformations indexed by composable pairs $(a,b)\in D_{2}$, $k\xrightarrow{b}j\xrightarrow{a}i$, defined as follows: $H^{l}_{a,b}(X,Y)=(T_{a}X\otimes{\mu_{a,b}}_{Y}){T_{a}^{2}}(X,T_{b}Y):T_{a}(X% \otimes T_{b}Y)\to T_{a}X\otimes T_{ab}Y$ and $H^{r}_{a,b}(Y,X)=({\mu_{a,b}}_{Y}\otimes T_{a}X){T_{a}^{2}}(T_{b}Y,X):T_{a}(T_% {b}Y\otimes X)\to T_{ab}Y\otimes T_{a}X$ for $X$, $Y$ objects of $\mathcal{C}_{k}$ and $\mathcal{C}_{j}$ respectively.
Definition 8.2. The Hopf polyad $Z$ so constructed is called the centralizer of the $G$-graded tensor category $\mathcal{C}$.
Definition 2.13. A comonoidal polyad $T$ is said to preserve tensor products (resp. tensor units) if for each $a\in D_{1}$, the comonoidal functor $T_{a}$ preserves tensor products (resp. tensor units). A strong comonoidal polyad is a comonoidal polyad which preserves tensor products and tensor units.
Definition 1.2. An r-category is a category admitting coequalizers of reflexive pairs. A functor $F$ between r-categories is r-exact if it preserves coequalizers of reflexive pairs.
Definition 2.17. A left (resp. right) Hopf polyad is a comonoidal polyad whose left (resp. right) fusion operator is an isomorphism. A Hopf polyad is a comonoidal polyad both of whose fusion operators are invertible.
Definition 7.3 ([M01, Section 4]). The rigid C${}^{*}$-tensor categories $\mathcal{C}_{1}$ and $\mathcal{C}_{2}$ are called Morita equivalent if there exist nonzero $C^{*}$-categories $\mathcal{C}_{12}$ and $\mathcal{C}_{21}$ with finite dimensional morphism spaces and with tensor functors $\mathcal{C}_{1}\otimes\mathcal{C}_{12}\rightarrow\mathcal{C}_{12}$, $\mathcal{C}_{12}\otimes\mathcal{C}_{21}\rightarrow\mathcal{C}_{11}$, etc., and with a duality functor $\mathcal{C}_{12}\rightarrow\mathcal{C}_{21}$ satisfying exactly the same properties as a rigid C${}^{*}$-tensor category.
Definition 8.6. Let $(\mathcal{M},\tau)$ be a von Neumann algebra with a normal semifinite faithful trace $\tau$ and let $\mathcal{A}\subset\mathcal{M}$ be a dense $*$-subalgebra contained in the domain of $\tau$. For every $V\in M_{m,n}(\mathbb{C})\otimes\mathcal{A}$, viewed as an operator from $L^{2}(\mathcal{M})^{\oplus n}$ to $L^{2}(\mathcal{M})^{\oplus m}$ given by left multiplication, we define $\beta^{\text{\rm\tiny(2)}}(V)=\dim_{\mathcal{M}}\bigl{(}\operatorname{Ker}V% \cap(\operatorname{Ker}V\cap\mathcal{A}^{\oplus n})^{\perp}\bigr{)}\;.$
Definition 3.3. Let $S$ be a II${}_{1}$ factor and $T\subset S$ an irreducible quasi-regular subfactor. We say that the inclusion $T\subset S$ is unimodular when all $T$-subbimodules of $L^{2}(S)$ have equal left and right dimension.
Definition 9.14. Let $S$ be a II${}_{1}$ factor and $T\subset S$ a quasi-regular irreducible subfactor. 1. [P86, Definition 4.1.3] $S$ has property (T) relative to $T$ if the following holds: whenever $\varphi_{i}:S\rightarrow S$ is a net of normal $T$-bimodular completely positive maps satisfying $\lim_{i}\|\varphi_{i}(x)-x\|_{2}=0$ for every $x\in S$, then $\lim_{i}\bigl{(}\sup_{x,\|x\|\leq 1}\|\varphi_{i}(x)-x\|_{2}\bigr{)}=0$. 2. [P01, Definition 2.1] $S$ has the Haagerup property relative to $T$ if there exists a net of normal $T$-bimodular completely positive maps $\varphi_{i}:S\rightarrow S$ such that $\lim_{i}\|\varphi_{i}(x)-x\|_{2}=0$ for every $x\in S$ and such that for every $i$, the map $\varphi_{i}:S\rightarrow S$ belongs to the compact ideal space $\mathcal{J}(\langle S,e_{T}\rangle)$ (see [P01, Section 1.3.3] and Lemma 9.20 below).
Definition 4.3. Let $T\subset\mathscr{S}\subset\operatorname{QN}_{S}(T)$ be an intermediate $*$-algebra. We define the $L^{2}$-cohomology of $T\subset\mathscr{S}$ as $H^{n}(T\subset\mathscr{S},\mathcal{H}_{\text{\rm reg}})$. Note that $H^{n}(T\subset\mathscr{S},\mathcal{H}_{\text{\rm reg}})$ canonically is an $\mathcal{M}(T\subset S)$-module. In the unimodular case, i.e. when $\mu$ is a trace on $\mathcal{M}(T\subset S)$, we define $\beta_{n}^{\text{\rm\tiny(2)}}(T\subset\mathscr{S}):=\dim_{\mathcal{M}(T% \subset S)}H^{n}(T\subset\mathscr{S},\mathcal{H}_{\text{\rm reg}})\;.$ Here, we use Lück’s dimension function for arbitrary modules over a von Neumann algebra with a semifinite trace, see [L02, Section 6.1] and [KPV13, Section A.4].
Definition 8.1. Let $S$ be a II${}_{1}$ factor with irreducible quasi-regular subfactor $T\subset S$. The inclusion $T\subset S$ is called amenable if there exists a net of unital, trace preserving, completely positive, $T$-bimodular maps $\varphi_{i}:S\rightarrow S$ such that $\lim_{i}\|\varphi_{i}(x)-x\|_{2}=0$ for all $x\in S$ and such that $\varphi_{i}$ has finite rank for every fixed $i$, in the sense that the closure of $\varphi_{i}(S)$ is a finite index $T$-subbimodule of $L^{2}(S)$.
Definition 2.1. Let $(T,\tau)$ be a von Neumann algebra with a normal faithful tracial state. We say that a bifinite Hilbert $T$-bimodule $\mathcal{H}$ with right support $z_{1}$ and left support $z_{2}$ is irreducible if the space $\operatorname{End}_{T-T}(\mathcal{H})$ of $T$-bimodular bounded operators equals $\mathcal{Z}(T)z_{1}$ represented by its right action on $\mathcal{H}$, and also equals $\mathcal{Z}(T)z_{2}$ represented by its left action on $\mathcal{H}$.
