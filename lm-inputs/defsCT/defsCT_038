Definition 2.21. A semi-Mackey functor $M$ on $\mathbb{S}$ is called deflative if for any stab-surjective 1-cell $\alpha\colon\frac{X}{G}\to\frac{Y}{H}$ in $\mathbb{S}$, the equality $M_{\ast}(\alpha)\circ M^{\ast}(\alpha)=\mathrm{id}_{M(\frac{Y}{H})}$ is satisfied. An $R$-linear Mackey functor is called deflative if it is deflative as a semi-Mackey functor. The full subcategory of deflative semi-Mackey functors is denoted by $\mathit{SMack}_{\mathrm{dfl}}(\mathbb{S})\subseteq\mathit{SMack}(\mathbb{S})$. Similarly, the full subcategory of deflative $R$-linear Mackey functors is denoted by $\mathit{Mack}_{\mathrm{dfl}}^{R}(\mathbb{S})\subseteq\mathit{Mack}^{R}(\mathbb% {S})$.
Definition 7.1. Let $P\colon\underline{\mathrm{FinGrp}}\to R\mathit{Mod}$ be any object in $\mathit{Res}^{R}(\mathscr{C})$. For each finite group $G$, define $R$-modules $S_{P}(G)$, $N_{P}(G)$ and $P_{\oplus}(G)$ as follows. (1) Define $S_{P}(G)$ by $S_{P}(G)=\bigoplus_{K\overset{f}{\to}G}P(K),$ where the direct sum runs over all group homomorphisms $K\overset{f}{\to}G$. For $K\overset{f}{\to}G$ and $\kappa\in P(K)$, the corresponding element in $S_{P}(G)$ is denoted by $(K\overset{f}{\to}G,\kappa)$ or simply by $(f,\kappa)$ (2) $N_{P}(G)\subseteq S_{P}(G)$ is the submodule generated by the following subset of $S_{P}(G)$. $\Set{(f,\kappa)-(f^{\prime},P(\pi)(\kappa))}{\ \xy(-7,6)*+{K^{\prime}}="0";(7,% 6)*+{K}="2";(-7,-6)*+{G}="4";(7,-6)*+{G}="6";{\ar^{\pi}"0";"2"};{\ar_{f^{% \prime}}"0";"4"};{\ar^{f}"2";"6"};{\ar_{\sigma_{g}}"4";"6"};{\ar@{}|% \circlearrowright"0";"6"};\endxy\ \text{in}\ \mathrm{FinGrp},\begin{array}[]{l% }\pi\ \text{is surjective},\\ g\in G,\ \kappa\in P(K).\end{array}}$ (3) $P_{\oplus}(G)$ is the quotient of $S_{P}(G)$ by $N_{P}(G)$: $P_{\oplus}(G)=S_{P}(G)/N_{P}(G).$ We denote the equivalence class of $(K\overset{f}{\to}G,\kappa)$ in $P_{\oplus}(G)$ by $[K\overset{f}{\to}G,\kappa]$, or simply by $[f,\kappa]$.
Definition 2.14. Let $\frac{X}{G}$ be any 0-cell in $\mathbb{S}$. Then a 2-category $\mathbb{S}/\!_{\frac{X}{G}}$ is defined as follows. (0) A 0-cell in $\mathbb{S}/\!_{\frac{X}{G}}$ is a 1-cell $(\frac{A}{K}\overset{\mathfrak{a}}{\to}\frac{X}{G})$ in $\mathbb{S}$, from some $\frac{A}{K}\in\mathbb{S}^{0}$. (1) A 1-cell in $\mathbb{S}/\!_{\frac{X}{G}}$ from $(\frac{A}{K}\overset{\mathfrak{a}}{\to}\frac{X}{G})$ to $(\frac{B}{L}\overset{\mathfrak{b}}{\to}\frac{X}{G})$ is a pair $(\varphi,\mu)$ of a 1-cell $\varphi$ and a 2-cell $\mu$ in $\mathbb{S}$ as in the following diagram. $\xy(-10,6)*+{\frac{A}{K}}="0";(10,6)*+{\frac{B}{L}}="2";(0,-8)*+{\frac{X}{G}}=% "4";{\ar^{\varphi}"0";"2"};{\ar_{(}0.4){\alpha}"0";"4"};{\ar^{(}0.38){\beta}"2% ";"4"};{\ar@{=>}^{\mu}(2,3);(-2,0)};\endxy$ (2) If $(\varphi,\mu)\colon(\frac{A}{K}\overset{\mathfrak{a}}{\to}\frac{X}{G})\to(% \frac{B}{L}\overset{\mathfrak{b}}{\to}\frac{X}{G})$ and $(\varphi^{\prime},\mu^{\prime})\colon(\frac{A}{K}\overset{\mathfrak{a}}{\to}% \frac{X}{G})\to(\frac{B}{L}\overset{\mathfrak{b}}{\to}\frac{X}{G})$ are 1-cells in $\mathbb{S}/\!_{\frac{X}{G}}$, then a 2-cell $\varepsilon\colon(\varphi,\mu)\Rightarrow(\varphi^{\prime},\mu^{\prime})$ in $\mathbb{S}/\!_{\frac{X}{G}}$ is a 2-cell $\varepsilon\colon\varphi\Rightarrow\varphi^{\prime}$ in $\mathbb{S}$, which makes the following diagram commutative. $\xy(-10,6)*+{\beta\circ\varphi}="0";(10,6)*+{\beta\circ\varphi}="2";(0,-8)*+{% \alpha}="4";(0,10)*+{}="5";{\ar@{=>}^{\beta\circ\varepsilon}"0";"2"};{\ar@{=>}% _{(}0.4){\mu}"0";"4"};{\ar@{=>}^{(}0.4){\mu^{\prime}}"2";"4"};{\ar@{}|% \circlearrowright"4";"5"};\endxy$ Composition of 1-cells $(\frac{A}{K}\overset{\mathfrak{a}}{\to}\frac{X}{G})\overset{(\varphi,\mu)}{% \longrightarrow}(\frac{B}{L}\overset{\mathfrak{b}}{\to}\frac{X}{G})\overset{(% \psi,\nu)}{\longrightarrow}(\frac{C}{H}\overset{\mathfrak{c}}{\to}\frac{X}{G})$ is defined to be $(\psi\circ\varphi,\mu\cdot(\nu\circ\varphi))\colon(\frac{A}{K}\overset{% \mathfrak{a}}{\to}\frac{X}{G})\to(\frac{C}{H}\overset{\mathfrak{c}}{\to}\frac{% X}{G}).$ Vertical composition of 2-cells $\xy(-20,0)*+{(\frac{A}{K}\overset{\mathfrak{a}}{\to}\frac{X}{G})}="0";(20,0)*+% {(\frac{B}{L}\overset{\mathfrak{b}}{\to}\frac{X}{G})}="2";{\ar@/^{2}.0pc/^{(% \varphi,\mu)}"0";"2"};{\ar|*+{{}_{(\varphi^{\prime},\mu^{\prime})}}"0";"2"};{% \ar@/_{2}.0pc/_{(\varphi^{\prime\prime},\mu^{\prime\prime})}"0";"2"};{\ar@{=>}% ^{\varepsilon}(0,6);(0,3)};{\ar@{=>}^{\varepsilon^{\prime}}(0,-3);(0,-6)};\endxy$ is defined to be $\varepsilon^{\prime}\cdot\varepsilon$, using the vertical composition in $\mathbb{S}$. Horizontal composition is also defined by using the horizontal composition in $\mathbb{S}$.
Definition 4.1. Define category $\mathit{Add}^{R}(\mathscr{C})$ (resp. $\mathit{Sadd}(\mathscr{C})$) as follows. - An object in $\mathit{Add}^{R}(\mathscr{C})$ (resp. $\mathit{Sadd}(\mathscr{C})$) is a contravariant functor $F\colon\mathscr{C}\to R\mathit{Mod}\quad(\text{resp.}\ \ E\colon\mathscr{C}\to% \mathit{Set})$ which sends coproducts in $\mathscr{C}$ to products in $R\mathit{Mod}$ (resp. $\mathit{Set}$). - A morphism is a natural transformation.
Definition 2.5. For any $\frac{X}{G}$ and $\frac{Y}{H}$ in $\mathbb{S}^{0}$, their 2-coproduct $(\frac{X}{G}\amalg\frac{Y}{H},\upsilon_{\frac{X}{G}},\upsilon_{\frac{Y}{H}})$ is defined to be a triplet of $\frac{X}{G}\amalg\frac{Y}{H}\in\mathbb{S}^{0}$ and $\upsilon_{\frac{X}{G}}\in\mathbb{S}^{1}(\frac{X}{G},\frac{X}{G}\amalg\frac{Y}{% H})$, $\upsilon_{\frac{Y}{H}}\in\mathbb{S}^{1}(\frac{Y}{H},\frac{X}{G}\amalg\frac{Y}{% H})$, satisfying the following conditions. (i) For any $\frac{W}{L}\in\mathbb{S}^{0}$ and $\alpha\in\mathbb{S}^{1}(\frac{X}{G},\frac{W}{L}),\,\beta\in\mathbb{S}^{1}(% \frac{Y}{H},\frac{W}{L})$, there exist $\alpha\cup\beta\in\mathbb{S}^{1}(\frac{X}{G}\amalg\frac{Y}{H},\frac{W}{L})$ and 2-cells $\xi,\eta$ as in the following diagram. $\xy(0,-6)*+{\frac{W}{L}}="0";(-18,10)*+{\frac{X}{G}}="2";(0,10)*+{\frac{X}{G}% \amalg\frac{Y}{H}}="4";(18,10)*+{\frac{Y}{H}}="6";{\ar_{\alpha}"2";"0"};{\ar|*% +{{}_{\alpha\cup\beta}}"4";"0"};{\ar^{\beta}"6";"0"};{\ar^{(}0.4){\upsilon_{% \frac{X}{G}}}"2";"4"};{\ar_{(}0.4){\upsilon_{\frac{Y}{H}}}"6";"4"};{\ar@{=>}_{% \xi}(-4,6);(-7.5,3)};{\ar@{=>}^{\eta}(4,6);(7.5,3)};\endxy$ (ii) For any triplets $(\gamma,\xi^{\prime},\eta^{\prime})$ as in (i), there exists a unique 2-cell $\zeta\colon\alpha\cup\beta\Rightarrow\gamma$ which satisfies $\xi^{\prime}\cdot(\zeta\circ\upsilon_{\frac{X}{G}})=\xi$ and $\eta\cdot^{\prime}(\zeta\circ\upsilon_{\frac{Y}{H}})=\eta$. By its universality, the 2-coproduct is determined up to adjoint equivalences, if it exists.
Definition 2.0.4 Let $X$ be an algebraic variety and let $\pi_{*}:\mathscr{T}\rightarrow\mathrm{D}(X)$ be a categorical resolution of $X$. The category $\mathscr{T}$ is said to be a weakly crepant resolution of $Y$ if $\pi^{*}$ is a also a right adjoint to $\pi_{*}$ when restricted to $\mathrm{D^{perf}}(X)$. Assume finally that $\mathscr{T}$ has a module structure over $X$. Then $\mathscr{T}$ is said to be a strongly crepant resolution of $X$ if the identity is a relative Serre functor for $\mathscr{T}^{c}$ with respect to $\mathrm{D^{b}}(X)$.
Definition 2.0.1 Let $X$ be an algebraic variety. A categorical resolution of $X$ is a smooth cocomplete compactly generated category $\mathscr{T}$ with a pair of adjoint functors: $\begin{split}&\displaystyle\pi_{*}:\mathscr{T}\rightarrow\mathrm{D}(X)\\ &\displaystyle\pi^{*}:\mathrm{D}(X)\rightarrow\mathscr{T},\end{split}$ such that • $\pi_{*}\pi^{*}\simeq id$, • $\pi^{*}$ and $\pi_{*}$ commute with arbitrary direct sums, • $\pi_{*}(\mathscr{T}^{c})\subset\mathrm{D^{b}}(X)$, where $\mathscr{T}^{c}$ is the subcategory of compact objects in $\mathscr{T}$.
Definition We first define a functor $T_{n}\colon\!{\mathcal{W}}{\operatorname{Top}}\to{\mathcal{W}}{\operatorname{% Top}}$ and a natural transformation $t_{n}\colon\!\operatorname{Id}\to T_{n}$. Let $F\in{\mathcal{W}}{\operatorname{Top}}$ then $T_{n}F$ is given below. $(T_{n}F)(X)=\mathrm{Nat}(\underset{S\in{\mathcal{P}}_{0}(\underline{n+1})}{% \operatorname{hocolim}}{\mathcal{W}}(S\Asterisk X,-),F)=\underset{S\in{% \mathcal{P}}_{0}(\underline{n+1})}{\operatorname{holim}}F(S\Asterisk X)$ The inclusion of the empty set as the initial object of ${\mathcal{P}}_{0}(\underline{n+1})$ and that $\emptyset\Asterisk X\cong X$ gives a natural transformation $t_{n,F}$ from $F(-)\cong F(\emptyset\Asterisk-)$ to the homotopy limit $T_{n}F$. Furthermore, we define $P_{n}F:=\operatorname{hocolim}\big{(}F\overset{t_{n,F}}{\longrightarrow}T_{n}F% \overset{t_{n,T_{n}F}}{\longrightarrow}T_{n}^{2}F\overset{t_{n,T_{n}^{2}F}}{% \longrightarrow}T_{n}^{3}F\longrightarrow\dots\big{)}$ ${}_{\blacksquare}$
Definition [13, Definition 11.1.1] Let $\mathcal{M}$ be a model category, and let the following be a commutative square in $\mathcal{M}$: $\xymatrix{A\ar[r]^{f}\ar[d]_{i}&B\ar[d]^{j}\\ C\ar[r]_{g}&D\\ }.$ $\mathcal{M}$ is called left proper if, whenever $f$ is a weak equivalence, $i$ a cofibration, and the square is a pushout, then $g$ is also a weak equivalence. $\mathcal{M}$ is called right proper if, whenever $g$ is a weak equivalence, $j$ a fibration, and the square is a pullback, then $f$ is also a weak equivalence. $\mathcal{M}$ is called proper if it is both left and right proper. ${}_{\blacksquare}$
Definition Consider the following collection of maps, where $\phi_{\underline{X},n}$ is defined via the projections which send those factors in $S$ to the basepoint. $\begin{array}[]{c}\Phi_{n}=\{\phi_{\underline{X},n}\colon\!\underset{S\in{% \mathcal{P}}_{0}(\underline{n})}{\operatorname{colim}}{\mathcal{W}}({\bigvee_{% l\in\underline{n}-S}X_{l}},-)\longrightarrow{\mathcal{W}}({\bigvee_{l=1}^{n}X_% {l}},-)\ |\ \underline{X}=(X_{1},\dots,X_{n}),X_{l}\in\operatorname{sk}{% \mathcal{W}}\}\\ \end{array}$ We then also define $\Phi_{\infty}=\cup_{n\geqslant 1}\Phi_{n}$. ${}_{\blacksquare}$
Definition A map $f\colon\!F\to G$ in $\Sigma_{n}\circlearrowleft{\mathcal{W}}{\operatorname{Top}}$ is said to be a $\pi_{*}$-isomorphism if $f$ induces a $\pi_{*}$-isomorphism on the underlying spectra of $F$ and $G$. ${}_{\blacksquare}$
Definition [13, Definition 13.2.1] A cofibrantly generated model category is a model category ${\mathcal{M}}$ with sets of maps $I$ and $J$ such that $I$ and $J$ support the small object argument (see [19, Definitions 15.1.1 and 15.1.7.]) and 1. a map is a trivial fibration if and only if it has the right lifting property with respect to every element of $I$, and 2. a map is a fibration if and only if it has the right lifting property with respect to every element of $J$. ${}_{\blacksquare}$
Definition An $n$–cube in ${\mathcal{W}}$ (or ${\operatorname{Top}}$) is a functor $\mathcal{X}$ from ${\mathcal{P}}(\underline{n})$ to ${\mathcal{W}}$ (resp. ${\operatorname{Top}}$). An $n$–cube is said to be strongly cocartesian if all of its two-dimensional faces are homotopy pushout squares. An $n$–cube is said to be cartesian if the map $\mathcal{X}(\emptyset)\longrightarrow\operatorname{holim}_{S\in{\mathcal{P}}_{% 0}(\underline{n})}\mathcal{X}(S)$ induced by the maps $\mathcal{X}(\emptyset)\to\mathcal{X}(S)$ is a weak homotopy equivalence. ${}_{\blacksquare}$
Definition The category $\Sigma_{n}\ltimes({\mathcal{W}}_{n}{\operatorname{Top}})$ is the category of $\Sigma_{n}\circlearrowleft{\operatorname{Top}}$-enriched functors from ${\mathcal{W}}_{n}$ to $\Sigma_{n}\circlearrowleft{\operatorname{Top}}$. ${}_{\blacksquare}$
Definition For $F\in{\mathcal{W}}{\operatorname{Top}}$ and an $n$-tuple of spaces in ${\mathcal{W}}$, $(X_{1},\ldots,X_{n})$, the $n^{th}$–cross effect of $F$ at $(X_{1},\ldots,X_{n})$ is the space $\operatorname{cr}_{n}(F)(X_{1},\ldots,X_{n})=\operatorname{Nat}(\underset{l=1}% {\overset{n}{\bigwedge}}{\mathcal{W}}(X_{l},-),F)$ Pre-composing $\operatorname{cr}_{n}(F)$ with the diagonal map ${\mathcal{W}}(X,Y)\to\underset{i=1}{\overset{n}{\bigwedge}}{\mathcal{W}}(X,Y)$ yields an object of ${\mathcal{W}}{\operatorname{Top}}$ which we call $\operatorname{diff}_{n}(F)$, which in keeping with language of orthogonal calculus, is the $n^{th}$ (unstable) derivative. That is $\operatorname{diff}_{n}(F)(X)=\operatorname{Nat}(\underset{l=1}{\overset{n}{% \bigwedge}}{\mathcal{W}}(X,-),F).$ ${}_{\blacksquare}$
Definition [18, Definition 5.12] For maps $i:A\rightarrow X$ and $p:E\rightarrow B$ in a model category $\mathcal{M}$, let the map below be the map of spaces induced by $\mathcal{M}(i,id)$ and $\mathcal{M}(i,p)$ after passing to the pullback. $\mathcal{M}(i^{\ast},p_{\ast}):\mathcal{M}(X,E)\rightarrow\mathcal{M}(A,E)% \times_{\mathcal{M}(A,B)}\mathcal{M}(X,B)$ A model category $\mathscr{M}$ is topological, provided that $\mathcal{M}(i^{\ast},p_{\ast})$ is a Serre fibration of spaces if $i$ is a cofibration and $p$ is a fibration; it is a weak equivalence if, in addition, either $i$ or $p$ is a weak equivalence. ${}_{\blacksquare}$
Definition The wreath product category $(\Sigma_{n}\wr{\mathcal{W}}^{n})$ has objects the class of $n$-tuples $(X_{1},\ldots,X_{n})$ of objects of ${\mathcal{W}}$. The morphisms from $\uline{X}=(X_{1},\ldots,X_{n})$ to $\uline{Y}=(Y_{1},\ldots,Y_{n})$ are given by $(\Sigma_{n}\wr{\mathcal{W}}^{n})\left(\uline{X},\uline{Y}\right)=\bigvee_{% \sigma\in\Sigma_{n}}\underset{l=1}{\overset{n}{\bigwedge}}{\mathcal{W}}(X_{l},% Y_{\sigma^{-1}(l)})$ with composition defined as for the wreath product of groups. ${}_{\blacksquare}$
Definition An object $F\in{\mathcal{W}}{\operatorname{Top}}$ is said to be $n$-homogeneous if it is $n$-excisive and $P_{n-1}F(X)$ is weakly equivalent to a point for each $X\in{\mathcal{W}}$. For $F\in{\mathcal{W}}{\operatorname{Top}}$, define $D_{n}F\in{\mathcal{W}}{\operatorname{Top}}$ as the homotopy fibre of $P_{n}F\to P_{n-1}F$. Since $P_{n}$ and $P_{n-1}$ commute with finite homotopy limits the functor $D_{n}F$ takes values in $n$-homogeneous functors and hence is called the $n$-homogeneous approximation to $F$. ${}_{\blacksquare}$
Definition We define a ${\operatorname{Top}}$-enriched functor $\mu_{n}:{\mathcal{W}}_{n}\rightarrow{\mathcal{W}}$. It sends the object $X$ to $X^{\wedge n}$ and on morphisms acts as the smash product. It is the adjoint to $n$-fold evaluation: ${\mathcal{W}}_{n}(X,Y)\wedge X^{\wedge n}\longrightarrow Y^{\wedge n}.$ ${}_{\blacksquare}$
Definition Given $f\colon\!A\to B$ a map of based of spaces and $g\colon\!X\to Y$ in ${\mathcal{W}}$, the pushout product of $f$ and $g$, $f\Box g$, is given by $f\Box g\colon\!B\wedge X\bigvee_{A\wedge X}A\wedge Y\to B\wedge Y.$ ${}_{\blacksquare}$
Definition Let ${\mathcal{W}}_{n}$ be the category enriched over topological spaces with $\Sigma_{n}$-action whose objects are those of ${\mathcal{W}}$ and whose spaces of morphisms are given by ${\mathcal{W}}_{n}(X,Y):=\underset{i=1}{\overset{n}{\bigwedge}}{\mathcal{W}}(X,Y)$ with the $\Sigma_{n}$-action which permutes the factors. (Note that this differs from the wreath product of Definition 7.) ${}_{\blacksquare}$
Definition Let $\underline{n}$ denote the set $\{1,\dots,n\}$ and let ${\mathcal{P}}(\underline{n})$ denote the powerset of $\underline{n}$. We define ${\mathcal{P}}_{0}(\underline{n})$ as the set of non–empty subsets of $\underline{n}$. ${}_{\blacksquare}$
Definition The $n$–homotopy groups of an object $F$ of $\Sigma_{n}\ltimes({\mathcal{W}}_{n}{\operatorname{Top}})$ at $A$ are denoted $n\pi_{p}^{A}(F)$ and defined as $n\pi_{p}^{A}(F):=\operatorname{colim}_{k\in\mathbb{Z}}\pi_{p}(\Omega^{nk}F(A% \wedge S^{k}))\cong\operatorname{colim}_{k\in\mathbb{Z}}\pi_{p+nk}(F(A\wedge S% ^{k})).$ The maps of this colimit diagram are induced by adjoints of the structure maps of $F$ $F(A\wedge S^{k})\wedge S^{n}=F(A\wedge S^{k})\wedge{\mathcal{W}}_{n}(S^{0},S^{% 1})\longrightarrow F(A\wedge S^{k+1}).$ A map is said to be an $n\pi_{\ast}^{A}$-isomorphism if it induces isomorphisms on $n\pi_{p}^{A}$ for all $p\in\mathbb{Z}$. ${}_{\blacksquare}$
Definition An object $F\in{\mathcal{W}}{\operatorname{Top}}$ is said to be $n$-excisive if it sends strongly cocartesian $(n+1)$-cubes in ${\mathcal{W}}$ to cartesian $(n+1)$-cubes in ${\operatorname{Top}}$. ${}_{\blacksquare}$
Definition We define the $n^{th}$ (stable) derivative of $F\in{\mathcal{W}}{\operatorname{Top}}$, to be the functor $\operatorname{diff}_{n}(F)$ in $\Sigma_{n}\ltimes({\mathcal{W}}_{n}{\operatorname{Top}})$. The structure map below is induced from the composition of ${\mathcal{W}}_{n}$ and is $\Sigma_{n}$-equivariant ${\mathcal{W}}_{n}(X,Y)\wedge\mathrm{Nat}({\mathcal{W}}_{n}(X,-),F)% \longrightarrow\mathrm{Nat}({\mathcal{W}}_{n}(X,-),F).$ ${}_{\blacksquare}$
Definition Let $F\in\Sigma_{n}\circlearrowleft{\mathcal{W}}{\operatorname{Top}}$ and $A\in{\mathcal{W}}$. We define a spectrum $F[A]$ via $F[A]_{k}:=F(A\wedge S^{k}),$ where we have forgotten the $\Sigma_{n}$-action. The assembly maps provide the structure maps of $F[A]$ as well as maps $F[A]\wedge B\to F[A\wedge B]$. We call $F[S^{0}]$ the underlying spectrum of $F$. ${}_{\blacksquare}$
Proposition-Definition 6.11. Let $\mathscr{S}_{2}:\operatorname{\text{Com}_{\mathcal{S}}(\mathscr{M})}% \longrightarrow\operatorname{\text{Com}_{\mathcal{S}}(\mathscr{M})}$ be the $\operatorname{\mathbf{K}}_{2}$-injective replacement functor obtained by the small object argument. Denote by $\tau:Id\longrightarrow\mathscr{S}$ the induced natural transformation. Let $\mathcal{F}$ be a $2$-constant commutative premonoid such that $\mathcal{F}_{\geq 2}$ is a strict usual commutative monoid. Then the following hold. 1. $\mathscr{S}(\mathcal{F})$ is also a $2$-constant commutative premonoid such that $\mathscr{S}(\mathcal{F})_{\geq 2}$ is usual commutative monoid. The map $\tau_{\geq 2}:\mathcal{F}_{\geq 2}\longrightarrow\mathscr{S}(\mathcal{F})_{% \geq 2}$ is an isomorphism of usual commutative monoids. 2. $\mathscr{S}(\mathcal{F})$ satisfies the co-Segal conditions. And the canonical map $\mathscr{S}(\mathcal{F})\longrightarrow|\mathscr{S}(\mathcal{F})|$ is an easy weak equivalence. Moreover we have an isomorphism $|\mathscr{S}(\mathcal{F})|\cong\mathscr{S}(\mathcal{F})_{\geq 2}$. 3. Let $L:\operatorname{\text{Com}_{\mathcal{S}}(\mathscr{M})}\longrightarrow\mathcal{B}$ be a functor that sends easy weak equivalences to isomorphisms and takes any $\operatorname{\mathbf{K}}_{2}$-cell complex to an isomorphism. Then for all $\mathcal{F}\in\operatorname{\text{Com}_{\mathcal{S}}(\mathscr{M})}$ not necessarily $2$-constant, the image by $L$ of the unit $\eta:\mathcal{F}\longrightarrow\iota(|\mathcal{F}|)$ is an isomorphism in $\mathcal{B}$. The functor $\mathscr{S}$ will be called the $2$-constant co-Segalification functor.
Definition 1. Let $\mathbb{C}$ be a small category. A natural transformation $f:Y\>\hbox{\raise 0.0pt\hbox{\kern 3.749943pt\hbox{\ignorespaces\ignorespaces% \ignorespaces\ignorespaces\ignorespaces{}\ignorespaces{\hbox{\kern 0.0pt\vrule w% idth -3.749943pt depth 0.0pt height 0.0pt}}\ignorespaces\ignorespaces{\hbox{% \kern 4.5pt\raise 0.0pt\hbox{{}\hbox{\kern 0.0pt\raise-1.999947pt\hbox{\hbox{% \kern 3.0pt\hbox{\hbox{\kern 0.0pt\raise 0.0pt\hbox{$\scriptstyle{}$}}}\kern 3% .0pt}}}}}}\ignorespaces\ignorespaces{\hbox{\kern 4.5pt\raise 0.0pt\hbox{{}% \hbox{\kern 0.0pt\raise-1.999947pt\hbox{\hbox{\kern 3.0pt\hbox{\hbox{\kern 0.0% pt\raise 0.0pt\hbox{$\scriptstyle{}$}}}\kern 3.0pt}}}}}}\ignorespaces{\hbox{% \kern 11.250057pt\raise 0.0pt\hbox{h\hbox{\xyatipfont\char 47\xybtipfont\char 4% 7}\hbox{\kern 0.0pt\raise 0.0ptC}}}}\ignorespaces{\hbox{\kern 3.749943pt\vrule w% idth 7.500114pt depth 0.0pt height 0.0pt}}{\hbox{\kern 18.749943pt\vrule width% -3.749943pt depth 0.0pt height 0.0pt}}\ignorespaces}}}\>X$ of presheaves on $\mathbb{C}$ is called representable if all of its fibers are representable objects, in the following sense: for every $C\in\mathbb{C}$ and $x\in X(C)$, there is a $D\in\mathbb{C}$, a $p:D\>\hbox{\raise 0.0pt\hbox{\kern 3.749943pt\hbox{\ignorespaces\ignorespaces% \ignorespaces\ignorespaces\ignorespaces{}\ignorespaces{\hbox{\kern 0.0pt\vrule w% idth -3.749943pt depth 0.0pt height 0.0pt}}\ignorespaces\ignorespaces{\hbox{% \kern 4.5pt\raise 0.0pt\hbox{{}\hbox{\kern 0.0pt\raise-1.999947pt\hbox{\hbox{% \kern 3.0pt\hbox{\hbox{\kern 0.0pt\raise 0.0pt\hbox{$\scriptstyle{}$}}}\kern 3% .0pt}}}}}}\ignorespaces\ignorespaces{\hbox{\kern 4.5pt\raise 0.0pt\hbox{{}% \hbox{\kern 0.0pt\raise-1.999947pt\hbox{\hbox{\kern 3.0pt\hbox{\hbox{\kern 0.0% pt\raise 0.0pt\hbox{$\scriptstyle{}$}}}\kern 3.0pt}}}}}}\ignorespaces{\hbox{% \kern 11.250057pt\raise 0.0pt\hbox{h\hbox{\xyatipfont\char 47\xybtipfont\char 4% 7}\hbox{\kern 0.0pt\raise 0.0ptC}}}}\ignorespaces{\hbox{\kern 3.749943pt\vrule w% idth 7.500114pt depth 0.0pt height 0.0pt}}{\hbox{\kern 18.749943pt\vrule width% -3.749943pt depth 0.0pt height 0.0pt}}\ignorespaces}}}\>C$, and a $y\in Y(D)$ such that the following square is a pullback, $\xymatrix{\mathsf{y}{D}{}{}{}{}{}{}{}{}{}{}\xy@@ix@{{\hbox{}}}}$
Definition 2.3. We will write $A/x$ for the cofiber of this map $x\colon\Sigma^{k}A\to A$.
Definition 2.1. We say that a rational $\mathbf{E}_{\infty}$-ring $A$ is noetherian if: (1) The commutative ring $\pi_{\mathrm{even}}(A)$ is noetherian. (2) The $\pi_{\mathrm{even}}(A)$-module $\pi_{\mathrm{odd}}(A)$ is finitely generated.
Definition 5.4. Let $K\subseteq T$ be a field extension. Assume that $K^{\prime}\subseteq T$, is another subfield which contains $K_{0}$. By composing with $i_{K,T}:Vec_{K}\rightarrow Vec_{T}$ we get a symmetric tensor functor $i_{K,T}F:\mathcal{C}\rightarrow Vec_{T}$. Then the category $\mathcal{C}\otimes_{K_{0}}K^{\prime}$ is the category we get from $\mathcal{C}$ by adding the morphisms $K^{\prime}\subseteq End_{Vec_{T}}(\boldsymbol{1})$ to $End_{\mathcal{C}}(\boldsymbol{1})$. More precisely: in the construction of $X^{p,q}$ in Section 3, we add all the elements of $K^{\prime}$ to $X^{0,0}$.
Definition 3.1. $X^{p,q}\subseteq W^{p,q}$ is the smallest collection of $\mathbb{Q}$-subspaces which satisfies the following conditions: 1. $x_{i}\in X^{p_{i},q_{i}}$ 2. the identity map $Id_{W}\in End_{K}(W)\cong W\otimes W^{*}$ is contained in $X^{1,1}$. 3. If $x\in X^{p,q}$ and $y\in X^{r,s}$ then $x\otimes y\in X^{p+r,q+s}$ (after rearranging the factors). 4. If $ev_{p,q}:W^{p,q}\rightarrow W^{p-1,q-1}$ is the map which evaluates the first copy of $W^{*}$ on the first copy of $W$ then $ev(X^{p,q})\subseteq X^{p-1,q-1}$. 5. For any $\sigma\in S_{p}$ and $\tau\in S_{q}$ we have that $(\sigma,\tau)(X^{p,q})=X^{p,q}$, where the action is given by permuting the tensor factors. 6. If $0\neq x\in X^{0,0}\subseteq W^{0,0}=K$, then also $x^{-1}\in X^{0,0}$ (the inversion is made in $K$).
Definition 5.3. We call $G$ the fundamental group of $\mathcal{C}$ (see also Section 8 of [6]).
Definition 6.3. We say that a commutative algebra $A$ inside $\mathcal{C}$ is a classifying algebra if: 1. It satisfies the conditions of Lemmas 6.1 and 6.2 2. Every fiber functor from $\mathcal{C}$ to some extension field $K_{1}$ of $K_{0}$ arises from some homomorphism from $B$ to $K_{1}$
Definition A.2. A semi-associoid is a set $M$ together with a partially defined ternary product $(xyz)=\mu(x,y,z)$ on $M$ satisfying the para-associative law: $(xy(zuv))=(x(uzy)v)=((xyz)uv)\,$ that is, if all terms on one side of the equation are defined, then so are those on the other sides, and we have equality. For instance, if $(z,u,v)\in D$ and $(x,y,(zuv))\in D$, then $(u,z,y)\in D$ and $(x,(uzy),y)\in D$, and the first equality holds, and so on. An associoid is a semi-associoid satisfying the idempotent law: $(xxy)=y,\quad(wzz)=w\,,$ whenever $(x,x,y)\in D$ and $(w,z,z)\in D$.
Definition A.9. A pair $(a,b)$ of commuting left and right prev’s is an associoid given by a pair $(a,b)$ of equivalence relations on $M$ and the ternary product $[xyz]$ defined on the domain $D_{a,b}:=(a\times M)\cup(M\times b)$ such that $\forall(x,y)\in a,\forall z\in M:\quad[xyz]\sim_{a}z,\qquad\forall x\in M,% \forall(y,z)\in b:\quad[xyz]\sim_{b}x.$ In other words, $(M,a,[xy;z]_{a})$ is a left prev and $(M,b,[x;yz]_{b})$ a right prev such that the natural compatibility conditions given below are satisfied.
Definition 3.1. A (generalized) associative pair is a pair of sets $(U^{+},U^{-})$ together with two ternary maps $U^{\pm}\times U^{\mp}\times U^{\pm}\to U^{\pm},\quad(x,y,z)\mapsto\langle xyz% \rangle^{\pm}$ satisfying the para-associative law $\langle xy\langle uvw\rangle^{\pm}\rangle^{\pm}=\langle x\langle vuy\rangle^{% \mp}w\rangle^{\pm}=\langle\langle xyu\rangle^{\pm}vw\rangle^{\pm}.$
Definition A.12. A bisection of a pregroupoid $(M,a,b)$ is a bisection of $(a,b)$, i.e., an element of $U_{ab}$, and a local bisection is an element of $U_{ab}^{\mathrm{loc}}$ (cf. eqn. (1.9)).
Definition 1.3. Let $a$ be an equivalence relation in $\Omega$. Then the following defines an equivalence relation $\mathbf{a}$ on $\mathcal{P}(\Omega)$, called induced by $a$: $x\sim_{\mathbf{a}}y\mbox{ iff: }a\mathbf{I}_{x}a=a\mathbf{I}_{y}a\mbox{ iff: }% \forall\omega\in\Omega:(\exists\xi\in x:\xi\sim_{a}\omega)\Leftrightarrow(% \exists\eta\in y:\eta\sim_{a}\omega).$
Definition A.5. A semi-pregroupoid is given by a set $M$ together with a pair $(a,b)$ of equivalence relations on $M$ and a para-associative ternary multiplication $M\times_{a}M\times_{b}M\to M,\quad(x,y,z)\mapsto[xyz]$ such that, for all $(x,y,z)\in M\times_{a}M\times_{b}M$, $[xyz]\sim_{a}z,\qquad[xyz]\sim_{b}x.$ If, moreover, the multiplication satisfies the idempotent law, then $M$ is called a pregroupoid.3This terminology is due to Kock, see [Ko05, Ko07]. Johnstone uses herdoid, see [Jo91].
Definition A.14. Let $(M,a,b,[\quad])$ be a pregroupoid. Recall (Lemma A.6) that the relations $a,b$ commute, and hence $c:=ab$ is again an equivalence relation on $M$. The quivalence classes of $c$ are called the connected components of $(M,a,b,[\quad])$. We say that the pregroupoid is transitive if it has just one equivalence class. In other terms, the morphism onto the pair pregroupoid $M\to M/a\times M/b,\quad x\mapsto([x]_{a},[x]_{b})$ (see Example A.4) is surjective.
Definition A.1. Let $M$ be a set. A partially defined ternary product on $M$ is a map, defined on some non-empty subset $D\subset M^{3}$ (called domain of $\mu$), $\mu:D\to M,\quad(x,y,z)\mapsto\mu(x,y,z)$ According to context, we use other notation for $\mu(x,y,z)$, such as $(xyz)$ or $[xyz]$.
Definition A.7. A (left) principal equivalence relation (abbreviated: (left) prev) $(M,a,[\quad])$ is an associoid $(M,D,[\quad])$ with domain $D=a\times M$ given by an equivalence relation $a$ on $M$ and such that $\forall(x,y)\in a,\forall z\in M:\qquad([xyz],z)\in a.$ In other words, it is a pregroupoid defined by $(a,b)$ with $b=\mathbf{O}_{M}$. Similarly, a right principal equivalence relation on a set $M$ is an associoid with domain $M\times b$, that is, a pregroupoid with $a=\mathbf{O}_{M}$. 4Once more, there may be conflicts of terminology: what is called “torseur” in [DG70] corresponds to what we would call a “prev in the category of schemes”.
Definition 3.10. For $x\in U_{b}$, $y\in U_{a}$, the canonical kernel is the map defined by $B^{a,x,b}_{y}:=B^{axb}_{y}:y\to y,\quad\eta\mapsto P^{a}_{y}\circ P^{b}_{x}(% \eta).$
Definition A.11. The automorphism group of an equivalence relation $a$ is the group permuting equivalence classes of $a$: $\mathrm{Aut}(a)=\{g\in\mathrm{Bij}(M)\mid\,g\circ a=a\circ g\}$ (so that the induced map $[g]:M/a\to M/a$ is well-defined), and the group of strict automorphisms of $a$ is the group preserving each equivalence class of $a$: $\mathrm{Aut}_{1}(a)=\{g\in\mathrm{Bij}(M)\mid\,g\circ a=a=a\circ g\}$ (so that the induced map is $\mathrm{id}_{M/a}$).
Definition A.4. A semitorsor is a semi-associoid with an everywhere defined ternary product map (often denoted by $G^{3}\to G$, $(x,y,z)\mapsto(xyz)$), and a torsor is a semitorsor satisfying the idempotent law. 2Other terms used in the literature instead of “torsor” are groud, heap, pregroup, herd or principal homogeneous space.
Definition 2.5 (Dimensions). For every type $A$, we will define a scalar constant $D_{A}:I$, referring to it as the dimension of type $A$. The dimension of $I$ is defined to be $D_{I}=1:I$, where $1=1_{*}:I\equiv I^{*}$.
Definition 2.14 (Soup reduction). We use the term soup reduction to refer to the binary relation that extends $\alpha$-equivalence with the sequent transformations that are caused by applying one of the soup rules. Thus, for two sequents $\Gamma\vdash_{S_{1}}t:T$ and $\Gamma\vdash_{S_{2}}t:T$, if the soup $S_{1}$ is transformed into $S_{2}$ through the application of one of the soup propagation rules, $S_{1}\rightarrow S_{2}$, then we say that one sequent reduces to the other via soup reduction. Similarly, if a sequent $J_{1}$ is transformed into $J_{2}$ by using the consumption rule to perform a substitution, we say that $J_{1}$ reduces to $J_{2}$ via soup reduction.
Definition A.1 (Syntactic category notational conventions). For notational convenience, we define the following combinators: $\alpha_{A,B,C}:=\lambda\left(a\otimes(b\otimes c)\right).\left((a\otimes b)% \otimes c\right):\left(A\otimes(B\otimes C)\right)\multimap\left((A\otimes B)% \otimes C\right)$ $\eta_{A}:=\lambda 1.(x_{*}\otimes x):I\multimap(A^{*}\otimes A)$ $\lambda_{A}:=\lambda(1\otimes a).a:(I\otimes A)\multimap A$ $\rho_{A}:=\lambda(a\otimes 1).a:(A\otimes I)\multimap A$ $\varepsilon_{A}:=\lambda(x\otimes x_{*}).1:(A\otimes A^{*})\multimap I$ $\sigma_{A,B}:=\lambda(a\otimes b).(b\otimes a):(A\otimes B)\multimap(B\otimes A)$
Definition 2.17 (Lambda abstraction in the dagger lambda calculus). Let $\lambda a.b:=a_{*}\otimes b$ and $A\multimap B:=A^{*}\otimes B$
