Definition 6.6. The following defines the functor $K:\simCat/[n]\to\mathfrak{A}^{\simCat^{\mathrm{op}}}/\Lambda_{k}[n]$. 1. A function $\xi:[m]\to[n]$ cuts $[m]$ into $n+1$ posets $\xi_{j}=\left\{i:[m]|\xi(i)=j\right\}$. 2. Let $\|\xi\|$ be the number of elements of the product $\Pi i:([n]-\left\{k\right\})\mapsto\xi_{i}$. 3. Define $K_{0}(\xi):[m+\|\xi\|]\to[n]$ as follows. $K_{0}(\xi)(i)=\left\{\begin{array}[]{cc}\xi(i)&\xi(i)<k\\ k&\xi(i-\|\xi\|)\leq k\leq\xi(i)\\ \xi(i-\|\xi\|)&k<\xi(i-\|\xi\|)\end{array}\right.$ 4. In the $\Pi i:([n]-\left\{k\right\})\mapsto\xi_{i}$ tuples get the . This ordering determines priority by comparing elements in sequence. $\displaystyle(x_{0},x_{1},\dotsc)\leq_{\rm lex}(x_{0},x_{1},\dotsc)\iff$ $\displaystyle x_{0}\leq y_{0}\land(x_{0}=y_{0}\to(x_{1},\dotsc)\leq_{\rm lex}(% y_{1},\dotsc))$ 5. Let $\kappa(\xi):\Pi i:([n]-\left\{k\right\})\mapsto\xi_{i}\to[m+\|\xi\|]$ be the nondecreasing injection $\kappa$ which sends the lexicographical product $\Pi i:([n]-\left\{k\right\})\mapsto\xi_{i}$ to the interval in $[m+\|\xi\|]$ which starts at the least $i$ such that $K_{0}(\xi)(i)=k$. 6. Let $\lambda(\xi):[m]\to[m+\|\xi\|]$ be the unique nondecreasing injection which skips the image of $\kappa$. This means $\lambda(i)=i$ if $\xi(i)<k$ and $\lambda(i)=i+\|\xi\|$ if $\xi(i)\geq k$. Moreover $K_{0}(\xi)\circ\lambda(\xi)=\xi$. 7. For each morphism $\phi:\xi\to\xi^{\prime}$ in $\simCat/[n]$ let $\phi_{i}:\xi_{i}\to\xi^{\prime}_{i}$ be the fibrewise morphism and let $\Pi i:([n]-\left\{k\right\})\mapsto\phi_{i}$ be the corresponding map of the (lexicographical) products $\Pi i:([n]-\left\{k\right\})\mapsto\xi_{i}\to\Pi i:([n]-\left\{k\right\})% \mapsto\xi^{\prime}_{i}$. 8. Let $K_{1}(\phi):K_{0}(\xi)\to K_{0}(\xi^{\prime})$ be the unique nondecreasing function which satisfies $K_{1}(\phi)\circ\kappa(\xi)=\kappa(\xi^{\prime})\circ\Pi i:([n]-\left\{k\right% \})\mapsto\phi_{i}$ and $K_{1}(\phi)\circ\lambda(\xi)=\lambda(\xi^{\prime})\circ\phi$. The maps $(K_{0},K_{1})$ define an endofunctor of $\simCat/[n]$. To get the functor, let $K(\xi)=h^{*}(\Delta(K_{0}(\xi)))$ for objects $\xi$ of $\simCat/[n]$ and $K(\phi)=h^{*}(\Delta(K_{1}(\phi)))$ for morphisms, where $h^{*}$ is the reindexing functor $\mathfrak{A}^{\simCat^{\mathrm{op}}}/\Delta[n]\to\mathfrak{A}^{\simCat^{% \mathrm{op}}}/\Lambda_{k}[n]$ along the horn inclusion $h:\Lambda_{k}[n]\to\Delta[n]$. Concretely, $K$ satisfies the following equations for all objects $\xi$ of $\simCat/[n]$, all $x:\mathbf{U}(\dom(K(\xi)))$ and all arrows $\phi$ of $\simCat/[n]$. $\displaystyle\mathbf{U}(\dom(K(\xi)))$ $\displaystyle=\left\{x:\mathbf{U}\Delta[m+\|\xi\|]|K_{0}(\xi)\circ x:\mathbf{U% }\Lambda_{k}[n]\right\}$ $\displaystyle K(\xi)(x)$ $\displaystyle=K_{0}(\xi)\circ x$ $\displaystyle\dim(x)$ $\displaystyle=\dom(x)$ $\displaystyle x\cdot\phi$ $\displaystyle=x\circ\phi$ $\displaystyle K(\phi)(x)$ $\displaystyle=K_{1}(\phi)\circ x$
Definition 1.1. A $X$ of $\mathfrak{A}$ is a triple $\left\langle\mathbf{U}X,\dim,\cdot\right\rangle$ where $\mathbf{U}X$ is an object of $\mathfrak{A}$, $\dim$ is a morphism $\mathbf{U}X\to\nno$. Here $\nno$ is the of $\mathfrak{A}$. The operator $\cdot$ is has the following properties. Let $\Ar(\simCat)$ be the object of non decreasing functions between decidable, finite and inhabited initial segments of the natural numbers. The domain $\dom(\cdot)$ of $\cdot$ is the following object. $\left\{\left\langle x,\xi\right\rangle:\mathbf{U}X\times\Ar(\simCat)\middle|% \dim(x)=\max(\cod(\xi))\right\}$ The codomain $\cod(\cdot)$ of $\cdot$ is $\mathbf{U}X$. The operator $\cdot$ satisfies the following equations. $\displaystyle\dim(x\cdot\xi)$ $\displaystyle=\max(\dom(\xi))$ $\displaystyle x\cdot\id$ $\displaystyle=x$ $\displaystyle x\cdot(\alpha\circ\beta)$ $\displaystyle=(x\cdot\alpha)\cdot\beta$ A morphism of simplicial objects $X\to Y$ is a morphism $f:\mathbf{U}X\to\mathbf{U}Y$ which commutes with $\dim$ and $\cdot$. Together with simplicial objects, they form the category of simplicial objects and morphisms $\mathfrak{A}^{\simCat^{\mathrm{op}}}$. The category $\mathfrak{A}^{\simCat^{\mathrm{op}}}$ is enriched over $\mathfrak{A}$. The object of morphisms $X\to Y$ in $\mathfrak{A}$ is ${\rm Nature}(X,Y)$. It represents families of morphisms $X\to Y$, i.e. for each object $I$ of $\mathfrak{A}$ there is a natural bijection between morphisms $I\to{\rm Nature}(X,Y)$ and morphisms $I\times X\to Y$ which commute with $\dim$, $\cdot$ and their $I$-fold multiples.
Definition 6.1. A is a modest fibration $\uni:U\to V$ such that every modest fibration $f:X\to Y$ is the pullback of $\uni$ along a unique morphism $\chi(f):Y\to V$.
Definition 2.5. A monomorphism $f:X\to Y$ of category $\mathfrak{A}^{\simCat^{\mathrm{op}}}$ is a if the subobject of faces of $Y$ which are not in the image of $X$ is decidable. A of $Y$ is a $y:\mathbf{U}Y$ such that if $\phi:[\dim(y)]\to[\dim(y)]$ in $\simCat$ and $y\cdot\phi=y$, then $\phi=\id_{\dim(y)}$. Elements of $\mathbf{U}Y$ which are not faces are called , so a face is a non degenerate element of $\mathbf{U}Y$. Hence $f$ is a cofibration if there is a morphism $\psi:\mathbf{U}Y\to\mathtt{bool}$ such that $\psi(y)=\mathtt{false}$ if an only if $y$ is degenerate or equal to $f(x)$ for some $x:\mathbf{U}X$.
Definition 6.10 (Face notation). We introduce a notation for monomorphisms in $\simCat/[n]$ or more accurately for the result of applying $\Delta$ to them. For $\xi:[m]\to[n]$ and $\phi:[m]\to\mathtt{bool}$ let $\face(\xi,p):\dom(\face(\xi,p))\to\Delta[m]$ satisfy $\displaystyle\mathbf{U}(\dom(\face(\xi,p)))$ $\displaystyle=\left\{\alpha:\mathbf{U}(\Delta[m])\middle|\forall i:\dom(\alpha% ).p\circ\alpha(i)=\mathtt{true}\right\}$ $\displaystyle\dim(\alpha)$ $\displaystyle=\dom(\alpha)$ $\displaystyle\alpha\cdot\chi$ $\displaystyle=\alpha\cdot\chi$ $\displaystyle\face(\xi,p)(\alpha)$ $\displaystyle=\alpha$ $\xymatrix{\bullet\ar[rr]^{\face(\xi,p)}\ar[dr]&&\Delta[m]\ar[dl]^{\Delta(\xi)}% \\ &\Delta[n]}$
Definition 3.3. A morphism $f:X\to Y$ is if it has the internal left lifting property with respect to the family of cycle inclusions, which is defined as follows. The cycle $\partial\Delta[n]$ is the subobject of non surjective functions of $\Delta[n]$. The $\dim$ and $\cdot$ are the same and the bases are related as follows: $\mathbf{U}(\partial\Delta[n])=\left\{\phi:\mathbf{U}(\Delta[n])|\exists i:[n].% \forall j:\dom(\phi).\phi(i)\neq j\right\}$ The is the monomorphism $\partial\Delta[n]\to\Delta[n]$. The family of cycle inclusions is the sum of all cycle inclusions $\Sigma n:\nno\mapsto\partial\Delta[n]\to\Sigma k\leq n:\nno\mapsto\Delta[n]$ that exists as a morphism of $\mathfrak{A}^{\simCat^{\mathrm{op}}}$. Note that $\Sigma x:y\mapsto z(x)$ denotes the of the $y$-indexed family $z$. Definition LABEL:fibration explains how lifting properties with respect to families work.
Definition 2.3. A morphism $f:X\to Y$ of category $\mathfrak{A}^{\simCat^{\mathrm{op}}}$ is a if it has the right lifting property with respect to the family of all horn inclusions, which is defined as follows. For each $n\in\mathbb{N}$, the $n$- $\Delta[n]$ is the internal simplicial set where $\mathbf{U}{\Delta[n]}$ is the object of all morphisms $\phi$ in $\simCat$ such that $\cod(\phi)=n$, $\dim(\phi)=\dom(\phi)$ and $\phi\cdot\xi=\phi\circ\xi$. Here $\mathbb{N}$ is the that exists outside of $\mathfrak{A}$. For each $k:[n]$ the $\Lambda_{k}[n]$ is the subobject of the non decreasing maps $[m]\to[n]$ that are not onto the set $[n]-\left\{k\right\}$. $\mathbf{U}{\Lambda_{k}[n]}=\left\{\phi:\mathbf{U}{\Delta[n]}|\exists i:[n]-% \left\{k\right\}.\forall j:\dom(\phi).\phi(i)\neq j\right\}$ The is the monomorphism $\Lambda_{k}[n]\to\Delta[n]$. The family of horn inclusions is the sum of all horn inclusions $\Sigma k\leq n:\nno\mapsto\Lambda_{k}[n]\to\Sigma k\leq n:\nno\mapsto\Delta[n]$ that exists as a morphism of $\mathfrak{A}^{\simCat^{\mathrm{op}}}$. The lifting property that $f$ satisfies is defined in a slice category of $\mathfrak{A}^{\simCat^{\mathrm{op}}}$. Let $I=\left\{\left\langle n,k\right\rangle:\nno\times\nno|k\leq n\right\}$. The family of horn inclusions is a morphism in $\mathfrak{A}^{\simCat^{\mathrm{op}}}/I_{\rm disc}$ (see example LABEL:discrete). So is the multiple $I_{\rm disc}\times f:I_{\rm disc}\times X\to I_{\rm disc}\times Y$ of $f$. The morphism $f$ is a fibration if $I_{\rm disc}\times f$ has the right lifting property with respect to $\Sigma k\leq n:\nno\mapsto\Lambda_{k}[n]\to\Sigma k\leq n:\nno\mapsto\Delta[n]$.
Definition 1.4. That $\mathfrak{A}$ is a $\Pi$- means all of the following. 1. The category $\mathfrak{A}$ is locally Cartesian closed. This means that for each object $X$ of $\mathfrak{A}$ each slice $\mathfrak{A}/X$ is Cartesian closed. 2. The category $\mathfrak{A}$ is extensive, which means that is has finite coproducts and that $\mathfrak{A}/(X+Y)$ is equivalent to $(\mathfrak{A}/X)\times(\mathfrak{A}/Y)$ for each pair of objects $X$ and $Y$ of $\mathfrak{A}$. 3. The category $\mathfrak{A}$ is exact in the sense of Barr. This means that all weak groupoids (see below) have coequalizers and that those coequalizers are stable under pullback. A consists of a pair of objects $X_{0}$, $X_{1}$ and morphisms $r:X_{0}\to X_{1}$ and $s,t:X_{1}\to X_{0}$ such that $s\circ r=t\circ r=\id_{X_{0}}$ such that for each $x,y,z:X_{0}$ if two of $\left\langle x,y\right\rangle$, $\left\langle x,z\right\rangle$ and $\left\langle y,z\right\rangle$ are in the image of $\left\langle s,t\right\rangle:X_{1}\to X_{0}\times X_{0}$, the third is too. A coequalizer for a weak groupoid is a coequalizer of $s$ and $t$.
Definition 2.8. A is a tuple $\left\langle\mathbf{U}X,\dim,\cdot,\filler\right\rangle$ where $\left\langle\mathbf{U}X,\dim,\cdot\right\rangle$ is a simplicial object and $\filler$ is a filler operator (see definition LABEL:enriched_factorization_system) for the unique morphism $!:\left\langle\mathbf{U}X,\dim,\cdot\right\rangle\to 1_{\rm disc}$. A morphism of complexes is simply a morphism of simplicial objects. The category of complexes and morphisms of complexes is $\mathfrak{A}^{\simCat^{\mathrm{op}}}_{f}$.
Definition 2.1. An on an $\mathcal{A}$-enriched category $\mathcal{C}$ consist of three sets of morphism: the fibrations $F$, the cofibrations $C$ and the weak equivalences $W$ which have the following properties. • The set $W$ satisfies 2-out-of-3 which means that for every pair of morphisms $f:X\to Y$ and $g:Y\to Z$ of $\mathcal{C}$, if two out of $f$, $g$ and $g\circ f$ are in $W$ then all three are. • The pairs of sets $(C,F\cap W)$ and $(C\cap W,F)$ are enriched factorization systems (see definition LABEL:enriched_factorization_system). Categories with such a structure are model categories. Members of $C\cap W$ are called and members of $F\cap W$ are called .
Definition 4.3. The Auslander-Reiten translate of a coherent $\mathcal{O}_{\mathbb{X}}$-module $C$ is the $\mathcal{O}_{\mathbb{X}}$-module $\tau(C,k):=Z^{-1}\mathbf{i}(\operatorname{\mathbf{R}\mathcal{H}\!\!\;\mathit{% om}}_{\mathbb{X}}(\operatorname{\mathbf{R}\mathcal{H}\!\!\;\mathit{om}}_{% \mathbb{X}}(C,\mathcal{O}_{\mathbb{X}}),f^{!}k)).$
Definition 2.10. The object $\tau_{\mathcal{C}}(I)$ is called the Auslander-Reiten translate with respect to $\mathcal{C}\subseteq\operatorname{fp}\mathcal{A}$ and $I\in\operatorname{Mod}\mathcal{C}$. The assignment $I\mapsto\tau_{\mathcal{C}}(I)$ yields a functor $\tau_{\mathcal{C}}\colon\operatorname{Inj}\operatorname{Mod}\mathcal{C}% \longrightarrow\overline{\mathcal{A}}.$
Definition 3.3. The dual of the transpose (or Auslander-Reiten translate) of $C$ with respect to $I$ is the $\Lambda$-module $\tau_{C}(I):=\operatorname{Hom}_{\operatorname{End}_{\Lambda^{\mathrm{op}}}(% \operatorname{Tr}C)^{\mathrm{op}}}(\operatorname{Tr}C,E(\bar{I})).$
Definition 3.2. We say that $\underline{\widehat{X}}^{0}$ is conical if there is a pro-rational $\mathbb{C}^{\times}$-action on $\underline{\widehat{A}}$ that rescales the Poisson bracket on $\underline{\widehat{A}}$ by $t^{-d}$ and whose weights on the maximal ideal of $\underline{\widehat{A}}$ are positive. Further, we say that $X^{0}$ has conical slices if $\underline{\widehat{X}}^{0}$ is conical for all $x\in X^{0}$. This holds for all examples of $X^{0}$ that we know.
Definition 2.1. We say that an element $\theta\in\tilde{\mathfrak{P}}_{\mathbb{Q}}$ is generic if it does not lie in $\mathcal{H}_{\mathbb{C}}$. The cones $wC_{i}$ will be called chambers.
Definition 3 (Poincaré series). Let $\mathcal{X}$ be a ns collection with finite-dimensional components. The generating series for Euler characteristics of components of $\mathcal{X}$ is called the Poincaré series of $\mathcal{X}$ and is denoted by $g_{\mathcal{X}}(t)$: $g_{\mathcal{X}}(t)=\sum_{n\geq 0}\chi(\mathcal{X}(n))t^{n}.$
Definition 1 (Koszul duality). Let $(\mathcal{X},\mathcal{R})$ be a choice of quadratic data. The Koszul duality for operads assigns to an operad $\mathcal{P}=\mathcal{P}(\mathcal{X},\mathcal{R})$ its Koszul dual cooperad $\mathcal{P}^{\scriptstyle\text{\rm!`}}:=\mathcal{C}(s\mathcal{X},s^{2}\mathcal% {R}).$
Definition 7 ([12, Def. 3.2]). The minimal model $(\mathcal{T}(\mathcal{E}),{\partial})$ of an $n$-ary operad has a gap of length $d\geq 1$ if there is a $q\geq 2$ such that $\mathcal{E}^{(p)}=0\text{ for }q\leq p\leq q+d-1$ while $\mathcal{E}^{(q-1)}\not=0\not=\mathcal{E}^{(q+d)}$.
Definition 2 (Koszul operad). A quadratic operad $\mathcal{P}$ is said to be Koszul if its Koszul complex is acyclic, so that the inclusion ${\mathbf{k}}\cong(\mathcal{P}\circ\mathcal{P}^{\scriptstyle\text{\rm!`}})(1)% \hookrightarrow\mathcal{P}\circ\mathcal{P}^{\scriptstyle\text{\rm!`}}$ induces an isomorphism in the homology.
Definition 2.4. If $\ast$ and $\ast^{\prime}$ are two right-monoidal structures on the same category $\mathcal{M}$ with the same unit object $R$ then a twist from the $\ast$ structure to the $\ast^{\prime}$-structure is a natural isomorphism $w_{M,N}:M\ast N\stackrel{{\scriptstyle\sim}}{{\rightarrow}}M\ast^{\prime}N$ such that $\langle\operatorname{id}_{\mathcal{M}},w,1_{R}\rangle$ is a right-monoidal functor from $\mathcal{M}$ with $\ast^{\prime}$ to $\mathcal{M}$ with $\ast$ structure.
Definition 7.4. A right-monoidal category $\langle\mathcal{M},\ast,R,\gamma,\eta,\varepsilon\rangle$ is corepresentable by a monoidal comonad $\langle C,C_{2},C_{0},\Delta,\epsilon\rangle$ in a (left-) monoidal structure $\langle\mathcal{M},\otimes,R,\mathbf{a}^{-1},\mathbf{r}^{-1},\mathbf{l}\rangle$ when it is twist-isomorphic to the following right-monoidal structure: $\displaystyle M\odot N$ $\displaystyle:=N\otimes CM$ $\displaystyle\dot{\gamma}_{L,M,N}$ $\displaystyle:=(N\otimes C_{M,CL})\circ(N\otimes(CM\otimes\Delta_{L}))\circ% \mathbf{a}^{-1}_{N,CM,CL}$ $\displaystyle\dot{\eta}_{M}$ $\displaystyle:=(M\otimes C_{0})\circ\mathbf{r}^{-1}_{M}$ $\displaystyle\dot{\varepsilon}_{M}$ $\displaystyle:=\epsilon_{M}\circ\mathbf{l}_{CM}\,.$
Definition 2.1. A right-monoidal category $\langle\mathcal{M},\ast,R,\gamma,\eta,\varepsilon\rangle$ consists of a category $\mathcal{M}$, a functor $\mbox{\,\rm\_}\,\ast\mbox{\,\rm\_}\,:\mathcal{M}\times\mathcal{M}\to\mathcal{M}$, an object $R$ of $\mathcal{M}$ and natural transformations $\displaystyle\gamma_{L,M,N}$ $\displaystyle:L\ast(M\ast N)\to(L\ast M)\ast N$ $\displaystyle\eta_{M}$ $\displaystyle:M\to R\ast M$ $\displaystyle\varepsilon_{M}$ $\displaystyle:M\ast R\to M$ subject to the following axioms: For all objects $K$, $L$, $M$, $N$ $\displaystyle(\gamma_{K,L,M}\ast N)\circ\gamma_{K,L\ast M,N}\circ(K\ast\gamma_% {L,M,N})$ $\displaystyle=\gamma_{K\ast L,M,N}\circ\gamma_{K,L,M\ast N}$ $\displaystyle\gamma_{R,M,N}\circ\eta_{M\ast N}$ $\displaystyle=\eta_{M}\ast N$ $\displaystyle\varepsilon_{M\ast N}\circ\gamma_{M,N,R}$ $\displaystyle=M\ast\varepsilon_{N}$ $\displaystyle(\varepsilon_{M}\ast N)\circ\gamma_{M,R,N}\circ(M\ast\eta_{N})$ $\displaystyle=M\ast N$ $\displaystyle\varepsilon_{R}\circ\eta_{R}$ $\displaystyle=R$
Definition 2.5. Let $F,G:\mathcal{M}\to\mathcal{N}$ be skew-monoidal functors. A monoidal natural transformation $\nu:F\to G$ is a natural transformation of the underlying functors which satisfies $\displaystyle\nu_{X\ast Y}\circ F_{X,Y}$ $\displaystyle=G_{X,Y}\circ(\nu_{X}\ast\nu_{Y})$ $\displaystyle\nu_{R}\circ F_{0}$ $\displaystyle=G_{0}\,.$ Opmonoidal transformations are similar transformations between skew-opmonoidal functors.
Definition 7.3. The right-monoidal structures twist isomorphic (see Definition 2.4) to ones arising from a bimonad w.r.t. some ordinary monoidal structure $\otimes$ as in Proposition 7.2 are called $\otimes$-representable or representable by a $\otimes$-bimonad.
Definition 2.2. If $\mathcal{M}$ and $\mathcal{N}$ are right-monoidal categories (with structures denoted by $\ast$, $R$, $\gamma$, $\eta$, $\varepsilon$ in both cases) then a right-monoidal functor $\mathcal{M}\to\mathcal{N}$ is a triple $\langle F,F_{2},F_{0}\rangle$ where $F$ is a functor $\mathcal{M}\to\mathcal{N}$ of the underlying categories, $F_{0}$ is an arrow $R\to FR$ and $F_{2}$ is a natural transformation $F_{X,Y}:FX\ast FY\to F(X\ast Y)$ satisfying $\displaystyle F\gamma_{X,Y,Z}\circ F_{X,Y\ast Z}\circ(FX\ast F_{Y,Z})$ $\displaystyle=F_{X\ast Y,Z}\circ(F_{X,Y}\ast FZ)\circ\gamma_{FX,FY,FZ}$ $\displaystyle F_{R,X}\circ(F_{0}\ast FX)\circ\eta_{FX}$ $\displaystyle=F\eta_{X}$ $\displaystyle F\varepsilon_{X}\circ F_{X,R}\circ(FX\ast F_{0})$ $\displaystyle=\varepsilon_{FX}$ for all $X,Y,Z\in\mathcal{M}$. Left-monoidal functors are similar functors between left-monoidal categories. They together will be referred to as skew-monoidal functors. A skew-opmonoidal functor $\mathcal{M}\to\mathcal{N}$ is a triple $\langle F,F^{2},F^{0}\rangle$ where $F$ is a functor $\mathcal{M}\to\mathcal{N}$, $F^{0}$ is an arrow $FR\to R$ and $F^{2}$ is a natural transformation $F^{X,Y}:F(X\ast Y)\to FX\ast FY$ such that $F$, $F_{0}:=F^{0}$ and $F_{X,Y}:=F^{Y,X}$ define a skew-monoidal functor $\mathcal{M}^{\mathrm{op},\mathrm{rev}}\to\mathcal{N}^{\mathrm{op},\mathrm{rev}}$.
Definition 8.1. A tetrahedral homomorphism from the $\ast$-structure to the $\otimes$-structure is a natural transformation $t_{L,M,N}:\ L\otimes(M\ast N)\ \to\ (L\otimes M)\ast N$ satisfying the following axioms: $\displaystyle(\mathbf{a}_{K,L,M}\ast N)\circ t_{K,L\otimes M,N}\circ(K\otimes t% _{L,M,N})$ $\displaystyle=t_{K\otimes L,M,N}\circ\mathbf{a}_{K,L,M\ast N}$ $\displaystyle(t_{K,L,M}\ast N)\circ t_{K,L\ast M,N}\circ(K\otimes\gamma_{L,M,N})$ $\displaystyle=\gamma_{K\otimes L,M,N}\circ t_{K,L,M\ast N}$ $\displaystyle t_{R,M,N}\circ\mathbf{l}^{-1}_{M\ast N}$ $\displaystyle=\mathbf{l}^{-1}_{M}\ast N$ $\displaystyle\varepsilon_{M\otimes N}\circ t_{M,N,R}$ $\displaystyle=M\otimes\varepsilon_{N}\,.$ A tetrahedral isomorphism is a tetrahedral homomorphism $t$ for which $w_{M,N}:=(\mathbf{r}_{M}\ast N)\circ t_{M,R,N}\ :\ M\otimes TN\to M\ast N$ is a natural isomorphism where $T=R\ast\mbox{\,\rm\_}\,$.
Definition A.1 An $n\times n$ matrix $Z$ over $k$ is transitive if for all $p\geq 0$ and $i_{1},\ldots,i_{p}\in\{1,\ldots,n\}$, $Z_{i_{0}i_{p}}=0\ \Rightarrow\ Z_{i_{0}i_{1}}Z_{i_{1}i_{2}}\cdots Z_{i_{p-1}i_% {p}}=0.$
Definition B.5 Let $\mathscr{E}$ be a category with pullbacks and $\mathbf{T}$ a monad on $\mathscr{E}$. A homomorphism $f\colon(A,\alpha)\to(B,\beta)$ of $\mathbf{T}$-algebras is a stable pullback-homomorphism if for every homomorphism $g\colon(C,\gamma)\to(B,\beta)$ of $\mathbf{T}$-algebras, the pullback of $f$ along $g$ is a pullback-homomorphism.
Definition 3.1 Let $a$ and $b$ be objects of a category $\mathbf{A}$. The patch ${[a,b]_{\mathbf{A}}}$ is the full subcategory of $\mathbf{A}$ with objects $\{c\in\mathbf{A}\>|\>\text{there exist maps }a\to c\to b\}$.
Definition B.1 Let $\mathbf{T}=(T,\eta,\mu)$ be a monad on a category $\mathscr{E}$. A homomorphism $\begin{array}[]{c}\xymatrix{TA\ar[r]^{Tf}\ar[d]\hfil&TB\ar[d]\\ A\ar[r]^{f}&B\end{array}}$ of $\mathbf{T}$-algebras is a pullback-homomorphism if the square (8) is a pullback.
Definition 3.3 A patch-finite category $\mathbf{A}$ has coarse Möbius inversion if $\zeta_{\mathbf{A}}\in k_{\mathrm{p}}\mathbf{A}$ is invertible. In that case, its coarse Möbius function is $\mu_{\mathbf{A}}=\zeta_{\mathbf{A}}^{-1}\in k_{\mathrm{p}}\mathbf{A}$.
Definition 17 (Lie Bialgebra). Let $L$ be a (possibly graded) $\mathbb{K}$-space. A Lie bialgebra on $L$ is the triple $(L,[\,,\,],\delta)$ such that • $(L,[\,,\,])$ is a Lie algebra; • $(L,\delta)$ is a Lie coalgebra; • The Lie algebra and coalgebra satisfy the following identity, called the Drinfeld compatibility: $\delta[a,b]=\sum_{(a)}((-1)^{|a^{\prime\prime}||b|}[a^{\prime},b]\otimes a^{% \prime\prime}+a^{\prime}\otimes[a^{\prime\prime},b])+\sum_{(b)}([a,b^{\prime}]% \otimes b^{\prime\prime}+(-1)^{|a||b^{\prime}|}b^{\prime}\otimes[a,b^{\prime% \prime}]),$ for all $a,b\in L$, where we write $\delta(a)=\sum_{(a)}a^{\prime}\otimes a^{\prime\prime}$ and $\delta(b)=\sum_{(b)}b^{\prime}\otimes b^{\prime\prime}$. If moreover, $[\,,\,]\circ\delta(a)\equiv 0$, for all $a\in L$, $(L,[\,,\,],\delta)$ is said to be involutive. If the Lie bracket has degree $k$ and the Lie cobracket has degree $l$, denote the Lie bialgebra with degree $(l,k)$.
Definition 38 (Lie${}_{\infty}$ Homomorphism). Suppose $V$ and $W$ are two Lie${}_{\infty}$ algebras. A Lie${}_{\infty}$ homomorphism from $V$ to $W$ is a differential graded coalgebra map $F:\mbox{$\bigwedge^{\bullet}V$}\longrightarrow\mbox{$\bigwedge^{\bullet}W$}.$ More precisely, a Lie${}_{\infty}$ homomorphism from $V$ to $W$ consists of a sequence of linear operators $F_{k}:\mbox{$\bigwedge^{k}V$}\longrightarrow W,\quad k=1,2,\cdots$ such that for all $a_{1}a_{2}\cdots a_{k}\in\bigwedge^{k}V$, $\displaystyle\sum_{i}\sum_{k_{1}+\cdots+k_{i}=k}\sum_{\sigma}\delta_{i}(F_{k_{% 1}}(a_{\sigma(1)}\cdots a_{\sigma(k_{1})})\cdots F_{k_{i}}(a_{\sigma(k_{1}+% \cdots+k_{i-1}+1)}\cdots a_{\sigma(k)}))\\ \displaystyle=\sum_{p+q=k+1}\sum_{\mu}F_{p}(\delta_{q}(a_{\mu(1)}\cdots a_{\mu% (q)})a_{\mu(q+1)}\cdots a_{\mu(k)}),$ where $\sigma$ runs over all $(k_{1},k_{2},\cdots,k_{i})$-unshuffles of $k$, and $\mu$ runs over all $(q,p)$-unshuffles of $k$, respectively.
Definition 7 (Perturbation Datum). Let $S$ be a pointed-boundary Riemann surface with Lagrangian labels. Suppose we have chosen strip-like ends for it, and also a Floer datum $(H_{\zeta},J_{\zeta})$ for each of the pairs of sub manifolds $(L_{\zeta 0},L_{\zeta 1})$ associated to the points at infinity $\zeta\in\Sigma$. A perturbation datum for $S$ is a pair $(K,J)$ where • $K\in\Omega^{1}(S,\mathscr{H})$ satisfies $K(\xi)|L_{C}=0$, for all $\xi\in TC$, where $C$ is a component of $\partial S$, and • $J$ is a family of almost complex structures $J\in C^{\infty}(S,\mathscr{J})$, such that they are compatible with the chosen strip-like ends and Floer data, in the sense that $\epsilon^{*}_{\zeta}K=H_{\zeta}(t)dt,\quad J(\epsilon_{\zeta}(s,t))=J_{\zeta}(t)$ for each $\zeta\in\Sigma^{\pm}$ and $(s,t)\in Z^{\pm}$.
Definition 25 (Augmentation). Given $\gamma\in\mathcal{P}_{\lambda}$, denote by $\mathcal{M}(\gamma;\emptyset)$ the moduli space of (equivalence classes of) $J$-holomorphic planes $F:\mathbb{C}\to\widehat{M}$ which is asymptotic to $\gamma$ at $\infty$. If $J$ is regular and $\dim\mathcal{M}(\gamma;\emptyset)=0$, denote by $\varepsilon(\gamma):=\#\mathcal{M}(\gamma;\emptyset),\quad\mbox{for all }% \gamma\in\mathcal{P}_{\lambda},$ where $\#$ is counting with sign (see [6]), and $\varepsilon(\gamma)$ is called the symplectic augmentation of $\gamma$.
Definition 3 (Cyclic Homology). Suppose $\mathcal{A}$ is an A${}_{\infty}$ category. Let $\displaystyle t_{n}:\overline{\mathrm{Hom}}(A_{1},A_{2})\otimes\overline{% \mathrm{Hom}}(A_{2},A_{3})\otimes\cdots\otimes\overline{\mathrm{Hom}}(A_{n+1},% A_{1})\\ \displaystyle\longrightarrow\overline{\mathrm{Hom}}(A_{n+1},A_{1})\otimes% \overline{\mathrm{Hom}}(A_{1},A_{2})\cdots\otimes\overline{\mathrm{Hom}}(A_{n}% ,A_{n+1}),$ for $n=0,1,2,\cdots$, be the linear map $t_{n}(\overline{a}_{1},\overline{a}_{2},\cdots,\overline{a}_{n+1}):=(-1)^{|% \overline{a}_{n+1}|(|\overline{a}_{1}|+\cdots+|\overline{a}_{n}|)}(\overline{a% }_{n+1},\overline{a}_{1},\cdots,\overline{a}_{n}).$ Extend $t_{n}$ to $\mathrm{Hoch}_{*}(\mathcal{A})$ trivially, and let $t=t_{0}+t_{1}+t_{2}+\cdots$, the cokernel $\mathrm{Hoch}_{*}(\mathcal{A})/(1-t)$ of $1-t$ forms a chain complex with the induced differential from the Hochschild complex (still denoted by $b$). Such chain complex is denoted by $\mathrm{Cycl}_{*}(\mathcal{A})$, and is called the Connes cyclic complex of $\mathcal{A}$. Its homology is called the cyclic homology of $\mathcal{A}$, and is denoted by $\mathrm{HC}_{*}(\mathcal{A})$. The cyclic cohomology of $\mathcal{A}$ is the homology of the dual cochain complex $\mathrm{Cycl}^{*}(\mathcal{A})$ of $\mathrm{Cycl}_{*}(\mathcal{A})$. Namely, suppose $f\in\mathrm{Hom}(\mathrm{Hoch}_{*}(\mathcal{A}),k)$, then $f\in\mathrm{Cycl}^{*}(\mathcal{A})$ if and only if for all $\alpha\in\mathrm{Hoch}_{*}(\mathcal{A})$, $f(\alpha)=f(t(\alpha))$.
Definition 22. A set of cylindrical ends for $S$ consists of proper holomorphic embeddings $\epsilon_{\eta}:{\mathcal{C}}^{\pm}\to S$, one for each $\eta\in\Lambda^{\pm}$, using locally complex coordinates on $S$, satisfying $\displaystyle\ \epsilon_{\eta}(r,\theta)=e^{\mp(r+{\rm i}\theta)}\ \ \ {\rm and% }\ \ \ \lim_{r\to\pm\infty}\epsilon_{\eta}(r,\cdot)=\eta$ and with the additional requirement that the images of the $\epsilon_{\eta}$ are pairwise disjoint.
Definition 30 (Analytic Data for Relating Maps). Let $S\in\overline{\mathcal{R}}^{1}_{k}$ be a stable one-punctured pointed-boundary Riemann surface with (compatibly labeled) Lagrangian labels. The analytic data for relating maps on $S$, denoted by $\mathbf{D}_{rel}$, consists of the following choices: • Cylindrical end $\epsilon^{-}_{\eta}$ for incoming interior puncture and strip-like ends $\epsilon^{+}_{1},\cdots,\epsilon^{+}_{k}$ for outgoing boundary punctures. • A Floer datum $(H_{\zeta},J_{\zeta})$ for each pair of Lagrangian submanifolds $(L_{\zeta,0},L_{\zeta,1})$ associated to $\zeta\in\Sigma$ is as above; • A perturbation datum for $S$ (with or without punctures) is a pair $(K,J)$, where $K\in\Omega^{1}(S,\mathscr{H})$ is a function-valued 1-form on $M$ satisfying $K(\xi)|_{L_{C}}=0$ for all $\xi\in TC\subset T(\partial S)$; $J\in C^{\infty}(S,\mathscr{J})$ is a family of almost complex structure; moreover, $K$ and $J$ should be compatible with the chosen cylindrical and strip-like ends and Floer data, $i.e.$ $\displaystyle\epsilon^{*}_{\eta}K=0,$ $\displaystyle J(\epsilon_{\eta}(r,\theta))=J_{\eta}(\theta),$ $\displaystyle\epsilon^{*}_{\zeta}K=H_{\zeta}(t)dt,$ $\displaystyle J(\epsilon_{\zeta}(s,t))=J_{\zeta}(t)$ for each $\eta\in\Lambda^{\pm}$, $(r,\theta)\in\mathcal{C}^{\pm}$, and $\zeta\in\Sigma^{\pm}$, $(s,t)\in Z^{\pm}$.
Definition 12 (Floer Cochain). If $\tilde{L}_{1},\tilde{L}_{2}$ intersect transversally, then the grading $\eta_{\tilde{L}_{1},\tilde{L}_{2}}(p)$ of $p$ is defined to be the index of $\overline{\partial}$ in above lemma. More generally, for two arbitrary $\tilde{L}_{1},\tilde{L}_{2}$ with analytic data, let $\mathrm{Hom}(\tilde{L}_{1},\tilde{L}_{2}):=\mbox{Span}\{y:[0,1]\to M|y(0)\in L% _{1},y(1)\in L_{2},\mbox{ and }dy/dt=X(t,y(t))\}.$ where the grading of $y$, when viewed as the intersection point $p$ of $\tilde{L}_{1},\phi(\tilde{L}_{2})$, is defined to be the grading $\eta_{\tilde{L}_{1},\phi(\tilde{L}_{2})}(p)$. An element in $\mathrm{Hom}(\tilde{L}_{1},\tilde{L}_{2})$ is called a Floer cochain of $\tilde{L}_{1}$ and $\tilde{L}_{2}$, and $y$ is sometimes called a Hamiltonian chord.
Definition 2 (Hochschild Homology). Let $\mathcal{A}$ be an A${}_{\infty}$ category. The Hochschild chain complex $\mathrm{Hoch}_{*}(\mathcal{A})$ of $\mathcal{A}$ is the chain complex whose underlying vector space is $\bigoplus_{n=1}^{\infty}\bigoplus_{A_{1},A_{2},\cdots,A_{n+1}\in\mathcal{O}b(% \mathcal{A})}\overline{\mathrm{Hom}}(A_{1},A_{2})\otimes\overline{\mathrm{Hom}% }(A_{2},A_{3})\otimes\cdots\otimes\overline{\mathrm{Hom}}(A_{n+1},A_{1})$ with differential $b$ defined by $\displaystyle b(\overline{a}_{1},\overline{a}_{2},\cdots,\overline{a}_{n+1})$ $\displaystyle=$ $\displaystyle\sum_{k=1}^{n+1}\sum_{i=1}^{n-k+1}\pm(\overline{a}_{1},\cdots,% \overline{a}_{k-1},\overline{m}_{i}(\overline{a}_{k},\cdots,\overline{a}_{k+i-% 1}),\overline{a}_{k+i},\cdots,\overline{a}_{n+1})$ $\displaystyle+$ $\displaystyle\sum_{j=0}^{n-1}\sum_{i=1}^{n-j}\pm(\overline{m}_{i+j+1}(% \overline{a}_{n-j+1},\overline{a}_{n-j+2},\cdots,\overline{a}_{n+1},\overline{% a}_{1},\overline{a}_{2},\cdots,\overline{a}_{i}),\overline{a}_{i+1}\cdots,% \overline{a}_{n-j}).$ The associated homology is called the Hochschild homology of $\mathcal{A}$, and is denoted by $\mathrm{HH}_{*}(\mathcal{A})$.
Definition 23. (1) Suppose $D\subset\mathbb{C}$ is the unit disk. We say a smooth map $F:D\setminus\{0\}\to W\times\mathbb{R}$ is asymptotic to a Reeb orbit $\gamma\subset W$ at $\pm\infty$ if $F(r,\theta)=(f(r,\theta),a(r,\theta))$ has the property that $\lim\limits_{r\to 0}a(r,\theta)=\pm\infty$ and the uniform limit $\displaystyle\lim_{r\to 0}f(r,\theta)$ exists and parameterizes $\gamma$. (2) More generally, suppose $\Lambda=\{\eta_{1},\eta_{2},\cdots,\eta_{k}\}$. We say that a smooth map $F:S\to W\times\mathbb{R}$ is asymptotic to a Reeb orbit ${\gamma}_{j}\in\mathcal{P}_{\lambda}$ in $\eta_{j}$ at $\pm\infty$, if there exists polar coordinates $(r,\theta)$ centered at $\eta_{j}$ such that $F$ restricted to a neighborhood of $\eta_{j}$ is asymptotic to $\gamma_{j}$ in the sense above. These $\eta_{j}$’s are called the punctures of $F$; they are either positive or negative according to the sign of $\pm\infty$.
Definition 6 (Floer Datum). For each ordered pair of Lagrangian submanifolds $L_{0},L_{1}\subset M$, a Floer datum consists of $H_{L_{0},L_{1}}\in C^{\infty}([0,1],\mathscr{H})$ and $J\in C^{\infty}([0,1],\mathscr{J})$, with the following property: if $X$ is the time-dependent Hamiltonian vector field of $H$ and $\phi$ is its flow, then $\phi^{1}(L_{0})$ intersects $L_{1}$ transversally.
Definition 32 (Consistent Universal Analytic Data for Relating Maps). A consistent universal choice of analytic data for a relating map is a choice $\mathbf{D}_{rel}$ of analytic data, for each integer $k\geq 1$ and every representative of $S\in\overline{\mathcal{R}}^{1}_{k}$, such that: (1) For any possible $m$, there is an open subset $\overline{U}\subset\overline{\mathcal{R}}^{1}_{\mathrm{glue}}$ containing the $\{+\infty\}\times(\partial\mathcal{R}^{1}_{k})^{m}$, such that the two analytic data (coming from $\mathbf{D}_{rel}$ or $\mathbf{D}_{\mathrm{Fuk}}$) on the glued space (one is inherited from the universal choice of data on each boundary stratum through the gluing process, the other is the pullback from the universal choice of data on $\mathcal{R}^{1}_{k}$ via the gluing map) agree over $U=\overline{U}\cap\mathcal{R}^{1}_{\mathrm{glue}}$; (2) Let $(K,J)$ be the first perturbation datum (obtained by gluing) on $\mathcal{R}^{1}_{\mathrm{glue}}$, and $(\overline{K},\overline{J})$ its extention to the compactification $\overline{\mathcal{R}}^{1}_{\mathrm{glue}}$, then the other datum (obtained by pullback from $\mathcal{R}^{1}_{k}$) also extends smoothly to $\overline{\mathcal{R}}^{1}_{\mathrm{glue}}$, and the extension agrees with $(\overline{K},\overline{J})$ over the subset $\{+\infty\}\times(\partial\mathcal{R}^{1}_{k})^{m}\subset\overline{\mathcal{R}% }^{1}_{\mathrm{glue}}$.
Definition 16 (Modified Floer Datum). For each ordered pair of Lagrangian submanifolds $L_{0},L_{1}\subset M$, a Floer datum consists of $H_{L_{0},L_{1}}\in C^{\infty}([0,1],\mathscr{H})$ and $J\in C^{\infty}([0,1],\mathscr{J})$, besides the requirement of Definition 6, satisfying the following additional properties: (1) for the opposite ordered pair $(L_{1},L_{0})$, $H_{L_{1},L_{0}}(t)=-H_{L_{0},L_{1}}(1-t)$; (2) if $X$ is the time-dependent Hamiltonian vector field of $H_{L_{0},L_{1}}$ and $\phi_{X}$ its flow, then $\phi^{1/2}_{X(t)}(L_{0})$ intersects $\phi^{1/2}_{-X(1-t)}(L_{1})$ transversally. (Note $-X(1-t)$ is the Hamiltonian vector field of $H_{L_{1},L_{0}}$.)
Definition 1 (A${}_{\infty}$ Category). An A${}_{\infty}$ category $\mathcal{A}$ consists of a set of objects $\mathcal{O}b(\mathcal{A})$, a graded vector space $\mathrm{Hom}_{\mathcal{A}}(A_{1},A_{2})$ for each pair of objects $A_{1},A_{2}\in\mathcal{O}b(\mathcal{A})$, and a sequence of operators: $m_{n}^{\mathcal{A}}:\mathrm{Hom}_{\mathcal{A}}(A_{1},A_{2})\otimes\mathrm{Hom}% _{\mathcal{A}}(A_{2},A_{3})\otimes\cdots\otimes\mathrm{Hom}_{\mathcal{A}}(A_{n% },A_{n+1})\to\mathrm{Hom}_{\mathcal{A}}(A_{1},A_{n+1}),$ where $|m_{n}^{\mathcal{A}}|=2-n$, for $n=1,2,\cdots,$ satisfying the following A${}_{\infty}$ relations: $\sum_{p=1}^{n}\sum_{k=1}^{n-p+1}(-1)^{\mu_{npk}}m_{n-k+1}^{\mathcal{A}}(a_{1},% \cdots,a_{p-1},m_{k}^{\mathcal{A}}(a_{p},\cdots,a_{p+k-1}),a_{p+k},\cdots,a_{n% })=0,$ where $a_{i}\in\mathrm{Hom}_{\mathcal{A}}(A_{i},A_{i+1})$, for $i=1,2,\cdots,n$, and $\mu_{npk}=\sum\limits_{r=1}^{n}(r-1)|a_{r}|+\sum\limits_{s=1}^{p-1}k|a_{s}|+p(% 2-k)$.
Definition 21 (Conley-Zehnder Index). It is easy to see $(\xi,d\lambda|_{\xi})$ is a symplectic bundle over $M$, and henceforth, to each transversally nondegenerate closed Reeb orbit $\gamma$, one may assign the corresponding Maslov index, called the Conley-Zehnder index and denoted by $\mu_{CZ}(\gamma)$ or simply $\mu(\gamma)$ (see [14]).
Definition 9 (Grading of Lagrangian Submanifolds). A graded Lagrangian submanifold of $(M,\widetilde{{Lag}}(M))$ is an oriented Lagrangian submanifold $L$ and a lift of $s$ to $\tilde{s}:L\to\widetilde{{Lag}}(M)$. The lifting $\tilde{s}$ is called the grading of $L$, and denote $L$ with $\tilde{s}$ by $\tilde{L}$.
Definition 27 (Lie${}_{\infty}$ Algebra). Suppose $L$ is a graded vector space over $\mathbb{K}$. A Lie${}_{\infty}$ algebra on $L$ is a degree $-1$ differential $\delta:\bigwedge^{\bullet}L\to\bigwedge^{\bullet}L$ which is a coderivation with respect to the coproduct $\Delta$.
Definition 2.26. ([Abu-b]) We say that a right $S$-semimodule $Q$ is (uniformly) $\mathcal{M}$-injective, where $\mathcal{M}$ is a class of right $S$-semimodules, iff for every (uniform) injective morphism $0\longrightarrow L\overset{f}{\longrightarrow}M$ with $M\in\mathcal{M},$ the induced morphism of Abelian monoids $\mathrm{Hom}_{S}(M,Q)\overset{(f,Q)}{\longrightarrow}\mathrm{Hom}_{S}(L,Q)$ is surjective (and uniform). If ${}_{S}Q$ is (uniformly) $M$-injective for every $M\in$ ${}_{S}\mathbb{S},$ then we say that ${}_{S}Q$ is (uniformly) injective. In fact, ${}_{S}Q$ is uniformly injective if and only if $\mathrm{Hom}_{S}(-,Q)$ preserves exact sequences.
Definition 3.15. Let $Q$ be a right $S$-semimodule. We say that $Q_{S}$ (uniformly) cogenerates a class $\mathcal{M}$ of right semimodules iff the following holds: for every morphisms $\iota:U\longrightarrow M$ with $M\in\mathcal{M},$ if $(\iota,Q):\mathrm{Hom}_{S}(M,Q)\longrightarrow\mathrm{Hom}_{S}(U,Q)$ is surjective (and uniform), then $0\longrightarrow U\overset{\iota}{\longrightarrow}M$ is injective (and uniform). If $Q$ (uniformly) cogenerates all left $S$-semimodules, then we say that $Q$ is a (uniform) cogenerator in $\mathbb{S}_{S}.$
