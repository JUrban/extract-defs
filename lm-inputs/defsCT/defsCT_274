Definition 4.1. Define $MG$ as the following $S*G$-bimodule. • Its underlying vector space is $M\otimes_{\mathbbm{k}}\mathbbm{k}G$, a tensor $m\otimes g$ is denoted by $m*g$ for all $m\in M$ and $g\in G$. • The actions of $S$ and $G$ on the left and on the right are such that, for all $m\in M$, $g,h,k\in G$ and $s,s^{\prime}\in S$, $\begin{array}[]{ccc}s\cdot(m*g)\cdot s^{\prime}&=&sm\,^{g}s^{\prime}*g\\ h\cdot(m*g)\cdot k&=&\,{}^{h}m*hgk\,.\end{array}$
Definition 1.9. Let $\underline{i}^{\prime\prime}=i_{0},\ldots,i_{m+n}$ be a sequence in $Q_{0}$, where $m,n\geqslant 1$. Denote the sequences $i_{0},\ldots,i_{m}$ and $i_{m},i_{m+1},\ldots,i_{m+n}$ by $\underline{i}$ and $\underline{i}^{\prime}$, respectively. Let $U\in\mathrm{mod}(\mathbbm{k}G_{i_{0}})$, $V\in\mathrm{mod}(\mathbbm{k}G_{i_{m}})$, and $W\in\mathrm{mod}(\mathbbm{k}G_{i_{m+n}})$. For all $f\in\operatorname{Hom}_{\mathbbm{k}G_{i_{0}}}(U,M(\underline{i};V))$ and $f^{\prime}\in\operatorname{Hom}_{\mathbbm{k}G_{i_{m}}}(V,M(\underline{i}^{% \prime};W))$, define a $\mathbbm{k}$-linear mapping $f^{\prime}\circledast f\colon U\to M(\underline{i^{\prime\prime}};W)$ by $f^{\prime}\circledast f=\sum_{\underline{y^{\prime\prime}}}(f^{\prime}% \circledast f)_{\underline{y^{\prime\prime}}}\,,$ where • $\underline{y^{\prime\prime}}$ runs through all sequences $y_{1},\ldots,y_{m+n}$ with $y_{t}\in\left[G/G_{i_{t}}\right]$ for all $t$ • and $(f^{\prime}\circledast f)_{\underline{y^{\prime\prime}}}$ is the mapping $U\to M_{\underline{y}^{\prime\prime}}(\underline{i}^{\prime\prime};W)$ such that, for all $u\in U$, $\begin{array}[]{rcl}(f^{\prime}\circledast f)_{\underline{y^{\prime\prime}}}(u% )&=&f^{(1)}_{\underline{y}}(u)\otimes\cdots\otimes f^{(m)}_{\underline{y}}(u)% \otimes\\ &&\,{}^{y_{1}\cdots y_{m}}(f_{\underline{y^{\prime}}}^{\prime(1)}(f^{(0)}_{% \underline{y}}(u)))\otimes\cdots\otimes\,^{y_{1}\cdots y_{m}}(f_{\underline{y^% {\prime}}}^{\prime(n)}(f^{(0)}_{\underline{y}}(u)))\otimes\\ &&y_{1}\cdots y_{m+n}f_{\underline{y^{\prime}}}^{\prime(0)}(f^{(0)}_{% \underline{y}}(u))\,,\end{array}$ where $\underline{y}=y_{1},\ldots,y_{m}$ and $\underline{y^{\prime}}=y_{m+1},\ldots,y_{m+n}$.
Definition 3.4. Let $G$ and $H$ be finite groups whose orders are not divisible by $\mathrm{char}(\mathbbm{k})$. Let ${}_{G}X_{H}$, ${}_{H}V$, and ${}_{G}U$ be (bi)modules such that $X$ and $V$ are finite dimensional. Assume that $U$ is simple. Define a pairing $\begin{array}[]{crcl}\langle-|-\rangle\colon&\operatorname{Hom}_{G}(U,X\otimes% _{H}V)\otimes_{\mathbbm{k}}\operatorname{Hom}_{H}(V,X^{*}\otimes_{G}U)&\to&% \mathbbm{k}\\ \end{array}$ as follows. For all $f\in\operatorname{Hom}_{G}(U,X\otimes_{H}V)$ and $\phi\in\operatorname{Hom}_{H}(V,X^{*}\otimes_{G}U)$, $\langle f|\phi\rangle\cdot\operatorname{Id}_{U}=\Psi_{X}^{V,U}(\phi)\circ f$ (following Lemma 3.2, the pairing $\langle-|-\rangle$ is non degenerate).
Definition 1.7. An intertwiner relative to $M$ is a morphism of $\mathbbm{k}G_{i_{0}}$-modules $U\to M(\underline{i};V)$, for some sequence $\underline{i}=i_{0},\ldots,i_{n}$ in $Q_{0}$ and some modules $U\in\mathrm{mod}(\mathbbm{k}G_{i_{0}})$ and $V\in\mathrm{mod}(\mathbbm{k}G_{i_{n}})$.
Definition 3.5. Let $G$, $H$, and $K$ be finite groups whose orders are not divisible by $\mathrm{char}(\mathbbm{k})$. Let ${}_{G}X_{H}$, ${}_{H}Y_{K}$, ${}_{G}U$, ${}_{H}V$, and ${}_{K}W$ be (bi)modules. For all $f_{1}\in\operatorname{Hom}_{G}(U,X\otimes_{H}V)$ and $f_{2}\in\operatorname{Hom}_{H}(V,Y\otimes_{K}W)$, define $f_{2}\times f_{1}\in\operatorname{Hom}_{G}(U,X\otimes_{H}Y\otimes_{K}W)$ as the following composite morphism $f_{2}\times f_{1}\colon U\xrightarrow{f_{1}}X\otimes_{H}V\xrightarrow{(X% \otimes_{H}f_{2})}X\otimes_{H}Y\otimes_{K}W\,.$
Definition 1.10. The algebra of intertwiners relative to $M$ is denoted by $\operatorname{\mathcal{I}ntw}$ and defined by $\operatorname{\mathcal{I}ntw}=\left(\bigoplus_{n,\underline{i},U,V}% \operatorname{Hom}_{\mathbbm{k}G_{i_{0}}}(U,M(\underline{i};V)),\circledast% \right)\,,$ where $n$ runs through all non negative integers, $\underline{i}=i_{0},\ldots,i_{n}$ runs through all sequences in $[G\backslash Q_{0}]$, and $U$ and $V$ run through $\operatorname{irr}(G_{i_{0}})$ and $\operatorname{irr}(G_{i_{n}})$, respectively.
Definition 4.7. Let $\underline{i}=i_{0},\ldots,i_{n}$ be a sequence in $Q_{0}$, where $n\geqslant 1$. Let $U_{0}\in\mathrm{mod}(\mathbbm{k}G_{i_{0}})$ be simple. Let $U_{n}\in\mathrm{mod}(\mathbbm{k}G_{i_{n}})$. Define $(-|-)$ to be the non degenerate pairing $(-|-)\colon\operatorname{Hom}_{G_{i_{0}}}(U_{0},M(\underline{i};U_{n}))\otimes% _{\mathbbm{k}}\operatorname{Hom}_{G_{i_{n}}}(U_{n},M^{*}(\underline{i}^{o};U_{% 0}))\to\mathbbm{k}\,,$ such that, for all $f\in\operatorname{Hom}_{G_{i_{0}}}(U_{0},M(\underline{i};U_{n}))$ and $\varphi\in\operatorname{Hom}_{G_{i_{n}}}(U_{n},M^{*}(\underline{i}^{o},U_{0}))$, $(f|\varphi)\cdot\operatorname{Id}_{U_{0}}=\Phi(\varphi)\circ f\,;$
Definition 2.2. Let $R\subset\mathbb{R}$ denote any unital subring. (1) Let ${\tt Semi}$ denote the category of abelian topological semigroups $(\mathscr{G},+,d_{\mathscr{G}})$ with translation-invariant metric $d_{\mathscr{G}}$, as above, and whose morphisms are semigroup homomorphisms that are Lipschitz. (2) Let ${\tt Mon}$ denote the full subcategory of ${\tt Semi}$, whose objects are monoids. (3) Let $R$-${\tt Mod}$ denote the subcategory of ${\tt Semi}$, whose objects are $R$-modules such that multiplication by scalars in $R$ are Lipschitz maps, and whose morphisms are Lipschitz $R$-module maps. (4) Let $R$-${\tt NMod}$ denote the full subcategory of $R$-${\tt Mod}$, whose objects are $R$-normed $R$-modules $\mathscr{G}$. In other words, $d_{\mathscr{G}}(0,ra)=|r|d_{\mathscr{G}}(0,a)$ for all $r\in R$ and $a\in\mathscr{G}$. (5) Let $\overline{R}$-${\tt Mod}\subset R$-${\tt Mod}$ (note the unconventional notation) denote the full subcategory of $R$-${\tt Mod}$ whose objects are complete metric spaces; and similarly define $\overline{R}$-${\tt NMod}\subset R$-${\tt NMod}$.
Definition 3.20. We write $\mathcal{E}^{(n)}_{4}$ for the fusion category of $A^{(n)}$-modules in the appropriate Galois conjugate of $\mathcal{C}(\mathfrak{sl}_{4},4)$.
Definition 2.2. We say an object $X\in\mathcal{C}$ is $K$-normal if for all $k\geq K$, we have $X^{\otimes k}\otimes{X^{*}}^{\otimes k}\cong(X\otimes X^{*})^{k}.$
Definition 2.9. We say two $G$-graded categories $\mathcal{C}_{1}$ and $\mathcal{C}_{2}$ are equivalent, up to twisting the associator, if there exists a 3-cocycle $\omega\in H^{3}(G,\mathbb{C}^{\times})$ such that there exists a monoidal equivalence $\mathcal{C}_{1}\simeq\mathcal{C}_{2}^{\omega}.$
Definition 2.7. Let $\mathcal{M}$ be a $\mathcal{C}$-bimodule category, and $\mathcal{F}\in\operatorname{Eq}(\mathcal{C})$ a monoidal auto-equivalence. We define a new bimodule ${}_{\mathcal{F}}\mathcal{M}$, which is equal to $\mathcal{M}$ as a right module category, and with left action given by $X\triangleright_{{}_{\mathcal{F}}\mathcal{M}}m:=\mathcal{F}(X)\triangleright_{% M}m.$ The structure morphisms for ${}_{\mathcal{F}}\mathcal{M}$ consist of a combination of the structure morphisms for $\mathcal{M}$, and the tensorator of $\mathcal{F}$.
Definition 2.4. We write $\operatorname{Vec}^{-}(\mathbb{Z}_{2M})$ for the category with $\operatorname{Vec}(\mathbb{Z}_{2M})$ fusion rules, and associator given by $\alpha_{n_{1},n_{2},n_{3}}:=\begin{cases}1,\quad\hskip 7.5pt\text{ if }n_{2}+n% _{3}<n_{1},\\ e^{2i\pi\frac{n_{1}}{M}}\text{ if }n_{2}+n_{3}\geq n_{1}.\end{cases}$
Definition 2.1. We say an object $X\in\mathcal{C}$ is normal if $X\otimes X^{*}\cong X^{*}\otimes X$.
Definition 2.6. [27] A left module category $\mathcal{M}$ over a fusion category $\mathcal{C}$ is a semi-simple $\mathbb{C}$-linear category along with a functor $\otimes:\mathcal{C}\times\mathcal{M}\to\mathcal{M}$, and natural isomorphisms $(X\otimes Y)\otimes M\to X\otimes(Y\otimes M)$ satisfying a straightforward pentagon equation.
Definition 2.3. Let $\mathcal{D}$ be a monoidal subcategory of $\mathcal{C}$ with a lift to a copy of $\operatorname{Rep}(G)$ in the centre. We define $\mathcal{C}_{\mathcal{D}}$, the de-equivariantization of $\mathcal{C}$ by $\mathcal{D}$, as the fusion category of $A$-modules in $\mathcal{C}$.
Definition 2.8. The Brauer-Picard group of $\mathcal{C}$, which we denote $\operatorname{BrPic}(\mathcal{C})$, is the group of invertible $\mathcal{C}$-bimodules with respect to the relative tensor product.
Definition 2.10. Let $\mathcal{C}$ a $\mathbb{Z}_{M_{1}}\times\mathbb{Z}_{M_{2}}$-graded fusion category. We define $\underline{\mathcal{C}}$ as the subcategory $\otimes$-generated by the objects in $\mathcal{C}_{1\times 1}$.
Definition 3.17. We write $\mathcal{E}^{(n,\pm)}_{16,6}$ for the fusion category of $(A^{(n)},\pm)$-modules in the appropriate Galois conjugate of $\mathcal{C}(\mathfrak{sl}_{2},16)\boxtimes\mathcal{C}(\mathfrak{sl}_{3},6)$.
Definition 7.7 (The cotangent complex). In the above notation, define the cotangent complex to be the class $[\mathcal{L}_{R}]\in\operatorname{Ho}(\mathbf{QCoh}(S_{{\boldsymbol{\cdot}}}))$, where the $S_{{\boldsymbol{\cdot}}}$-module $\mathcal{L}_{R}$ is defined by: • $\mathcal{L}_{R,\alpha}=\Omega_{R_{\alpha}}\otimes_{R_{\alpha}}S_{\alpha}$ for every $\alpha\in\mathcal{N}$, • for every $\alpha\leq\beta$ the map $l_{\alpha\beta}\colon\mathcal{L}_{R,\alpha}\otimes_{S_{\alpha}}S_{\beta}\to% \mathcal{L}_{R,\beta}$ is obtained applying the functor $\Omega_{-}\otimes_{-}S_{\beta}$ to the map $R_{\alpha}\to R_{\beta}$.
Definition 3.4. Given a thickening $p\colon A\to K$ in $\mathbf{M}$ and a fibrant diagram $X\in(\mathbf{M}_{K})^{\mathcal{C}}=(K\downarrow\mathbf{M})^{\mathcal{C}}=% \Delta K\downarrow\mathbf{M}^{\mathcal{C}},$ a deformation of $X$ along $p$ is a commutative square $\xymatrix{\Delta A\ar[d]^{p}\ar[r]^{f}&\mathcal{X}\ar[d]\\ \Delta K\ar[r]&X}$ such for every $a\in\mathcal{C}$ the map $f_{a}\colon A\to\mathcal{X}_{a}$ is flat and the map $\mathcal{X}_{a}\amalg_{A}K\to X_{a}$ is a weak equivalence.
Definition 6.1. A Reedy-Palamodov resolvent of $X$, relative to an affine open cover with nerve $\mathcal{N}$ is a Reedy cofibrant resolution of the diagram $S_{{\boldsymbol{\cdot}}}$ of (6.1).
Definition 2.1 (Definitions 2.9 and 2.13 of [19]). A morphism $A\to B$ in a model category $\mathbf{M}$ is called flat if the push-out functor $-\amalg_{A}B\colon\mathbf{M}_{A}\to\mathbf{M}_{B}$ preserves pull-back squares of trivial fibrations. The model category $\mathbf{M}$ is said to be strong left proper if every cofibration is flat.
Definition 2.2 (Definition 4.2 of [19]). Let $\mathbf{M}$ be a left proper model category. A morphism $A\to K$ is said to be a thickening if for every commutative diagram $\xymatrix{A\ar@/_{1}pc/[dr]^{f}\ar@{->}[r]^{g}&E\ar[d]^{h}\\ &D}$ such that $f,g$ are flat and $h\amalg\operatorname{Id}_{K}\colon E\amalg_{A}K\to D\amalg_{A}K$ is a weak equivalence (respectively: an isomorphism), then also $h$ is a weak equivalence (respectively: an isomorphism).
Definition 7.1 ([20, Definition 3.12]). In the above notation, an $S_{{\boldsymbol{\cdot}}}$-module $\mathcal{F}\in\operatorname{Mod}(S_{{\boldsymbol{\cdot}}})$ is called quasi-coherent if for every $\alpha\leq\beta$ in $\mathcal{N}$ the map $f_{\alpha\beta}\colon\mathcal{F}_{\alpha}\otimes_{S_{\alpha}}S_{\beta}\to% \mathcal{F}_{\beta}$ is a quasi-isomorphism of DG-modules over $S_{\beta}$.
Definition 2.3. Let $K\xrightarrow{f}X$ be a morphism in a left-proper model category $\mathbf{M}$, with $X$ a fibrant object. A deformation of $f$ over a thickening $A\xrightarrow{p}K$ is a commutative diagram $\xymatrix{A\ar[d]^{p}\ar[r]^{f_{A}}&X_{A}\ar[d]\\ K\ar[r]^{f}&X}$ such that $f_{A}$ is flat and the induced map $X_{A}\amalg_{A}K\to X$ is a weak equivalence. A direct equivalence is given by a commutative diagram $\xymatrix{A\ar[d]_{g_{A}}\ar[r]^{f_{A}}&X_{A}\ar[d]\\ Y_{A}\ar[r]\ar[ru]^{h}&X}$ where $h$ is a weak equivalence. Two deformations are equivalent if they are so under the equivalence relation generated by direct equivalences.
Definition 2.4 (Universal local system). The universal local system $E$ on $L$ is a local system that is uniquely determined by the following conditions: As a vector space, $E_{q}=\mathbb{K}\langle\pi^{-1}(q)\rangle$ for $q\in L$.1$E$ has finite rank because we assumed $|\Gamma|=|\pi_{1}(L,o_{L})|<\infty$ For any $y\in\pi^{-1}(q)$ and $c:[0,1]\to L$ such that $c(0)=q$, the parallel transport of $E$ satisfies $I_{c}(y)=\mathbf{c}(1)$, where $\mathbf{c}:[0,1]\to\mathbf{L}$ is the unique path such that $\pi\circ\mathbf{c}=c$ and $\mathbf{c}(0)=y$.
Definition 3.1. A cylindrical almost complex structure on $SY:=(\mathbb{R}\times Y,d(e^{r}\alpha))$ is an almost complex structure such that • $J$ is invariant under $\mathbb{R}$ action • $J(\partial_{r})=R_{\alpha}$, where $R_{\alpha}$ is the Reeb vector field of $\alpha$ • $J(\ker(\alpha))=\ker(\alpha)$ • $d\alpha(\cdot,J\cdot)|_{\ker(\alpha)}$ is a metric on $\ker(\alpha)$
1.2.2 Definition. An $\mathcal{M}$-filtration of dimensional type $d$ of $X\in\mathcal{C}$ is a finite filtration by $\mathcal{M}$-subojects $X=X_{d}\supset\cdots\supset X_{p}\supset X_{p-1}\supset\cdots\supset X_{-1}=\emptyset$ where $d\geq-1$ is an integer. We say that such an $\mathcal{M}$-filtration is good if each $(X_{p},X_{p-1})$ is a good pair with $H^{n}(X_{p},X_{p-1})=0$ if $n\neq p$.
2.3.1 Definition. We say that a cohomology $H$ in $\mathcal{A}$, provided with an external product $\kappa^{H}$, satisfies the Künneth formula if, for any $n,n^{\prime}\in\mathbb{Z}$ and for all pairs $(X,Y)$ and $(X^{\prime},Y^{\prime})$ in $\mathcal{C}^{\mathrm{good}}$, we have that $\kappa_{n,n^{\prime}}^{H}:H^{n}(X,Y)\otimes H^{n^{\prime}}(X^{\prime},Y^{% \prime})\to H^{n+n^{\prime}}(X\times X^{\prime},X\times Y^{\prime}\cup Y\times X% ^{\prime})$ is an isomorphism and $\xy\xymatrix{H^{n}(X,Y)\otimes H^{n^{\prime}}(X^{\prime},Y^{\prime})\ar[r]^{% \kappa^{H}\ \ }\ar[d]&H^{n+n^{\prime}}(X\times X^{\prime},X\times Y^{\prime}% \cup Y\times X^{\prime})\ar[d]^{\alpha^{*}}\\ H^{n^{\prime}}(X^{\prime},Y^{\prime})\otimes H^{n}(X,Y)&H^{n^{\prime}+n}(X^{% \prime}\times X,X^{\prime}\times Y\cup Y^{\prime}\times X)\ar[l]^{(\kappa^{H})% ^{-1}\ \ }}$ commutes with sign $(-1)^{nn^{\prime}}$.
1.3.1 Definition. Say that the cohomology $H$ is cellular with respect to $\mathcal{C}^{\square}$ if the following assumptions and conditions are satisfied: (i) for any object $X\in\mathcal{C}$ there exists a non-empty family of good $\mathcal{M}$-filtrations of the same dimensional type $d=\dim(X)$, (ii) for any two $\mathcal{M}$-filtrations of $X$ there is a third good $\mathcal{M}$-filtration containing the join of the given ones and (iii) if $f:X\to X^{\prime}$ is a morphism in $\mathcal{C}$ then there is a good $\mathcal{M}$-filtration on $X^{\prime}$ containing the direct image under $f$ of a good $\mathcal{M}$-filtration of $X$.
1.2.1 Definition. Let $(\mathcal{A},\otimes)$ be an abelian tensor category, with a right exact tensor $\otimes$. Define the good pairs for a model $H$ as above to be those $(X,Y)\in\mathcal{C}^{\square}$ such that there exists an integer $n$ such that $H^{m}(X,Y)=0$ for $m\neq n$ and $H^{n}(X,Y)\in\mathcal{A}^{\flat}\subset\mathcal{A}$.
Definition 3.3 By an $A$-$A$-quantale $Q$ is meant an $A$-$A$-bimodule equipped with a quantale multiplication $(x,y)\mapsto xy$ satisfying the following additional conditions for all $a\in A$ and $x,y\in Q$: $\displaystyle({{}_{a}{|}{x}})y$ $\displaystyle={{}_{a}{|}{(xy)}}\;,$ $\displaystyle({{x}{|}_{a}})y$ $\displaystyle=x({{}_{a}{|}{y}})\;,$ $\displaystyle{{(xy)}{|}_{a}}$ $\displaystyle=x({{y}{|}_{a}})\;.$
Definition 4.18 The homomorphism $\mu_{A}:Q\otimes_{A}Q\to Q$ in the above factorization will be referred to as the reduced multiplication of $Q$. By a multiplicative quantal frame is meant an $A$-$A$-quantal frame such that the right adjoint of the reduced multiplication preserves joins.
Definition 3.12 If $Q$ is a unital quantale, by a unital support on $Q$ will be meant a support in the sense of section 2.3; that is, a sup-lattice homomorphism $\operatorname{\varsigma}_{Q}:Q\to Q$ (or simply $\operatorname{\varsigma}$) satisfying the following three conditions for all $x\in Q$: $\displaystyle\operatorname{\varsigma}(x)$ $\displaystyle\leq e\;;$ $\displaystyle\operatorname{\varsigma}(x)$ $\displaystyle\leq xx^{*}\;;$ $\displaystyle x$ $\displaystyle\leq\operatorname{\varsigma}(x)x\;.$ We shall call such a quantale unitally supported. By a homomorphism of unitally supported quantales $f:Q\to R$ will be meant a homomorphism of unital involutive quantales that commutes with the unital supports; that is, such that for all $x\in Q$ we have $f(\operatorname{\varsigma}_{Q}(x))=\operatorname{\varsigma}_{R}(f(x))\;.$ The category thus obtained will be denoted by $\mathit{Qu}_{e}^{\operatorname{\varsigma}}$. (Not to be confused with $\mathit{Qu}_{(e)}^{\operatorname{\varsigma}}$, cf. Definition 3.8.)
Definition 3.5 An $A$-$A$-quantale is involutive if it is an involutive semigroup; the involution is denoted by $a\mapsto a^{*}$ and it is required to satisfy, besides the standard conditions $x^{**}=x$ and $(xy)^{*}=y^{*}x^{*}$, the following two conditions: $\displaystyle(\bigvee_{i}x_{i})^{*}$ $\displaystyle=\bigvee_{i}x^{*}_{i}\;,$ $\displaystyle({{}_{a}{|}{{{x}{|}_{b}}}})^{*}$ $\displaystyle={{}_{b}{|}{{{x^{*}}{|}_{a}}}}\;.$
Definition 3.1 Let $A$ be a locale. An $A$-$A$-bimodule $M$ is a sup-lattice equipped with two unital (resp. left and right) $A$-module structures $(a,m)\mapsto{{}_{a}{|}{m}}\quad\text{and}\quad(a,m)\mapsto{{m}{|}_{a}}$ satisfying the following additional condition for all $a,b\in A$ and $m\in M$: $\displaystyle{{({{}_{a}{|}{m}})}{|}_{b}}$ $\displaystyle={{}_{a}{|}{({{m}{|}_{b}})}}\;.$ A homomorphism of $A$-$A$-bimodules is a sup-lattice homomorphism that preserves both actions.
Definition 4.5 By a principal quantale $Q$ will be meant an equivariantly supported quantal frame $Q$ satisfying the equivalent conditions of the following Theorem 4.6.
Definition 3.7 By a based quantale will be meant an involutive quantale $Q$ equipped with the structure of an involutive $Q_{0}$-$Q_{0}$-quantale for some locale $Q_{0}$. A homomorphism of based quantales $f:Q\to R$ consists of a pair $(f_{1},f_{0})$ where $f_{1}:Q\to R$ is a homomorphism of involutive quantales and $f_{0}:Q_{0}\to R_{0}$ is a homomorphism of locales such that the following conditions hold for all $x\in Q$ and $a\in Q_{0}$: $\displaystyle f_{1}\bigl{(}{{}_{a}{|}{x}}\bigr{)}$ $\displaystyle={{}_{f_{0}(a)}{|}{f_{1}(x)}}\;;$ $\displaystyle f_{1}\bigl{(}{{x}{|}_{a}}\bigr{)}$ $\displaystyle={{f_{1}(x)}{|}_{f_{0}(a)}}\;.$ The category thus obtained is called the category of based quantales and it is denoted by $\mathit{Qu}^{\flat}$. By a strong homomorphism of based quantales will be meant a homomorphism $f$ such that $f_{1}$ is a strong homomorphism of involutive quantales. The subcategory of $\mathit{Qu}^{\flat}$ whose homomorphisms are strong is denoted by $\mathit{Qu}_{1}^{\flat}$.
Definition 4.1 By a based quantal frame is meant a based quantale $Q$ such that for all $x,y,y_{i}\in Q$ and $a\in Q_{0}$ the following properties hold: $\displaystyle x\wedge\bigvee_{i}y_{i}$ $\displaystyle=\bigvee_{i}x\wedge y_{i}\;,$ $\displaystyle({{}_{a}{|}{x}})\wedge y$ $\displaystyle={{}_{a}{|}{(x\wedge y)}}\;,$ $\displaystyle({{x}{|}_{a}})\wedge y$ $\displaystyle={{(x\wedge y)}{|}_{a}}\;.$ By a supported quantal frame, stably suported quantal frame, and equivariantly supported quantal frame, is meant a based quantal frame equipped with a support, a stable support, and an equivariant support, respectively.
Definition 3.14 Let $Q$ be a supported quantale. The support $\operatorname{\varsigma}$ is stable if $\operatorname{\varsigma}{(xy)}\leq\operatorname{\varsigma}(x)$ for all $x,y\in Q$. In this case $Q$ is called stably supported.
Definition 5.1 Let $Q$ be a multiplicative equivariantly supported reflexive quantal frame $(Q,\operatorname{\varsigma},\upsilon)$. We say that $Q$ satisfies unit laws if for all $a\in Q$ the following condition holds: $\bigvee_{xy\leq a}({{}_{\upsilon(x)}{|}{y}})=a\,.$
Definition 3.8 A based quantale $Q$ is supported if it is equipped with a sup-lattice homomorphism $\operatorname{\varsigma}_{Q}:Q\rightarrow Q_{0}$ (denoted simply $\operatorname{\varsigma}$ if no confusion will arise), called the support, which satisfies the following conditions for all $x,y\in Q$: $\displaystyle\operatorname{\varsigma}(1_{Q})$ $\displaystyle=1_{Q_{0}}\;,$ $\displaystyle{{}_{\operatorname{\varsigma}(x)}{|}{y}}$ $\displaystyle\leq xx^{*}y\;,$ $\displaystyle{{}_{\operatorname{\varsigma}(x)}{|}{x}}$ $\displaystyle=x\;.$ By a homomorphism $f:Q\to R$ of supported quantales is meant a homomorphism of based quantales $(f_{1},f_{0})$ that commutes with the supports; that is, such that for all $x\in Q$ we have $f_{0}(\operatorname{\varsigma}_{Q}(x))=\operatorname{\varsigma}_{R}(f_{1}(x))\;.$ The category thus obtained is called the category of supported quantales and it is denoted by $\mathit{Qu}^{\operatorname{\varsigma}}$. We shall denote by $\mathit{Qu}_{1}^{\operatorname{\varsigma}}$ the subcategory of $\mathit{Qu}^{\operatorname{\varsigma}}$ whose homomorphisms are strong, and by $\mathit{Qu}_{(e)}^{\operatorname{\varsigma}}$ the subcategory of $\mathit{Qu}^{\operatorname{\varsigma}}$ whose objects are unital quantales and whose homomorphisms are unital.
Definition 3.16 Let $Q$ be a based quantale. A support $\operatorname{\varsigma}$ is said to be equivariant if for all $a\in Q_{0}$ and $x\in Q$ we have $\operatorname{\varsigma}({{}_{a}{|}{x}})=a\wedge\operatorname{\varsigma}(x)\;.$ A supported quantale is called equivariantly supported whenever the support is equivariant. The full subcategory of $\mathit{Qu}^{\operatorname{\varsigma}}$ whose objects are the equivariantly supported quantales is denoted by $\mathit{Qu}^{\textrm{eq}}$. The subcategory of $\mathit{Qu}^{\textrm{eq}}$ whose homomorphisms are strong is denoted by $\mathit{Qu}_{1}^{\textrm{eq}}$, and the subcategory of $\mathit{Qu}^{\textrm{eq}}$ whose objects are unital quantales and whose homomorphisms are unital is denoted by $\mathit{Qu}_{e}^{\textrm{eq}}$.
Definition 5.4 By a groupoid quantale $Q$ will be meant a multiplicative equivariantly supported reflexive quantal frame that satisfies unit laws and moreover satisfies the following condition, referred to as the inverse law, for all $a\in Q$: ${{}_{\upsilon(a)}{|}{1_{Q}}}=\bigvee_{xy^{*}\leq a}x\wedge y\;.$
Definition 4.10 By a reflexive quantal frame $(Q,\upsilon)$ will be meant an $A$-$A$-quantal frame equipped with a frame homomorphism $\upsilon:Q\to A$ that for all $a\in A$ satisfies $\upsilon({{}_{a}{|}{1_{Q}}})=a=\upsilon({{1_{Q}}{|}_{a}})\;.$
Definition 2.1. Let $M$ be a monoid and $X$ a right $M$-set. The theory for $(X,M)$-graphs, $\mathbb{G}_{(X,M)}$, has two objects $V$ and $A$ with homsets given by $\mathbb{G}_{(X,M)}(V,A)\vcentcolon=X,\ \ \ \mathbb{G}_{(X,M)}(A,V)\vcentcolon=% \varnothing,\ \ \ \mathbb{G}_{(X,M)}(V,V)\vcentcolon=\{\operatorname{id}_{V}\}% ,\ \ \ \mathbb{G}_{(X,M)}(A,A)\vcentcolon=M.$ Composition is defined as $m\circ x=x.m$ (the right-action via $M$), $m\circ m^{\prime}=m^{\prime}m$ (monoid operation of $M$). The category of $(X,M)$-graphs is defined to be the category of presheaves $\widehat{\mathbb{G}}_{(X,M)}\vcentcolon=[\mathbb{G}_{(X,M)}^{op},\mathbf{Set}]$.
Definition 2.1. 1. Let $M$ be a monoid and $X$ a right $M$-set. The theory for $(X,M)$-graphs, $\mathbb{G}_{(X,M)}$, is the category with two objects $V$ and $A$ and homsets given by $\displaystyle\mathbb{G}_{(X,M)}(V,A)\vcentcolon=X,$ $\displaystyle\mathbb{G}_{(X,M)}(A,V)\vcentcolon=\varnothing,$ $\displaystyle\mathbb{G}_{(X,M)}(V,V)\vcentcolon=\{\operatorname{id}_{V}\},$ $\displaystyle\mathbb{G}_{(X,M)}(A,A)\vcentcolon=M.$ Composition is defined as $m\circ x=x.m$ (the right-action via $M$), $m\circ m^{\prime}=m^{\prime}m$ (monoid operation of $M$). 2. Let $M$ be a monoid such that the set $\operatorname{Fix}(M)\vcentcolon=\left\{\,m^{\prime}\in M\,\middle|\,\forall m% \in M,m^{\prime}m=m^{\prime}\,\right\}$ is non-empty. Let $X\vcentcolon=\left\{\,x_{m^{\prime}}\,\middle|\,m^{\prime}\in\operatorname{Fix% }(M)\,\right\}$ be the right $M$-set with right-action $x_{m}.m^{\prime}\vcentcolon=x_{mm^{\prime}}$ for each $m\in M$ and $x_{m^{\prime}}\in X$. The theory for reflexive $(X,M)$-graphs, $\operatorname{r\mathbb{G}}_{(X,M)}$ is the same as for $\mathbb{G}_{(X,M)}$ but with $\displaystyle\operatorname{r\mathbb{G}}_{(X,M)}(A,V)\vcentcolon=\{\ell\},$ and composition $\ell\circ m=\ell$, $\ell\circ x_{m^{\prime}}=\operatorname{id}_{V}$, and $x\circ\ell=x$ for each $m\in M$ and $m^{\prime}\in\operatorname{Fix}(M)$. The category of $(X,M)$-graphs (resp. reflexive $(X,M)$-graphs) is defined to be the category of presheaves $\widehat{\mathbb{G}}_{(X,M)}\vcentcolon=[\mathbb{G}_{(X,M)}^{op},\mathbf{Set}]$ (resp. $\widehat{\operatorname{r\mathbb{G}}}_{(X,M)}\vcentcolon=[\operatorname{r% \mathbb{G}}_{(X,M)}^{op},\mathbf{Set}].$)
Definition 3.2.2 Let $X$ be smooth projective variety and let $\mathscr{A}$ semi-orthogonal component of $\mathrm{D^{b}}(X)$ which is Calabi-Yau of dimension $3$ and connected (that is $\mathrm{HH}_{-3}(\mathscr{A})=\mathbb{C}$). Let $\mathrm{rank}$ be a non-trivial rank function on $\mathscr{A}$ and assume that a homological unit for $\mathscr{A}$ with respect to $\mathrm{rank}$ is $\mathbb{C}\oplus\mathbb{C}[3]$. Assume furthermore that hypothesis 3.2.1 is satisfied. We define the Hodge spaces of $\mathscr{A}$ as: 1. $H^{3,0}(\mathscr{A})=\mathrm{HH}_{-3}(\mathscr{A})=\mathbb{C}$, $H^{0,0}(\mathscr{A})=\mathbb{C}\subset\mathrm{HH}_{0}(\mathscr{A})$, $H^{1,0}(\mathscr{A})=H^{2,0}(\mathscr{A})=0$. 2. $H^{3,1}(\mathscr{A})=\mathrm{HH}_{-2}(\mathscr{A})$, $H^{3,2}(\mathscr{A})=0$ and $H^{2,1}(\mathscr{A})=\mathrm{HH}_{-1}(\mathscr{A})$. 3. We choose $V_{1}$ a maximal isotropic subspace of $\mathrm{HH}_{0}(\mathscr{A})$ (for $\omega_{\mathrm{HH}_{0}(\mathscr{A})}$) containing $H^{0,0}(\mathscr{A})$ and $V_{2}$ a maximal isotropic subspace in $\mathrm{HH}_{0}(\mathscr{A})$ which is complementary to $V_{1}$. We let $H^{1,1}(\mathscr{A})$ be a complementary subspace of $H^{0,0}(\mathscr{A})$ in $V_{1}$. We let $H^{3,3}(\mathscr{A})$ be a line in $V_{2}$ and $H^{2,2}(\mathscr{A})$ be a complementary subspace of $H^{3,3}(\mathscr{A})$ in $V_{2}$. 4. $H^{p,q}(\mathscr{A})=\mathrm{c}_{\mathscr{A}}(H^{q,p}(\mathscr{A}))$ for any $(p,q)\in[0,\ldots,3]$ such that $p<q$.
Definition 3.1.1 Let $\mathscr{A}$ be a smooth proper triangulated category which is Calabi-Yau of dimension $3$. Let $\mathrm{rank}$ be a non-trivial rank function on $\mathscr{A}$ and let $\mathfrak{T}_{\mathscr{A}}^{\bullet}$ be a homological unit for $\mathscr{A}$ with respect to $\mathrm{rank}$. We define the Hodge numbers of $\mathscr{A}$ as: 1. for all $i\in[0,\ldots,3]$, $h^{i,0}(\mathscr{A})=\mathfrak{T}_{\mathscr{A}}^{3-i}$, 2. $h^{3,1}(\mathscr{A})=\dim\mathrm{HH}_{-2}(\mathscr{A})-h^{2,0}(\mathscr{A})$, 3. $h^{3,2}(\mathscr{A})=h^{1,0}(\mathscr{A})$ and $h^{2,1}(\mathscr{A})=\dim\mathrm{HH}_{-1}(\mathscr{A})-h^{1,0}(\mathscr{A})-h^% {3,2}(\mathscr{A})$, 4. $h^{3,3}(\mathscr{A})=h^{0,0}(\mathscr{A})$ and $h^{1,1}(\mathscr{A})=h^{2,2}(\mathscr{A})=\dfrac{\dim\mathrm{HH}_{0}(\mathscr{% A})-h^{0,0}(\mathscr{A})-h^{3,3}(\mathscr{A})}{2}$. 5. $h^{p,q}(\mathscr{A})=h^{q,p}(\mathscr{A})$ for any $p,q\in[0,\ldots,3]$.
