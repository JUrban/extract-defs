Definition 3.3. A set of $A$-algebras $\{A\to B_{i}\}_{i\in I}$ is a fppf cover of $A$ if it is a faithful cover and every $B_{i}$ is a perfect $A$-module.
Definition 2.9. (Algebras and Modules) (1) An (associative) algebra object in a monoidal category $\mathcal{C}$ is a simplicial object $A:N(\Delta)^{op}\to\mathcal{C}$ so that $A([0])$ is a final object and $A([n])\simeq A([1])\otimes\ldots\otimes A([1])$. The $\infty$-category of (associative) algebra objects in $\mathcal{C}$, denoted by $\operatorname{Alg}(\mathcal{C})$ is the full subcategory of $\operatorname{Fun}(N(\Delta)^{op},\mathcal{C})$ spanned by algebra objects in $\mathcal{C}$. (2) An $\infty$-category $M$ is left tensored over a monoidal category $\mathcal{C}$ if there is an action $\mathcal{C}\times\mathcal{M}\to\mathcal{M}$ which is defined upto coherent homotopies. This is encoded as a simplicial object $f:N(\Delta)^{op}\to\operatorname{Cat}_{\infty}$ so that $f([n])\simeq\mathcal{C}^{n}\times\mathcal{M}$ and $f([0])\simeq\mathcal{M}$. (3) Given a $\infty$-category $\mathcal{M}$ which is left tensored over a monoidal $\infty$-category $\mathcal{C}$, there is an $\infty$-category of left module objects in $\mathcal{M}$ denoted by $\operatorname{LMod}(\mathcal{M})$, It is the fullsubcategory of $\operatorname{Fun}(N(\Delta)^{op},\mathcal{M})$ spanned by simplicial objects $M:N(\Delta)^{op}\to\mathcal{M}$ where $M([n])\simeq A^{\otimes_{n}}\otimes M$ and $M([0])\simeq M$. Here $A\in\operatorname{Alg}(\mathcal{C})$ and $M\in\mathcal{M}$. There is a map of $\infty$-categories $\operatorname{LMod}(\mathcal{M})\to\operatorname{Alg}(\mathcal{C}).$ If $A\in\operatorname{Alg}(\mathcal{C})$ we let $\operatorname{LMod}_{A}(\mathcal{M})$ denote the the fiber $\operatorname{LMod}(\mathcal{M})\times_{\operatorname{Alg}(\mathcal{C})}\{A\}$. We refer to $\operatorname{LMod}_{A}(\mathcal{M})$ as the $\infty$-category of left $A$-modules in $\mathcal{M}$. (4) Given an $\infty$-category $M$ which is right tensored over a monoidal category $\mathcal{C}$ and $A\in\operatorname{CAlg}(\mathcal{C})$, the $\infty$-categories $\operatorname{RMod}_{\mathcal{C}}(\mathcal{M})$ and $\operatorname{RMod}_{A}(\mathcal{M})$ are similarly defined.
Definition 3.7. A derived stack is perfect if it has affine diagonal and the $\infty$-category $QC(X)$ is the inductive limit $QC(X)\simeq\operatorname{Ind}\,\operatorname{Perf}(X)$ of the full $\infty$-category of perfect complexes.
Definition 2.17. ([4, 5.5]) An $\infty$-category is presentable if it is closed under all small colimits (also limits by Prop.5.5.2.4 [4] and more over are generated in a weak sense by a small category (accessible). Presentable $\infty$-categories form an $\infty$-category $Pr^{L}$ whose morphism are continous funtors, i.e. functors that preserve all small colimits, $\operatorname{Map}_{Pr^{L}}(\mathcal{C},\mathcal{D})=\operatorname{Fun}^{L}(% \mathcal{C},\mathcal{D})$. There is another $\infty$-category $Pr^{R}$ of presentable $\infty$-categories and functors preserving small limits. There is an equivalence $Pr^{L}\simeq(Pr^{R})^{op}$ of $\infty$-categories.
Definition 2.4. Let $p:\mathcal{C}\to\mathcal{D}$ be a functor between $\infty$-categories. (1) Given an edge $f:x\to y$ in $\mathcal{D}$, we say $f$ is $p$-cartesian if the canonical map $\mathcal{C}_{/x}\to\mathcal{D}_{/p(x)}\times_{\mathcal{D}_{/p(y)}}\mathcal{C}_% {/y}$ is an equivalence of $\infty$-categories. The edge $f:x\to y$ is said to the co-cartesian lift of $p(f)$ relative to $x$. (2) The functor $p:\mathcal{C}\to\mathcal{D}$ is a co-cartesian fibration if every object in $\operatorname{Fun}([1],\mathcal{D})\times_{s,\mathcal{D},p}\mathcal{C}$ has a co-cartesian lift. There is a dual notion of cartesian fibrations. A functor $p:\mathcal{C}\to\mathcal{D}$ is cartesian if $p^{op}:\mathcal{C}^{op}\to\mathcal{D}^{op}$ is co-cartesian.
Definition 2.24. Let $F:\mathcal{C}\to\mathcal{D}$ be a map of $\infty$-categories that admits a right adjoint $G$ and let $K=F\circ G$ be the composition comonad on $\mathcal{D}$. Then $F$ is said to be comonadic if the comparison map $F^{\prime}:\mathcal{C}\to\hbox{LComod}_{K}(\mathcal{D})$ is an equivalence of $\infty$-categories.
Definition 2.7. (Commutative monoids) For every $n\geq 0$ let $\langle n\rangle^{0}=\{1,\ldots,n\}$ and $\langle n\rangle=\langle n\rangle^{0}_{*}=\{*,1,2,\ldots,n\}$ the pointed set obtained by adjoining the basepoint $*$ to $\langle n\rangle$. The category $\Gamma$ has as objects $\langle n\rangle$ and given a morphism from $\langle m\rangle$ to $\langle n\rangle$ in $\Gamma$ is a map $\alpha:\langle m\rangle\to\langle n\rangle$ so that $\alpha(*)=*$. Let $\mathcal{C}$ be an $\infty$-category closed under finite limits. A commutative monoid in $\mathcal{C}$ is a functor $f:N(\Gamma)\to\mathcal{C}$ so that $f\langle 0\rangle$ is a final object and $f(\langle n\rangle)\simeq f(\langle 1\rangle)\times\ldots\times f(\langle 1\rangle)$.
Definition 3.5. A derived stack $X$ is algebraic if it can be covered by a derived affine scheme $\operatorname{Spec}(A)$ and if it has affine diagonal. Equivalently, there exists a cosimplicial object $A^{\bullet}$ in $\operatorname{CAlg}_{R}$, so that $X$ is equivalent to the colimit of the simplicial derived affine scheme $\operatorname{Spec}(A^{\bullet})$ in $\mathcal{LF}\operatorname{ib}(\operatorname{CAlg}_{R})$.
Definition 2.15. (Bi-algebras and Hopf algebras) (1) A commutative bi-algebra in a symmetric monoidal category $\mathcal{C}$ is an object in $\operatorname{CoAlg}(\operatorname{CAlg}(\mathcal{C}))$. Given $A\in\operatorname{CAlg}(\mathcal{C})$, a commutative bi-algebra over $A$ in $\mathcal{C}$ is an object in $\operatorname{CoAlg}(\operatorname{CAlg}_{A})$. We shall denote the $\infty$-categories by $\operatorname{BiAlg}(\mathcal{C})$ and $\operatorname{BiAlg}_{A}(\mathcal{C})$ respectively. (2) Given a commutative bi-algebra $A$ in $\mathcal{C}$, this can be expressed as a functor $f:N(\Delta)\to\operatorname{CAlg}(\mathcal{C})$. We say that $A$ is a commutative Hopf algebra object in $\mathcal{C}$ if $f^{op}:N(\Delta)^{op}\to\operatorname{Aff}(\mathcal{C})$ defines a group object in $\operatorname{Aff}(\mathcal{C})$. The $\infty$-category of commutative Hopf algebra objects in a symmetric monoidal category $\mathcal{C}$ is the full subcategory of $\operatorname{BiAlg}(\mathcal{C})$ spanned by Hopf algebra objects. Denote this $\infty$-category by $\operatorname{CHopf}(\mathcal{C})$. Given $A\in\operatorname{CAlg}(\mathcal{C})$, define the $\infty$-category of commutative Hopf algebra objects over $A$ to be the full subcategory of $\operatorname{BiAlg}_{A}$ spanned by commutative Hopf algebra objects.
Definition 4.2. Let $\mathsf{B}G$ be the classifying space of $G$. This is an $\infty$-groupoid. Then a $G$-action on $X$ is a functor of $\infty$-categories $f:\mathsf{B}G\to\mathcal{C}$ so that the object in $\mathsf{B}G$ maps to $X\in\mathcal{C}$. Alternatively, let $\mathsf{B}\operatorname{Aut}_{X}(\mathcal{C})\subseteq\mathcal{C}$ be the full sub $\infty$-groupoid spanned by $X$. Then a $G$ action on $X$ is a map of $\infty$-groupoids $\mathsf{B}G\to\mathsf{B}\operatorname{Aut}_{X}(\mathcal{C}).$ The $\infty$-category $\operatorname{Fun}(\mathsf{B}G,\mathcal{C})$ is the category of $G$-objects in $\mathcal{C}$. Define $X^{hG}=\lim(f)$. The simpicial model for $\mathsf{B}G$ gives rise to a group cobar complex $C^{\bullet}(G;X)$, which is a cosimplicial object in $\mathcal{C}$. The fixed points $X^{hG}\simeq\operatorname{Tot}(C^{\bullet}(G;X))$.
Definition 2.22. An object in $\operatorname{CAlg}(Pr^{L,\sigma})$ corresponds to a symmetric monoidal $\infty$-category $\mathcal{C}^{\otimes}$ whose underlying category $\mathcal{C}^{\otimes}_{[1]}\simeq\mathcal{C}$ is presentable and stable, and the product $\mathcal{C}\times\mathcal{C}\to\mathcal{C}$ preserves colimits separately in each variable. We shall call $\operatorname{CAlg}(Pr^{L,\sigma})$ the $\infty$-category of symmetric monoidal presentable stable $\infty$-categories.
Definition 2.5. (Monoid objects) Let $\mathcal{C}$ be an $\infty$-category which admits finite limits. A monoid object in $\mathcal{C}$ is a simplcial object $f:N(\Delta)^{op}\to\mathcal{C}$ having the property that $f([0])$ is a final object, and for each $n\in\mathbb{N}$, the inclusions $\{i-1,i\}\to[n]$ for $1\leq i\leq n$ induce the equivalence $f([n])\to f([1])\times\ldots\times f([1])$ where the right hand side in the $n$-fold product. Here $f([1])$ is thought of as underlying object of the monoid. Denote by $\operatorname{Mon}(\mathcal{C})$ the full subcategory of $\operatorname{Fun}(N(\Delta)^{op},\mathcal{C})$ spanned by monoid objects.
Definition 2.19. The $\infty$-category $Pr^{L}$ is both left and right tensored over $Pr^{L}$ by the tensor product of presentable $\infty$-categories. Denote by $\operatorname{LMod}(Pr^{L})$ and $\operatorname{RMod}(Pr^{L})$ the $\infty$-categories of left and right module objects respectively. Given a presentable monoidal $\infty$-category $\mathcal{C}$, denote by $\operatorname{LMod}_{\mathcal{C}}(Pr^{L})=\operatorname{LMod}(Pr^{L})\times_{% \operatorname{Alg}(\mathcal{C})}\{\mathcal{C}\}$ and, $\operatorname{RMod}_{\mathcal{C}}(Pr^{L})=\operatorname{RMod}(Pr^{L})\times_{% \operatorname{Alg}(\mathcal{C})}\{\mathcal{C}\}$ the $\infty$-categories of left and right modules over $\mathcal{C}$ respectively.
Definition 2.3. ([4, 5.2]) A pair of $\infty$-functors $F:\mathcal{C}\to\mathcal{D}$ and $G:\mathcal{D}\to\mathcal{C}$ are adjoint if they induce equivalent $\infty$-correspondences $\mathcal{C}^{op}\times\mathcal{D}\to\mathcal{S}$. This is formulated as a fibration $p:\mathcal{M}\to\Delta^{1}$ which is both cartesian and co-cartesian and $p^{-1}\{0\}\simeq\mathcal{C}$ and $p^{-1}\{1\}\simeq\mathcal{D}$. Here $F$ is left adjoint to $G$.
Definition 2.11. Let $\mathcal{C}$ be a monoidal $\infty$-category. Let $\mathcal{M}$ be right tensored over $\mathcal{C}$ and let $\mathcal{N}$ be left tensored over $\mathcal{C}$. Let $F:\mathcal{M}\times\mathcal{N}\to\mathcal{D}$ be a balanced pairing ([DAG2]). Then there is a two-sided bar construction $\operatorname{Bar}_{\bullet}:\operatorname{RMod}(\mathcal{M})\times_{% \operatorname{Alg}(\mathcal{C})}\operatorname{LMod}(\mathcal{N})\subset% \operatorname{Fun}(N(\Delta)^{op},\mathcal{M}\times\mathcal{N})\to% \operatorname{Fun}(N(\Delta)^{op},\mathcal{D}).$ If $\mathcal{D}$ admits geometric realizations of simplicial objets, the relative tensor product can be defined as the composition $|-|\circ\operatorname{Bar}_{\bullet}:\operatorname{RMod}(\mathcal{M})\times_{% \operatorname{Alg}(\mathcal{C})}\operatorname{LMod}(\mathcal{N})\to\mathcal{D}.$
Definition 4.5. Given $\mathfrak{X}\in\operatorname{co}\mathcal{CF}\operatorname{ib}(\operatorname{% CAlg}_{R})$ classified by a pre-stack $X:\operatorname{CAlg}_{S}\to\operatorname{Cat}_{\infty}$, let $\mathsf{B}\operatorname{Aut}_{\mathfrak{X}}\to\operatorname{CAlg}_{S}$ be the a co-cartesian fibration classified by $\operatorname{CAlg}_{S}\to\operatorname{Cat}_{\infty}$ $R\mapsto\mathsf{B}\operatorname{Aut}_{X(R)}(\mathcal{S}).$ An action of a derived group scheme $\mathbb{G}$ on a pre-stack $X$ is a map of co-cartesian fibrations $\mathsf{B}\mathbb{G}\to\mathsf{B}\operatorname{Aut}_{\mathfrak{X}}$ over $\operatorname{CAlg}$.
Definition 3.2. Let $A$ be an $E_{\infty}$-ring. An $A$-module is $M$ is faithful if for every $A$-module $N$, $M\wedge_{A}N\simeq*\Rightarrow N\simeq*$. A set of $A$-algebras $\{A\to B_{i}\}_{i\in I}$ is a fpqc cover (or faithful cover) of $A$ if for each $A$-module $N$ with $N\wedge_{A}B_{i}\simeq*$ for every $i$, we have $N\simeq*$. In particular, a single faithful $A$-algebra $B$ covers $A$ in this sense.
Definition 3.13. For $X$ a simplicial set and $r\geq 1$, define the $r$-dihedral subdivision functor $\text{sbd}_{r}$ to be $\text{sbd}_{r}X=\text{sq}\left(\text{sd}_{r}\left(X\right)\right)$ Which by Propositions 3.7 and 3.12 gives a functor $\Gamma_{r}:\Delta\mathfrak{D}\textbf{-Sets}\to\textbf{sSets}$ $X\mapsto\left(\text{sbd}_{r}X\right)^{D_{r}}$
Definition 4.1. Let $\mathscr{C}$ be a simplicial category, a topology on $\mathscr{C}$ is a Grothendieck topology on the category $Ho\left(\mathscr{C}\right)$. A simplicial site $\left(\mathscr{C},\tau\right)$ is the data of $\mathscr{C}$ along with the topology $\tau$.
Definition 2.13. Table 1 gives all of the planar crossed simplicial groups along with their geometric realisations, where we consider all $M,N\in\mathbb{N}$. The groups $\widetilde{SO\left(2\right)}$ and $\widetilde{O\left(2\right)}$ are the universal covers of $SO\left(2\right)$ and $O\left(2\right)$ respectively. where we consider all $M,N\in\mathbb{N}$. The groups $\widetilde{SO\left(2\right)}$ and $\widetilde{O\left(2\right)}$ are the universal covers of $SO\left(2\right)$ and $O\left(2\right)$ respectively.
Definition 4.8. Let $\Delta\mathfrak{G}$ be a compact planar crossed simplicial group and $\mathscr{C}$ a small Grothendieck site. A strong (resp. weak) $\Delta\mathfrak{G}$ presheaf on $\mathscr{C}$ is a functor $\mathcal{F}:\mathscr{C}^{op}\to\Delta\mathfrak{G}\textbf{-Set}^{s}$ $\left(\text{resp. }\mathcal{F}:\mathscr{C}^{op}\to\Delta\mathfrak{G}\textbf{-% Set}^{w}\right)$ We will denote by $\Delta\mathfrak{G}\textbf{-Pr}^{s}\left(\mathscr{C}\right)$ (resp. $\Delta\mathfrak{G}\textbf{-Pr}^{w}\left(\mathscr{C}\right)$) the category of all strong (resp. weak) $\Delta\mathfrak{G}$-presheaves on $\mathscr{C}$.
Definition 3.27. The functor $L:\mathscr{P}\to\textbf{Top}^{SO\left(2\right)}$ takes a triple $\left(A,B,\nabla A\to B\right)$ to the pushout in the diagram \xymatrix—∇A—_—ΔC— \ar[r] \ar[d]_ζ & —B—_—ΔC— \ar[d] —A— \ar[r] & X where the map $\zeta:|\nabla A|_{|\Delta\mathfrak{C}|}\to|A|$ is the natural map which induces weak equivalences on passage to fixed point subspaces for all finite subgroups of $SO\left(2\right)$. A morphism $\left(A,B,\nabla A\to B\right)\to\left(A^{\prime},B^{\prime},\nabla A^{\prime}% \to B^{\prime}\right)$ gives rise to a commutative diagram: \xymatrix—A— \ar[d] & —∇A—_—ΔC— \ar[r] \ar[l] \ar[d]& —B—_—ΔC— \ar[d] —A’— & —∇A’—_—ΔC— \ar[l] \ar[r] & —B’—_—ΔC—
Definition 2.11. A planar crossed simplicial group is a crossed simplicial group which is of type $\Delta\mathfrak{C}$ or $\Delta\mathfrak{D}$ in the classification theorem.
Definition 4.4. A map $f:\mathscr{F}\to\mathscr{F}^{\prime}$ in $\textbf{sPr}\left(\mathscr{C}\right)$ is a local weak equivalence if: • the induced map $\widetilde{\pi_{0}}\mathscr{F}\to\widetilde{\pi_{0}}\mathscr{F}^{\prime}$ is an isomorphism of sheaves. • the induced squares \xymatrixπ_n F \ar[r] \ar[d] &\ar[d] π_n F’ F_0 \ar[r] & F’_0 are pullbacks after sheafification.
Definition 3.20. Let $\mathscr{C}$ and $\mathscr{D}$ be model categories. A functor $F:\mathscr{C}\to\mathscr{D}$ is Reedy admissible if $F$ preserves colimits and $F$ has the property that given a morphism $\left(A,B,FA\to B\right)\to\left(A^{\prime},B^{\prime},FA^{\prime}\to B^{% \prime}\right)$ in $\mathscr{C}_{F}\mathscr{D}$ such that $A^{\prime}\to A$ is a trivial cofibration in $\mathscr{C}$ and $FA^{\prime}\bigcup_{FA}B\to B^{\prime}$ is a trivial cofibration in $\mathscr{C}$ then $B\to B^{\prime}$ is a weak equivalence in $\mathscr{D}$.
Definition 4.12. Let $\mathscr{X}$ be a simplicial Grothendieck topos. We define two categories $\mathbb{P}\left(\mathscr{X}\right):=\textbf{Fun}\left(\mathscr{X}^{op},% \mathscr{C}_{F}\mathscr{D}\right)$ $\mathbb{Q}\left(\mathscr{X}\right):=\textbf{Fun}\left(\mathscr{X}^{op},% \mathscr{C}\right)_{F^{\ast}}\textbf{Fun}\left(\mathscr{X}^{op},\mathscr{D}\right)$ where $F^{\ast}:\textbf{Fun}\left(\mathscr{X}^{op},\mathscr{C}\right)\to\textbf{Fun}% \left(\mathscr{X}^{op},\mathscr{D}\right)$ is the induced functor on the presheaf categories by composition on $F$.
Definition 3.6. Let $X$ be a simplicial set, define the $r$-fold edgewise subdivision of $X$ to be the simplicial set $\text{sd}_{r}X$ where $\text{sd}_{r}X_{n}=X_{r\left(n+1\right)-1}$ where the face and degeneracy maps can be described in terms of the face and degeneracies of $X$ in the following way: $d_{i}^{\prime}=d_{i}\cdot d_{i+\left(n+1\right)}\cdot\cdots\cdot d_{i+\left(r-% 1\right)\left(n+1\right)}:X_{r\left(n+1\right)-1}\to X_{rn-1}$ $s_{i}^{\prime}=s_{i+\left(r-1\right)\left(n+1\right)}\cdot\cdots\cdot s_{i+% \left(n+1\right)}\cdot s_{i}:X_{rn-1}\to X_{r\left(n+1\right)-1}$
Definition 2.7. Let $\Delta\mathfrak{G}$ be a crossed simplicial group and $\mathscr{C}$ be a category. A $\Delta\mathfrak{G}$-object in $\mathscr{C}$ is defined to be a functor: $X:\left(\Delta\mathfrak{G}\right)^{op}\to\mathscr{C}$ We shall denote such a functor as $X_{\ast}$ with $X_{n}$ being the image of $[n]$. If $\lambda:[m]\to[n]$ is a morphism in $\Delta\mathfrak{G}$ we shall denote by $\lambda^{\ast}:X_{n}\to X_{m}$ the associated morphism in $X\left(\lambda\right)$. We shall denote the category of all $\Delta\mathfrak{G}\text{-}\mathscr{C}$ objects as $\Delta\mathfrak{G}\text{-}\mathscr{C}$.
Definition 2.1. A crossed simplicial group is a category $\Delta\mathfrak{G}$ equipped with an embedding $i:\Delta\hookrightarrow\Delta\mathfrak{G}$ such that: (1) The functor $i$ is bijective on objects. (2) Any morphism $u:i[m]\to i[n]$ in $\Delta\mathfrak{G}$ can be uniquely written as $i\left(\phi\right)\circ g$ where $\phi:[m]\to[n]$ is a morphism in $\Delta$ and $g$ is an automorphism of $i[m]$ in $\Delta\mathfrak{G}$. We call this decomposition the canonical decomposition. To every crossed simplicial group $\Delta\mathfrak{G}$ we can assign a sequence of groups $\mathfrak{G}_{n}=\text{Aut}_{\Delta\mathfrak{G}}\left([n]\right)$.
Definition 4.2. Let $\mathscr{C}$ be a simplicial category, the category of simplicial presheaves is the functor category $\textbf{sPr}\left(\mathscr{C}\right)=\textbf{Fun}\left(\mathscr{C}^{op},% \textbf{sSets}\right)$
Definition 3.18. Let $\mathscr{C}$ and $\mathscr{D}$ be categories, and $F:\mathscr{C}\to\mathscr{D}$ a functor. Then the category $\mathscr{C}_{F}\mathscr{D}$ has: (1) Objects given by triples $\left(A,B,FA\to B\right)$ with $A\in\mathscr{C}$ and $B\in\mathscr{D}$. (2) Morphisms specified by maps $f_{1}:A\to A^{\prime}$ and $f_{2}:B\to B^{\prime}$ such that the following diagram commutes: \xymatrixFA \ar[r] \ar[d]_Ff_1 & B \ar[d]^f_2 FA’ \ar[r] & B’
Definition 4.10. A fibrant object $\mathscr{F}$ of $\Delta\mathfrak{G}\textbf{-Pr}^{s}\left(\mathscr{C}\right)$ with the pointwise model structure satisfies strong descent if for every hypercover $\mathscr{H}\to X$ the induced map of $\Delta\mathfrak{G}$-sets $\mathscr{F}\left(X\right)\to\text{Holim}_{[n]\in\Delta}\mathscr{F}\left(% \mathscr{H}_{n}\right)$ is a weak equivalence in $\Delta\mathfrak{G}\textbf{-Sets}^{s}$.
Definition 3.2. Let $\Delta\mathfrak{G}$ be a crossed simplicial group, then the standard $\Delta\mathfrak{G}$-set is given by: $\Delta\mathfrak{G}[n]=\text{Hom}_{\left(\Delta\mathfrak{G}\right)^{op}}\left([% n],-\right)$
Definition 3.28. The functor $R:\textbf{Top}^{SO\left(2\right)}\to\mathscr{P}$ takes $X$ to the triple $\left(S\left(X^{SO\left(2\right)}\right),S_{c}\left(X\right),\nabla S\left(X^{% SO\left(2\right)}\right)\to S_{c}\left(X\right)\right)$ Here the map $\nabla S\left(X^{SO\left(2\right)}\right)\to S_{c}\left(X\right)$ is the adjoint to the composite $|\nabla S\left(X^{SO\left(2\right)}\right)|_{|\Delta\mathfrak{C}|}\to|S\left(X% ^{SO\left(2\right)}\right)|\to X^{SO\left(2\right)}\hookrightarrow X$ A map $X\to Y$ in $\textbf{Top}^{SO\left(2\right)}$ induces maps $S\left(X^{SO\left(2\right)}\right)\to S\left(Y^{SO\left(2\right)}\right)$ and $S_{c}\left(X\right)\to S_{c}\left(Y\right)$ which fit in the following commutative diagram: \xymatrix∇S(X^SO(2)) \ar[r] \ar[d] & S_c(X) \ar[d] ∇S(Y^SO(2)) \ar[r] & S_c(Y)
Definition 4.16. A $\Delta\mathfrak{C}_{N}$-coupled presheaf on a small Grothendieck site $\mathscr{C}$ is an object of $\textbf{sPr}\left(\mathscr{C}\right)_{\left(\nabla^{N}\right)^{\ast}}\Delta% \mathfrak{C}_{N}\textbf{-Pr}\left(\mathscr{C}\right)^{s}$ We will denote by $\Delta\mathfrak{C}_{N}\textbf{-Pr}^{c}\left(\mathscr{C}\right)$ the category of all $\Delta\mathfrak{C}_{N}$ coupled presheaves on $\mathscr{C}$.
Definition 3.11. Let $X$ be a simplicial set, define the Segal subdivision of $X$ to be the simplicial set $\text{sq}X$ where $\text{sq}X_{n}=X_{2n+1}$ where the face and degeneracy maps can be described in terms of the face and degeneracies of $X$ in the following way: $d_{i}^{\prime}=d_{i}\cdot d_{2n+1-i}:X_{2n+1}\to X_{2n-1}$ $s_{i}^{\prime}=s_{2n-1}\cdot s_{i}:X_{2n-1}\to X_{2n+1}$
Definition 3.32. Let $\Xi_{n}=S_{|\Delta\mathfrak{D}|}\left(\Delta^{n}_{\mathbb{Z}/2}\right)$ where $\Delta^{n}_{\mathbb{Z}/2}:=\mathbb{Z}/2\times\Delta^{n}$. Then we have that $\Xi_{\ast}$ is a cosimplicial dihedral set. We then define $\Xi:\Delta\mathfrak{R}\textbf{-Sets}\to\Delta\mathfrak{D}\textbf{-Sets}^{s}$ $\Xi X=X\otimes_{\Delta\mathfrak{R}^{op}}\Xi_{\ast}$
Definition 2.12. A morphism $\rho:\widetilde{G}\to G$ is a connective covering if $\rho$ is a covering of its image and $p^{-1}\left(G_{e}\right)$ coincides with $\widetilde{G}_{e}$. A planar Lie group is a connective covering of $O\left(2\right)$.
Definition 3.23. Let $\nabla_{n}=S_{\Delta\mathfrak{C}}\left(|\Delta[n]|\right)$. Then we have that $\nabla_{\ast}$ is a cosimplicial cyclic set. We then define $\nabla:\textbf{sSets}\to\Delta\mathfrak{C}\textbf{-Sets}^{s}$ $\nabla X=X\otimes_{\Delta^{op}}\nabla_{\ast}$ This functor has a right adjoint $A$ given by $A\left(Y\right)_{n}=\text{Hom}_{\Delta\mathfrak{C}\textbf{-Sets}}\left(\nabla_% {n},Y\right)$
Definition 4.6. A hypercover of an object $X\in\mathscr{C}$ is the data of a simplicial presheaf $\mathscr{H}$ together with a morphism $\mathscr{H}\to X$ such that: (1) For any integer $n$, the presheaf $\mathscr{H}_{n}$ is a disjoint union of representable presheaves. (2) For any $n\geq 0$, the morphism of presheaves $\mathscr{H}_{n}\simeq\text{Hom}\left(\Delta^{n},\mathscr{H}\right)\to\text{Hom% }\left(\partial\Delta^{n},\mathscr{H}\right)\times_{\text{Hom}\left(\partial% \Delta^{n},X\right)}\text{Hom}\left(\Delta^{n},X\right)$ induces an epimorphism after sheafification.
Definition 1.3.6. Let $A$ be an abelian group. Then $A$ is called reduced if $A$ has no divisible subgroups other then $0$.
Definition 1.2.2. Let $p:\mathcal{J}\to\mathcal{I}$ be a functor between categories. The functor $p$ is said to be (left) cofinal if for every object $i$ in $\mathcal{I}$ the over category ${p}_{/i}$ is nonempty and connected.
Definition 1.3.17. An abelian group $G$ is called cotorsion if for every torsion free abelian group $A$, we have $\operatorname{Ext}(A,G)=0$.
Definition 3.1.5. Let $(\mathcal{C},U)$ be a quasi concrete category. A morphism $f$ in $\mathcal{C}$ is called surjective if the morphism $U(f)$ is surjective in $\operatorname{\mathcal{S}et}$.
Definition 1.3.1. Let $A$ be an abelian group. (1) If $n\in\mathbb{Z}$, we denote $nA:=\{na\>|\>a\in A\}.$ (2) If $a\in A$ and $n\in\mathbb{Z}$, we denote by $n|a$ the statement that $a\in nA$. (3) We say that $A$ is divisible if $\bigcap_{n=1}^{\infty}nA=A,$ that is, if for every $a\in A$ and every $n\geq 1$ we have $n|a$.
Definition 2.0.2. Let $H$ be an abelian group. (1) We say that $H$ satisfies the equation condition if every system of equations over $H$, with a matrix of the form $\xymatrix{1&l_{0}&0&0&\cdots\\ &1&l_{1}&0&\cdots\\ &&1&l_{2}&\cdots\\ &&&1&\cdots\\ &&&&\cdots}$ has a solution in $H$. (2) We say that $H$ satisfies the strong equation condition if every system of equations over $H$, with a matrix that is upper triangular and has identities in its diagonal, has a solution in $H$.
Definition 1.1.2. Let $G$ be a group. We define the abelianization of $G$ to be the quotient group $\operatorname{Ab}(G):=G/\operatorname{C}(G).$ It is easy to see that $\operatorname{Ab}(G)$ is an abelian group. If $f:G\to H$ is a group homomorphism, then we have shown that $f|_{\operatorname{C}(G)}:\operatorname{C}(G)\longrightarrow\operatorname{C}(H).$ It follows that we have an induced map $\operatorname{Ab}(f):\operatorname{Ab}(G)\longrightarrow\operatorname{Ab}(H).$ This turns $\operatorname{Ab}$ into a functor from the category of groups to the category of abelian groups $\operatorname{Ab}:\operatorname{\mathcal{G}rp}\longrightarrow\operatorname{% \mathcal{A}b}.$ Note that we have a projection map $\pi_{G}:G\longrightarrow\operatorname{Ab}(G),$ and this map is natural in $G$ in the sense that it defines a natural transformation of functors from groups to groups $\pi:\operatorname{Id}_{\operatorname{\mathcal{G}rp}}\longrightarrow% \operatorname{Ab}.$
Definition 1.3.9. Let $A$ be an abelian group and $p$ a prime number. We define, recursively, for every ordinal $\lambda$, a subgroup $p^{\lambda}A\subseteq A$, by: (1) $p^{0}A:=A$. (2) For every ordinal $\lambda$ we define $p^{\lambda+1}A:=p(p^{\lambda}A)$. (3) For every limit ordinal $\delta$ we define $p^{\delta}A:=\bigcap_{\lambda<\delta}p^{\lambda}A$. Clearly we have defined a function $p^{(-)}A$ from ordinals to sets, that is monotone decreasing and continuous. In particular, $p^{(-)}A$ stabilizes, that is, there exists an ordinal $\lambda$ such that $p^{\lambda+1}A=p^{\lambda}A$. The smallest such ordinal is called the $p$-length of A, and is denoted $\l_{p}(A)$.
Definition 4.0.1. Let $\alpha$ be an ordinal. We define $\operatorname{des}(\alpha):=\{(\mu_{1},...,\mu_{n})\>|\>\text{$n\geq 0$ and $% \alpha>\mu_{1}>\cdots>\mu_{n}$}\}.$ Note that $\operatorname{des}(\alpha)$ contains also the empty string $\phi$, corresponding to $n=0$. Let $\mu=(\mu_{1},...,\mu_{n})\in\operatorname{des}(\alpha)$. We define $\l(\alpha):=n\geq 0,$ $\min(\mu):=\begin{cases}\alpha&\text{ if $\mu=\phi$,}\\ \mu_{n}&\text{ if $\mu\neq\phi$,}\end{cases}$ and if $n\geq m\geq 0$ we define $\mu|_{m}:=(\mu_{1},...,\mu_{m})$.
Definition 1.3.4. Let $A$ be an abelian group. We define, recursively, for every ordinal $\lambda$, a subgroup $A^{\lambda}\subseteq A$, called the $\lambda$th Ulm subgroup of $A$, by: (1) $A^{0}:=A$. (2) For every ordinal $\lambda$ we define $A^{\lambda+1}:=\bigcap_{n=1}^{\infty}nA^{\lambda}$. (3) For every limit ordinal $\delta$ we define $A^{\delta}:=\bigcap_{\lambda<\delta}A^{\lambda}$. Clearly we have defined a function $A^{(-)}$ from ordinals to sets, that is monotone decreasing and continuous. In particular, $A^{(-)}$ stabilizes, that is, there exists an ordinal $\lambda$ such that $A^{\lambda+1}=A^{\lambda}$. The smallest such ordinal is called the Ulm length of $A$, and is denoted by $u(A)$. We always have $u(A)\leq|A|$.
